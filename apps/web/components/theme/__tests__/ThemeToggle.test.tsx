/**
 * UX-110: dark mode retired. ThemeToggle component deleted.
 * Original assertions are no longer applicable. Owner to delete this
 * file in a follow-up — kept here as a placeholder so the test suite
 * stays green and the deletion intent is documented.
 */
import { describe, it } from "vitest";

describe.skip("ThemeToggle (removed in UX-110)", () => {
  it("dark mode is no longer supported", () => {
    // intentional no-op
  });
});
