# Carelog — Master Backlog

> **This is the single source of truth for all planned work.** Every task — feature, bug, tech debt, infra, polish — is tracked here with a lifecycle status. Read this file **before** starting any task. Update it **immediately** when status changes. If it isn't here, it isn't planned. Run `/backlog-sync` at least once a day (and on session start) to reconcile against git/PRs.

Last consolidated: **2026-04-16** (codebase scan same day). Last update: **2026-05-20 parallel-2 oncall+journal sprint close** — ON-78 (PR #662 via s1) shipped the `shift_type` enum DB foundation (migration + regenerated types + pgTAP no-widen assertion); the web/mobile ShiftForm selector was NOT in the PR, so it's seeded as **ON-85** (P3, additive UI over the now-existing column). TD-141 (PR #661 via sM) unified the three divergent journal-body read policies through `pickJournalBody` (handoffSummary + VisitSummary now resolve the same body); the optional schema-collapse to a single `body` column is deferred — the `/oop` divergence finding that spawned the row is resolved. Also reconciled prior drift: TD-192 (PR #657, backend-techdebt sprint) flipped 🔎 In-review → ✅ Shipped; TD-187 (PR #656) was already shipped in the #658 sync. §0 Ready 17→16 (ON-78 + TD-141 out, ON-85 in); In review 1→0. **Process note:** neither parallel-2 track's claim persisted to main's `claims.json` — they lived as uncommitted worktree edits and the feature PRs correctly excluded them, so close cleanup was worktree-session-dir-only (no claims/INDEX rows to remove). Previous: **2026-05-20 parallel-4-ci-velocity sprint close** — TD-198/199/200/201 all shipped (PRs #649/#648/#650/#647): vitest 3-project matrix split + pgTAP `deps` gate, turbo type-check/lint cache, vitest node threads pool, Playwright `fullyParallel`+workers=2. Also fixed a required-check regression TD-198 introduced (matrix rename orphaned `Web —*` contexts on skip → re-pointed branch protection at `CI Summary`). See §7. §0 not adjusted here (rows were never in the Ready enumeration; `/backlog-sync` due). Previous: **2026-05-20 — Phase 7 Task-coordination & on-call epic seeded** (ON-77…ON-84) from a caregiver-flow conversation; ROADMAP §Phase 7 added. Foundation = ON-77 (task model + RLS + configurable role-based permissions, P1); ON-78 (on-call `shift_type` enum); ON-79–83 blocked on the foundation; ON-84 (verification layer) deferred. §0: Ready 17→19, Blocked 0→5, Deferred 18→19. Previous `/backlog-sync`: **2026-05-20 parallel-2-ci-velocity sprint close** — TD-195 (PR #641 via s2) + TD-197 (PR #642 via s3) both shipped; CI gating + tRPC contract-drift safety net land together. See §7 entry for details. §0 Ready: 19 → 17. Previous: **2026-05-19 parallel-2-oop-residuals sprint close (FIRST parallel-sprint flow test)** — 2 tracks shipped concurrently across s2 + s3 (operator ran `MAX_SESSIONS=3` override for s1+s2+s3): TD-174 (#636 via s3, clean first try) shipped FOR UPDATE annotation in `confirm_ocr_job` migration + pgTAP coverage comment; TD-191 (#637 via s2, 1 hotfix cycle) shipped 5 /oop residuals — useEditMode `onCancel` ref-of-latest pattern, React-warning regex tightening, formatMutationError gate-only cosmetic, ESLint TemplateLiteral asymmetry comment, pgTAP Case 9 explicit pre-state assertion. **Orchestrator hotfix c9102b5:** s2 added 16th pgTAP test in `update_emergency_info.test.sql` but forgot to bump `SELECT plan(15)→plan(16)` — CI RLS pgTAP failed; orchestrator took over per "Subagent Stalls → take over directly" rule; 1-line fix pushed; CI green; PR merged. **Process lessons from new parallel flow:** (1) `/parallel-sprint-close` skill assumes outcome.json files persist until close runs — operator manually `--close`d s2/s3 mid-sprint, wiping session dirs + claims; close fell back to manual `chore(backlog):` sync; document the dependency. (2) Track-executor opus-on-opus was skipped by s2 with rationale "plan-level already ran" — but plan-review covers DESIGN, track-review covers IMPLEMENTATION (would have caught the plan-count bug pre-CI). (3) MAX_SESSIONS=3 worked fine — no Opus quota events fired. (4) Pre-commit `--no-verify` justified by documented pnpm+worktree env gotcha. **TD-189 sprint (s1) was concurrently parked; state files wiped externally, must restart fresh.** §0 Ready: 19 → 17 (TD-174+TD-191 out via close). Previous: **2026-05-19 td-178-182-184 sprint close (session s2) + TD-192/193/194 seeds** — TD-178 (PR #632) shipped 2 throw-site migration to `Error("identity_*_failed", { cause })` form; TD-179 had already fixed the 3rd site. TD-182 (PR #633) shipped DB-side `.in("role", [...])` + exported `isRefillRecipient` predicate + 60-row volume test (caught /opus-on-opus cycle-1 must-fix on `.limit(50)` row-set widening). TD-184 (PR #634) shipped items 1+2 (rawArgs constant + auditOne split) — item 3 deferred. **Bonus deliverable:** multi-session race fix shipped to `~/.claude/scripts/sessions-state-update.sh` (mkdir-mutex + jq atomic update; refuses claim conflicts; race-tested with parallel claim-adds) + appendix in `~/.claude/docs/multi-session-setup.md`. **/oop --from-sprint:** 0 must-fix · 3 should-fix · 2 nit → TD-192 (extend stable-code pattern to membershipsRepository 5 sites + normalize update path + Sentry .cause hygiene), TD-193 (extract `resolveMembershipsToUsers` shared helper), TD-194 (adopt sessions-state-update.sh in /sessions-prune + /sprint --session skills). /a11y auto-skipped (backend-only sprint; tooling script only). /housekeeping-wave: no-op (wave touched only app code + tooling, no harness drift). §0 Ready: 19 → 19 (3 out via close, 3 in via TD-192/193/194). Sprint counter: 4 → 5 (next sprint at 6, NOT a Vercel-deploy cadence). **Process win:** /opus-on-opus cycle 1's must-fix on TD-182 caught the row-set widening BEFORE the wave — preserved limit-50 semantics via explicit DB role filter. Previous: **2026-05-19 — TD-152 flipped to ✅ Shipped (PR #557).** Previous: **2026-05-18 td-188-179 sprint close + TD-191 seed** — TD-188 (PR #622) shipped 3 sub-fixes for TD-186 /oop residuals: tightened `formatMutationError` PG constraint regex (`/violates (?:unique|foreign key|check) constraint "([^"]+)"/`, no longer over-strips `license_key`-style fields), removed unnecessary `flushSync` from `useEditMode` (caller-grep confirmed no consumer depended on synchronous flush; LOCKED JSDoc deleted with migration note), extended ESLint rule's `checkObjectExpression` to inspect TemplateLiteral keys (closes `Sentry.setTag(\`${prefix}_email\`, …)` bypass). TD-179 (PRs #623 / #626) shipped atomic `update_emergency_info` RPC: `jsonb_strip_nulls(contact_info || patch)` server-side merge eliminating JS read-merge-write race; SECURITY DEFINER + `search_path = public, pg_temp` + service_role-only grants; SQLSTATE-based JS error mapping (P0002 → recipient_not_found, P0IDF → identity_not_found; class P0 is documented PL/pgSQL user space). PR #626 hotfixed /oop must-fix: dropped raw `error.message` from catch-all `throw` (Postgres error strings can echo column values like `Key (email)=(jane@x.com)` — PHI leak path through Sentry); now surfaces SQLSTATE only (`identity_update_failed: ${error.code ?? "UNKNOWN"}`). PHI sentinel test asserts no PII fragments survive in thrown error. Also fixed: SQLSTATE `45IDF` → `P0IDF` (class 45 unassigned per spec); pgTAP `pg_proc` lookups schema-qualified with `pronamespace = 'public'::regnamespace` to prevent namespace-collision false positives. /opus-on-opus cycle 1 caught 2 must-fix (search_path `pg_catalog` → `pg_temp` matching project idiom; flushSync removal flipped LOCKED contract → mandated caller-grep + JSDoc migration note); cycle 2 caught CRITICAL SQLSTATE P0003 collision (reserved by Postgres for `too_many_rows`). /oop on merged code surfaced 1 must-fix (PHI leak, hotfixed in #626) + 5 should-fix/nit deferred as **TD-191** (P3 Low, ~1h): useEditMode `onCancel` ref-instability via factory closure capture, React-warning regex tightening (`/^Warning: /` anchor), formatMutationError gateInput allocation nit, TemplateLiteral asymmetry in `checkObjectExpression` (Identifier/Literal key paths skip TL bypass that ObjectExpression catches), pgTAP Case 9 cross-test state dependency. /a11y auto-skipped (backend-only sprint). §0 Ready: 15 → 19 (2 out via close, 1 in via TD-191, + TD-189 P1/High flagged & TD-190a-d seeded mid-sprint). Sprint counter: 3/4 (next sprint hits Vercel-deploy cadence). **Process win:** /opus-on-opus cycle 2's "nit" catch on SQLSTATE P0003 was actually CRITICAL — Postgres would have raised `too_many_rows` instead of `recipient_not_found`, breaking the entire error-mapping contract silently. Don't dismiss nits without reading them. Previous sync: **2026-05-18 td-186-183 sprint close + TD-187/188 seeds** — TD-186 (PR #619) shipped 3 sub-fixes for prior /oop residuals: useEditMode `onCancel?` arg with `flushSync` post-close ordering, new `apps/web/lib/formatMutationError.ts` (whitelist tRPC codes + strip Postgres constraint + Zod field-path prefix), ESLint rule extended to `setTag/setTags/setExtra/setExtras` with new `forbiddenTagKey` messageId for singular-form Literal-key inspection (17 formatter tests + 10 rule tests + 2 hook ordering tests; 2302 vitest pass). TD-183 (PR #620) shipped `isoWeekStamp` hoist to `@carelog/utils` + dead-code removal of `getWeekStamp` (verified zero consumers — original "weeklyDigest disagrees with refillAlert" framing was wrong). **2 seeds:** **TD-187** (`weeklyDigest` idempotency gap — discovered during TD-183 scoping; no `email_dispatch_log`-style guard; cron retry could re-send digests; P2 Medium, ~3h) and **TD-188** (TD-186 /oop residuals — `formatMutationError` constraint regex over-strips `license_key`-style fields; `flushSync` in useEditMode unnecessary + warning hazard; ESLint `forbiddenTagKey` misses TemplateLiteral; P3 Low, ~1h). /a11y: 0/0/2 nit (both pre-existing patterns, refactor neutral). /oop: 0 must-fix, 3 should-fix → TD-188. §0 Ready: 15 → 15 (2 out via close, 2 in via TD-187/188). **Subagent env note:** TD-183 subagent hit pnpm+worktree symlink mismatch — bypassed pre-commit via `--no-verify` after stash-comparison confirmed env-cause (CLAUDE.md gotcha). CI on fresh resolution passed. **Process win:** /opus-on-opus cycle 1 caught 2 real safety must-fixes (Sentry singular-form `setTag("email", x)` leak; raw `err.message` would leak Postgres constraint names + Zod schema paths) → led to `formatMutationError` helper being scoped into this sprint. Previous sync: **2026-05-18 td-181-180 sprint close + TD-186 seed** — TD-181 (PR #617) shipped PHI ESLint rule extension: new `resend.emails.send` target matcher + `ArrayExpression` walker recursion + `spreadIdentifier` warning (severity `error`, 0 codebase regressions). Scope correction: TD-181 row claimed Sentry.captureException + addBreadcrumb were uncovered; verified they were already covered. Real gaps were Resend + spread-id. `refillAlert.test.ts` PHI sentinel KEPT (grep-based detection of template-string interpolation + spread patterns + field-name STRINGS that keys-only static rule cannot replicate). 8 new rule tests (37 → 45). TD-180 (PR #616) shipped `useEditMode` factory-pattern hook + adopted in CareTeamList + EmergencyFooterCard. **Acceptance flag honestly raised:** line-reduction gate ≥15 not met (actual −2/−4) because plan's baseline estimate was inflated; user accepted for future-reuse value. /a11y: 0 must-fix, 0 should-fix, 1 nit (pre-existing focus-return pattern, not introduced by refactor). /oop --from-sprint: 0 must-fix, 3 should-fix → all 3 promoted to TD-186 (cancel-contract leak on direct `editMode.cancel()` invocation, generic-error swallowing in mutation `onError`, Sentry.setTag/setExtra rule gap). §0 Ready: 16 → 15 (2 out via close, 1 in via TD-186). **Process win:** /opus-on-opus cycle 1 caught 3 must-fix items pre-dispatch (hook signature wrong for tRPC config-time API; sentinel removal premature; Resend keys-only acceptance criteria missed real leak shape); cycle 2 clean. Previous sync: **2026-05-18 td-170de-pair sprint close + TD-185 seed** — TD-170d (PR #611) shipped E2E string-drift CI gate: 193-line `scripts/check-e2e-string-drift.mjs` + 14 unit tests + `e2e-string-drift` CI job. Cross-file move-aware single pass with `// e2e:no-references` allowlist. Retro-validation against PRs #525/#528/#529 confirmed all 3 historical drifts caught (exit 1, drift counts 1/1/2). TD-170e (PR #610) shipped 1-line testid swap for medication-tagging strict-mode violation (pre-existing latent bug). Side-quest: PR #612 landed `docs/handoff/` CareSync 2.0 prototype from Claude designer (168KB, 11 files, hash-routed 8 pages, mirrors burnt-orange Living Room palette). /oop --from-sprint on TD-170d surfaced **TD-185** (1 must-fix shell injection in `git()`+`grepE2eForString()` + 2 should-fix scope issues) — patched in **PR #613** (in review, auto-merge armed). §0 Ready: 18 → 16 (TD-170d/e out via close); In review: 0 → 1 (TD-185). **Process lesson:** investigation Phase 1 should inspect ALL CI shard logs in one pass — TD-170b initially only read shard 1, missing PR #528/#525 drifts that needed 2 additional firefighting PRs (#607, #608). Previous sync: **2026-05-17 ON-71 Phase 2 + TD-176 sprint close** — ON-71 closed end-to-end (Phase 1 da3cbb9 + Phase 2 PR #599 with batched-per-(org,recipient) Resend dispatch, new `email_dispatch_log` idempotency table, `getRefillRecipients` helper, plain-text-only email body, PHI sentinel test). TD-176 (PR #598) codified Sentry `component` tag convention across 3 surfaces (tRPC/API/Inngest) + swept 2 legacy sites. Pre-plan /owasp threat model (1 Crit + 4 High + 6 Med) fully mapped; /opus-on-opus 2 cycles to clean. /oop seeded TD-181 (ESLint rule extension for PHI in Sentry+Resend args), TD-182 (refactor PostgREST .or string), TD-183 (consolidate week-stamp helpers + fix `weeklyDigest` ISO bug), ON-71c (notification opt-out), ON-71d (bounce handling). §0 Ready 12 → 15 (5 in via TD-181/182/183 + ON-71c/d, 2 out via ON-71/TD-176 close). **Process win:** no backlog drift this sprint — pre-flight `git log --grep=<id>` on each row ID before planning caught nothing already-shipped, applying the lesson from Sprint 1 this session. Previous sync: **2026-05-17 UX-103b+UX-105b sprint close + 3 backlog-drift catches (UX-109/110/112) + TD-170 close** — UX-103b (PR #595) shipped CareTeamList edit mode (invite + remove via existing memberships mutations, coordinator-gated, two-click confirm). UX-105b (PR #596) shipped EmergencyFooterCard edit mode (DNR/contact/hospital via new identityRepository.updateEmergencyInfo + new recipients.updateEmergencyInfo tRPC; PHI-hardened with 2 sentinel tests asserting no name/phone leak to Sentry on zod-failure or repo-error paths). TD-170 (PR #592) closed: 2-line E2E helper fix for OTP confirm-button selector. **Drift cleanup:** UX-109/110/112 all shipped 2026-05-10 via PR #429/#430/#435 but never closed in §1 — flipped to ✅ Shipped this sync. /oop --from-sprint seeded TD-178 (stable identity error codes), TD-179 (jsonb_set RPC for updateEmergencyInfo), TD-180 (useEditMode hook DRY). §0 Ready: 11 → 12 (3 in, 2 out via UX-103b/105b close); Blocked: 1 → 0 (TD-170 closed). Lesson: future `/sprint` Step 2 should grep `git log` for the proposed row id BEFORE planning, not after writing the plan. Previous sync: **2026-05-17 TD-144+SEO-005+TD-173 sprint close** — TD-144 (PR #589) shipped atomic OCR confirm RPC closing the medication-duplication race; SEO-005 (PR #588) shipped Geist `display:swap` FOIT fix + CWV baseline doc (scope-honest close — larger image-deferral work N/A until UX-109); TD-173 (PR #587) shipped tab-role regression test for BriefingHandoffPanel. /oop --from-sprint surfaced TD-174 (annotate FOR UPDATE as load-bearing), TD-175 (backfill `scan_source='ocr_scan'` on pre-RPC OCR medications), TD-176 (standardize Sentry component tag convention API routes vs tRPC). Previous sync: **2026-05-17 4-track parallel wave close** — TD-153a (PR #582), TD-147 (PR #583), TD-111 (PR #584) merged; UX-065 (PR #585) auto-merge armed. /oop on D's module running in background. Process lesson: parallel subagents on the shared working tree collided — Track D stalled at commit, Track B used git plumbing to bypass pre-commit, Track C committed twice to escape the race. Next sprint MUST use `git worktree add` per CLAUDE.md ¶Worktree placement. Previous sync: **2026-05-17 deepest-night (td-172 sprint close — arc complete)** — TD-172 (PR #580) renamed `invite` Sentry path tag from `membership.error` to `caller.error` for cross-router alert-dashboard consistency. 5-sprint memberships error-handling arc (TD-167→168→169→171→172) fully closed. /oop depth pass deliberately skipped for this 4-line cosmetic close — value vs deploy-cost ratio was negligible. Previous sync: **2026-05-17 deeper-night (td-171 sprint close + TD-172 seed)** — TD-171 (PR #578) closed the final memberships error-handling gap: added PGRST116-filtered Sentry capture on `invite.membershipError`. Bundled the TD-169 should-fix (added `remove` PGRST116 test with `not.toHaveBeenCalled()` assertion). Memberships error-handling arc now complete across all 5 mutations. /oop --from-sprint surfaced TD-172 — tag inconsistency: `invite` uses `path: "membership.error"` vs `caller.error` everywhere else; alert dashboards filtering on `caller.error` across the router would miss invite captures. Previous sync: **2026-05-17 deep-night (td-169 sprint close + TD-170/171 seeds)** — TD-169 (PR #576) added Sentry capture on transport errors in `changeRole`/`remove` `callerError`/`targetError` branches (PGRST116-filtered to avoid spamming on legit no-rows). TD-170 (PR #575) seeded as P1 investigation: last 50 main CI runs are 0 success — E2E `page.waitForURL: Timeout 30000ms exceeded` across every auth-gated spec; sprint precondition E2E gate waived for this run with explicit user ack. /oop --from-sprint surfaced TD-171 — same structural gap on `invite.membershipError` (out-of-scope for TD-169's named surface). Previous sync: **2026-05-17 late-late-night (td-168 sprint close + Vercel preview-skip)** — TD-168 (PR #572) closed the changeRole/remove leak follow-up; `chore(vercel)` PR #573 added `vercel.json` `ignoreCommand` so non-main branches skip Vercel preview builds (free-tier deploy quota was being burned). /oop --from-sprint on TD-168 surfaced TD-169 (callerError branches in `changeRole`/`remove` discard Supabase transport errors silently as FORBIDDEN — should also Sentry-capture). Previous sync: **2026-05-17 late-night (td-167/155 sprint close)** — TD-167 (PR #569) swapped 5 invite-accept error-message leak sites; TD-155 (PR #570) closed no-op with design comment. /oop --from-sprint surfaced TD-168 — same class of leak in `changeRole` and `remove` tRPC mutations in the same file (3 more sites: `updateError.message`, `countError.message`, `deleteError.message`). Previous sync: **2026-05-17 night (td-142 sprint close)** — TD-142 shipped (PR #567) as deletion-only collapse: both production paths already used `supabaseAdmin.rpc("accept_invite", ...)`; the racy TS function was dead code. /oop --from-sprint surfaced TD-167 (`/api/invite/[token]/accept` route echoes raw Postgres error strings — pre-existing on main, not regressed). Previous sync: **2026-05-17 late-evening (live-test-tail sprint close)** — TD-166 / TD-154 / TD-159 shipped (PRs #563 / #564 / #565). No new rows surfaced by /oop (1 must-fix was a timing artifact — reviewer pre-merge; the 2 should-fixes were the same artifact + a low-priority inner-catch swallow on the pendingPlan path mitigated by TD-165's outer try/finally). Previous sync: **2026-05-17 evening (td-146-followups sprint close)** — TD-146 (Inngest cron audit), TD-164 (UUID-validate recipientId), TD-165 (network-catch on DashboardClient) shipped (PRs #559 / #560 / #561). TD-163 closed as already-resolved (PNGs are gitignored, never tracked). /oop --from-sprint surfaced TD-166 (`setLoading(false)` unreachable on pending_invite + pendingPlan redirect paths — pre-existing, not regressed). Previous sync: **2026-05-17 PM (observability-followups sprint close)** — TD-148 + TD-162 shipped (#555 / #556); TD-152 in review (#557 auto-merge armed). /oop --from-sprint surfaced TD-164 (validate recipientId UUID pre-router.push) + TD-165 (try/catch network-failure path complement to TD-152). Previous sync: **2026-05-17 (live-test-p1s sprint close)** — 3-track wave shipped TD-149/150/151 (PRs #551/#552/#553) restoring dashboard journal-write path + DMARC operator runbook. /oop --from-sprint surfaced TD-162 (apply TD-149 `count=estimated` fix to `ai.ts`) and TD-163 (delete stale `QuickLogFab` screenshot artifact). Previous sync: **2026-05-16 PM (SEC-001 close + TD-146 partial finding)** — operator walked the secrets-rotation runbook end-to-end. Rotated all 5 production secrets (Upstash, Resend, Inngest signing+event, Supabase JWT-signing-key, Stripe webhook). Supabase migrated off legacy `eyJ…` JWTs to the new `sb_secret_*`/`sb_publishable_*` format — required swapping `SUPABASE_SERVICE_ROLE_KEY` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` Production values to the new format before weekly-digest stopped failing with "Legacy API keys are disabled". TD-146 hypothesis #2 confirmed: Inngest crons DO fire and show under **Functions → Runs** (3 weekly-digest runs in last 24h); the "0 events in 24h" reading was Events-tab metric semantics (tracks `inngest.send()` calls, not cron triggers) — not breakage. Surfaced an unrelated bug: pre-rotation weekly-digest runs were silently failing with `Invalid API key` for an unknown duration due to Supabase's legacy-key disablement; rotation cleared the failure.

Replaces: `BACKLOG_PHASE2–5.md`, `BACKLOG_UI_REDESIGN.md`, `docs/superpowers/plans/CLAUDE_BACKLOG.md`. `BUILD_STATUS.md` and `TECH_DEBT.md` are **historical logs only** — new work is tracked here.

Human account-signup tasks (Supabase/Vercel/Stripe/etc.) live in `docs/project-info/runbooks/THIRD_PARTY_SETUP.md` and are referenced from §8.

---

Human Backlog Items:
- ability to schedule team wide meetings and embed zoom links


## Prioritization principle — UI styling Pareto (added 2026-05-16)

UI styling work obeys Pareto: ~20% of the styling effort gets the UI to ~80% — past the interpretability threshold where users can read and use the surface. The remaining 80% of polish effort (micro-spacing, motion, edge-case visual states, theming nuance) compounds slowly and should not block product progress.

**Rule:** when triaging UX-*, A11Y-*, PP-*, or impeccable-extracted polish rows, ask "does shipping this move the surface *from broken/illegible to readable*, or *from readable to refined*?"

- **From broken to readable** → keep at current priority. Interpretability fixes block usability.
- **From readable to refined** → demote. Move to §6 (Deferred UI polish) or stack at the bottom of §1. Revisit only after core feature work clears.

Apply during `/next-best-backlog-items` scoring and during `/backlog-sync` re-tiering. This is the user's explicit directive — don't override it without checking in.


## 0. Status board (at-a-glance)

Counts reflect items in §1–§6 only; §7 is the shipped log.

| Lifecycle | Count | Where |
|---|---|---|
| 🟢 Ready | 29 | **🚩 TD-189 (P1 / High — pick first)** · **🚩 ON-77 (P1 / High — Phase 7 Task epic foundation)** · TD-133 · TD-175 · TD-177 · TD-190a · TD-190b · TD-190c · TD-190d · TD-193 · TD-194 · UX-077 · SEC-008 · SEC-009 · PP-009 · ON-70 · ON-71c · ON-71d · ON-74 · ON-85 · **🚩 TD-202 (P1 — Stripe webhook drops billing events)** · **🚩 TD-203 (P1 — OCR stub stores fake data; verify prod exposure)** · TD-204 · TD-205 · TD-206 · TD-207 · TD-208 · TD-209 · TD-210 · TD-211 · TD-212 · TD-213 · TD-214 (+ rows in §3–§5 not enumerated) |
| 🔎 In review | 0 | — |
| 🟡 Spike | 1 | UX-046 (clinician-share surface) |
| 🔴 Blocked / Investigation | 5 | §5 Phase 7 Task epic — ON-79 (web UI, blocked by ON-77) · ON-80 (mobile, by ON-77/79) · ON-81 (notifications, by ON-77/78) · ON-82 (future/recurring, by ON-77/79/81) · ON-83 (recipient-initiated, by ON-77/79) |
| 🧊 Deferred | 19 | §5 ON-84 (Task verification layer — un-defer after core task loop ships) · ON-55 (ON-69 struck dup) · §1 UX-066 (superseded) · UX-035 (BriefHero now wired) · UX-051 (caresync.app DNS not provisioned) · TD-126 (LCP premise wrong; 132ms local) · **OOP-003 (messaging pgTAP already exists on main)** · **OOP-007 (`user_is_org_coordinator()` already exists + consumed; 0 inline checks remain)** · **TD-196 (Pact contract testing — revisit when mobile splits from web on independent Expo OTA cadence; intermediate snapshot tests in TD-197)** · SEO-003 (no /carezone-alternative; /about lacks step content) · ON-72/73 (Phase 3/4 prereqs) · §6 UX-08/09/11/22/23/24 · §3 PP-013 |
| 🧑 Needs human | 12 | §5 ON-54 · §8 A2 · C3 · PP-008 · §4 A11Y-018 · §1 LAUNCH-001 · LAUNCH-005 · TD-03 · TD-83 · UX-106 · SEO-006 · SEO-007 |

> If this table looks stale, run `/backlog-sync` — it rewrites it from the story rows below.

---

## Legend

| Tag | Meaning |
|---|---|
| 🟢 | **Ready** — scoped, unblocked, not yet picked up |
| ⚡ | **In progress** — an agent or human is actively working on it |
| 🔎 | **In review** — PR open, awaiting review or CI |
| 🧊 | **Deferred** — intentionally parked |
| ✅ | **Shipped** — moved to §7 |
| 🔴 | **Blocked** — prerequisite open; note `Blocked by:` inline |
| 🧑 | **Needs human** — account signup, env var, click-through — see §8 |

Every active row **must** include a `Status:` field (`Ready` / `In progress` / `In review` / `Blocked` / `Shipped`) and, when applicable, `Owner:` (agent name or human) and `Branch:`/`PR:` once work starts. `/backlog-sync` fills what it can infer.

**Story-ID prefixes**
- `ON-*` — general stories (mobile a11y, mechanical sweeps, large features)
- `PP-*` — platform parity (web/iOS/Android)
- `A11Y-*` — accessibility tooling
- `UX-*` — deferred UI redesign polish
- `TD-*` — tech debt (newly opened; historical items live in `docs/project-info/technology/TECH_DEBT.md`)
- `OOP-*` — invariant-preserving refactor candidates (see `docs/plans/2026-05-14-oop-refactor.md` and the `/oop` skill)
- `LAUNCH-*` — launch readiness (App Store, EAS, web go-live, observability, compliance)
- `P2-*`..`P5-*` — phase backlogs (all shipped, retained as a log in §7)
- `B*`/`D*`/`A*`/`C*` — before-launch Claude tasks (shipped where no 🧑 gate)

---

## 1. Active / next-up

| ID | Status | Owner | Branch / PR | Story | Notes |
|---|---|---|---|---|---|
| TD-215 | 🟢 Ready · **P2 / Medium** | — | — | **Sweep ✅ Shipped/Closed rows out of §1 into the §7 compact log** | Backlog hygiene (dogfoods the §11 row template). **Surface (verified 2026-05-20):** `BACKLOG.md` §1 (lines ~73–518) — `sed -n '73,518p' BACKLOG.md \| grep -cE '^\| [A-Z]+-[0-9]'` = 251 table rows, of which **198 are `✅ Shipped`/`✅ Closed`** and only 53 active. **Assertion of current state:** `/backlog-sync` flips status in place but never relocates shipped rows to §7, so §1 has accumulated 198 inert rows that bloat the active section and were the bulk of the parallel-PR collision surface TD-189 sharding aimed at. **Fix:** extend `/backlog-sync` (or a one-shot `chore(backlog):` sweep) to move each `✅ Shipped`/`✅ Closed` §1 row into the §7 compact log (preserving PR ref + one-line resolution), leaving §1 to the ~53 active rows. **Risk:** LOW (doc-only; no code). **Size:** M (~half-day — content preservation across 198 rows; assert total `grep -cE '^\| [A-Z]+-[0-9]'` count conserved before/after). **Verification command:** `b=$(grep -cE '^\| [A-Z]+-[0-9]' BACKLOG.md); echo "total rows $b must be conserved post-sweep"`. Supersedes TD-189 Track A (sharding) — see `docs/plans/2026-05-18-td-189-backlog-hygiene-upgrades.md` §PIVOT. |
| ON-77 | 🟢 Ready · **P1 / High** | — | — | **Task data model + RLS + configurable role-based permissions** | Foundational row for the Phase 7 Task-coordination epic (ROADMAP §Phase 7); **blocks ON-79/80/81/82/83/84**. Surfaced 2026-05-20 from a caregiver flow: a coordinator (or the recipient) raises a discrete activity that must be completed, optionally pinned to a shift, with role-gated completion and lifecycle tracking. **Schema:** new `tasks` table — `id, org_id, recipient_id, title, instructions text (markdown), checklist jsonb (ordered clickable to-do items: [{label, done}]), status task_status, assigned_to uuid REFERENCES auth.users, created_by, requested_by uuid (the human who raised it), requested_on_behalf_of uuid NULL REFERENCES care_recipients (set when the recipient is the originator — see ON-83), shift_id uuid NULL REFERENCES shifts(id) ON DELETE SET NULL (pin to current OR future shift), due_at timestamptz NULL, completed_at, completed_by`. New `task_status` enum (`todo`, `in_progress`, `done`, `cancelled`) — string-literal-union on the TS side per code style. **Role-based access (configurable per org by admin):** a capability map deciding (a) which roles may CREATE tasks and (b) which roles may COMPLETE a given task. **Open design decision (resolve in plan):** store as a per-org `task_permissions` config (jsonb on organizations or a dedicated table) keyed by role→capability, vs. a per-task `allowed_completer_roles` column, vs. both. Default posture: coordinators always create+complete; admin can widen creation to other roles. RLS via existing `user_can_access_recipient()` / `user_is_org_coordinator()` helpers + a new completion-permission predicate; **pgTAP coverage required** (cross-org rejection, role-gated completion allow/deny, recipient-scope). **Risk:** MEDIUM (new RLS surface — get the completion predicate right). **Size:** L (~1–2 days: migration + enum + RLS + pgTAP + types regen). |
| ON-78 | ✅ Shipped · 2026-05-20 · PR #662 (DB foundation only; web/mobile ShiftForm selector → ON-85) | — | — | **On-call shift type (`shift_type` enum on `shifts`)** | Part of the Phase 7 Task epic (ROADMAP §Phase 7). Adds a shift *type* dimension orthogonal to the existing `shift_status` lifecycle (open/claimed/confirmed/completed/missed). **Schema:** new `shift_type` enum (`standard`, `on_call`); add `shift_type shift_type NOT NULL DEFAULT 'standard'` column to `shifts`; backfill existing rows to `standard` (the DEFAULT covers this). An on-call shift designates its `assigned_to` caregiver as the routing target for task requests arriving during `[starts_at, ends_at)` — consumed by ON-81 (notifications). **Web + mobile shift-create UI** gains a type selector (can fold into this row or split a thin UI follow-up). RLS unchanged (inherits `shifts` policies); add a pgTAP assertion that the new column doesn't widen access. Independent of ON-77 — touches `shifts`, not `tasks`. **Risk:** LOW (additive enum + column + DEFAULT backfill). **Size:** M (~half day: migration + enum + type selector UI + types regen). |
| ON-85 | 🟢 Ready · **P3 / Low** | — | — | **On-call shift-type selector in shift-create UI (web + mobile)** | Follow-up to ON-78 (PR #662), which shipped the `shift_type` enum + column on `shifts` but no UI. Add a `standard`/`on_call` selector to the web ShiftForm (`apps/web/app/(app)/journal/[recipientId]/ShiftForm.tsx` — default `standard`, wired into the create payload, labelled control + focus ring + tokens per `.claude/rules/ui-standards.md`) and the mobile shift-create screen; update `ShiftForm.test.tsx`. The DB column already exists, so this is additive UI only. **Risk:** LOW. **Size:** S (~1h web; mobile is a thin follow-up). |
| TD-147 | ✅ Shipped · 2026-05-17 · PR #583 | — | — | **Decision-doc surfaces consolidated to two canonical (ADR + UX_DECISIONS)** | Resolution: codified two-surface convention (ADR for architecture, UX_DECISIONS for product/language) in root CLAUDE.md §Decision docs. Cross-referenced header notes added to `docs/adr/README.md` and `UX_DECISIONS.md`. Plan-embedded "Guiding decisions" in `audit-remediation-2026-05-09.md` archived with disposition of all 5 entries; entry 5 (skill-dedup no-delete rule) flagged for `.claude/CLAUDE.md` integration. |
| TD-152 | ✅ Shipped · 2026-05-17 · PR #557 | — | — | **Wire Sentry around Quick log handlers + `care_events` REST failure path** | Resolution: Sentry instrumentation added around dashboard Quick log handlers + `care_events` REST count fallback. Merged 2026-05-17. |
| TD-153 | ✅ Shipped (Path A) · 2026-05-17 · PR #582 | — | — | **Happy-path runbook reconciled to current prod (Path A — doc rewrite)** | Resolution: §2 rewritten to match current production (passwordless 6-digit OTP, dashboard-as-primary-care-surface, correct Settings tabs, no Stripe Checkout). Anchor scan confirmed no live-test consumers. Out-of-scope callout points the removed-screens-rebuild question at Path B (separately deferred — open as TD-153b if/when product decides to rebuild journal/medications routes or in-Settings team invite). |
| TD-154 | ✅ Shipped · 2026-05-17 · PR #564 | — | — | **OTP error wording conflates "invalid" with "expired"** | Resolution: deduplicated both branches in `friendlyOtpError()` to `"That code didn't work. Check the digits or send a new code."` Security-equivalent (no wrong-vs-expired oracle); no user pushed to discard a valid code. |
| TD-155 | ✅ Shipped · 2026-05-17 · PR #570 | — | — | **Confirm OTP rate limiter is keyed on email, not IP** | Resolution: no-op investigation. The carelog Upstash middleware (`apps/web/lib/rateLimit.ts`) is correctly IP-keyed for `auth/verify` — defends against rotating-IP brute force on a single victim's 6-digit code. Live-test 429s came from Supabase Auth's own send-rate limiter (SignInForm calls `supabase.auth.signInWithOtp` client-direct), not carelog middleware. PR #570 adds a design-note comment documenting the choice. |
| TD-156 | 🟢 Ready · **P3 / Low** | — | — | **Supabase Auth email subjects differ for new vs existing users (minor inbox enumeration)** | Surfaced 2026-05-17 by Cowork auth-unhappy suite run `1778990299` scenario 4. UI is correctly indistinguishable for new vs existing emails ("Check your email…"), but Supabase Auth dispatches different *email templates* server-side: new users get `"Confirm Your Signup"`, existing users get `"Your Magic Link"`. Confirmed via Resend logs (email IDs `43891be0-…` for new, `f5498db3-…` / `f439c728-…` for existing). Practical exploit value: essentially nil — an attacker with mailbox access already has full compromise. But it's worth standardizing the templates for clean threat model. **Fix:** in Supabase Dashboard → Auth → Email Templates, set "Confirm Signup" and "Magic Link" templates to identical subject lines (e.g. both `"Your CareSync sign-in code"`). **Risk:** LOW. **Size:** XS (~10 min in Supabase dashboard). |
| TD-157 | 🟡 Spike · **P2 / Medium** | — | — | **Spike: does requesting a fresh OTP revoke existing sessions for same user?** | Surfaced 2026-05-17 by Cowork auth-unhappy suite run `1778990299` scenario 5 (inconclusive). Empirical signal: Tab A signed in successfully (session cookie present, `/dashboard` rendered); after Tab B fired a fresh OTP request for the same email, Tab A's URL was back at `/signin`. If reproducible, requesting a new OTP for an existing user revokes their existing sessions — atypical for Supabase Auth and potentially user-hostile (a malicious actor knowing an email could log a user out by triggering OTP requests). Tab navigation noise during the test makes attribution unreliable. **Repro steps:** sign in with email X in tab A; from tab B, fire OTP request for email X; immediately check tab A's session via `/dashboard` reload (no human-driven tab switches). Document actual behavior. If it reproduces, file as a bug-fix follow-up. **Risk:** LOW (read-only investigation). **Size:** XS (~20 min). |
| TD-158 | 🟢 Ready · **P3 / Low** | — | — | **Tighten PostgREST `PGRST205` schema-suggestion leak** | Surfaced 2026-05-17 by Cowork RLS suite run `1778990299`. Querying a non-existent table (e.g. `/rest/v1/recipients` when actual is `care_recipients`) returns `404 PGRST205` with body `"Perhaps you meant the table 'public.care_recipients'"`. Authenticated users (i.e. anyone with a valid Supabase JWT) can use this for free schema introspection. **Severity:** low — table names aren't secrets and only authenticated users see this. But worth confirming PostgREST `db-extra-search-path` and `db-schemas` config tightness (Supabase project Settings → API → Exposed schemas). If both are set to `public` only, that's fine; if broader, narrow. **Risk:** LOW. **Size:** XS (~15 min config check). |
| TD-159 | ✅ Shipped · 2026-05-17 · PR #565 | — | — | **"Export care history (coordinators only)" label visible to non-coordinators** | Resolution: wrapped the link in new `HistoryExportLink` component that gates on `role=coordinator AND accepted_at IS NOT NULL` client-side; "(coordinators only)" sub-label removed. Server-side gate at `history-export/page.tsx` unchanged. Backlog option (a) — capability hidden, not soft-nudged. |
| TD-160 | 🟢 Ready · **P2 / Medium** | — | — | **Manually verify Stripe webhook secret rotation actually took** | Surfaced 2026-05-17 by Cowork Stripe suite run `1778990299`. Cowork could not verify the post-SEC-001 webhook secret rotation: (a) the Stripe MCP doesn't expose `/v1/events` or `/v1/webhook_endpoints`; (b) Stripe dashboard is blocked by Chrome safety restrictions; (c) Stripe account is empty so no recent deliveries exist to assert on (Checkout doesn't exist in flow). **Manual verification steps:** (1) operator opens Stripe Dashboard → Developers → Webhooks → care-log.org/api/stripe/webhook → Attempts; OR (2) `stripe events list --limit 5` from local Stripe CLI; OR (3) `stripe trigger invoke.payment_succeeded` and confirm 200 in webhook attempts. Any 400 = rotation didn't take and `STRIPE_WEBHOOK_SECRET` in Vercel needs re-paste. **Risk:** MEDIUM (if rotation failed silently, real live-mode webhook traffic would 400 once enabled). **Size:** XS (~5 min manual). |
| TD-161 | 🟢 Ready · **P3 / Low** | — | — | **Delete stray Stripe-CLI test products (`prod_URGgNaNkicIb2X`, `prod_URGfzM4RzfC0A0`)** | Surfaced 2026-05-17 by Cowork Stripe suite. Two test-mode products both named "myproduct", created by Stripe CLI testing. Harmless but cluttering the Products list. Verify they're not referenced by any subscriptions / prices, then delete via dashboard or `stripe products delete <id>`. **Risk:** LOW. **Size:** XS (~5 min). |
| TD-163 | ✅ Closed · 2026-05-17 (no-op) | — | — | **Delete stale screenshot artifact under `QuickLogFab.test.tsx/__screenshots__/`** | Resolution: `.gitignore:38` already covers `**/__tests__/**/__screenshots__/` — the "orphan" PNGs are local-only vitest artifacts, never tracked in git. No PR needed; running `rm` on local artifacts is operator-discretion. |
| TD-166 | ✅ Shipped · 2026-05-17 · PR #563 | — | — | **`setLoading(false)` unreachable on `pending_invite` + `pendingPlan` redirect paths in DashboardClient** | Resolution: call `setLoading(false)` explicitly before each early `return` (pending_invite bridge, pendingPlan-success bridge). +2 vitest cases asserting skeleton clears post-redirect. |
| TD-167 | ✅ Shipped · 2026-05-17 · PR #569 | — | — | **`/api/invite/[token]/accept` route echoes raw Postgres error messages to client** | Resolution: 5 leak sites (HTTP route + tRPC mutation) swapped to generic client messages + Sentry-side `captureException` with `tags: { component, path }`. Documented sentinels preserve UX copy; unknown sentinels fire Sentry capture. +3 route tests, +1 router test, 1 router test updated. |
| TD-168 | ✅ Shipped · 2026-05-17 · PR #572 | — | — | **`changeRole` + `remove` tRPC mutations echo raw Postgres error messages to client** | Resolution: 3 leak sites swapped to generic client messages (`"Failed to change role"` / `"Failed to remove member"`) + `Sentry.captureException(wrapAdminError(error), { tags: { component, path } })` + `cause: rawError` preserved server-side. +3 vitest cases pinning generic message + Sentry tags. |
| TD-170 | ✅ Shipped · 2026-05-17 · PR #592 | — | — | **E2E (Playwright) chronically red on `main` — every auth-gated test fails with `page.waitForURL: Timeout 30000ms exceeded`** | Surfaced 2026-05-17 by `/sprint` preflight check. Last 50 main CI runs: 24 failure / 26 cancelled / **0 success**. Failure pattern is uniform across AI-assistant, OCR review, onboarding, notification-preferences specs — tests successfully start the sign-in flow but never land on the post-auth route within 30s. Hypotheses (ranked): (1) **Supabase Auth test-mode bypass broken** — Playwright suite likely depends on an `auth.signInWithPassword`/OTP-bypass path that regressed; check `e2e/fixtures/` + `playwright.config.ts` for session-injection seams. (2) **CI env-var drift post-SEC-001 rotation (2026-05-16)** — `SUPABASE_SERVICE_ROLE_KEY` swapped from legacy `eyJ…` to `sb_secret_*` format; if the test fixture uses the legacy key shape it would silently fall back to anon. (3) **Session-cookie domain mismatch** — Next.js 16 `proxy.ts` cookie API + Playwright browser-context cookies not being persisted. (4) **Vercel preview URL dependency** — if Playwright targets a preview deploy that's now skipped per PR #573, no app to test against. **Risk:** HIGH — masks all real E2E regressions; merge gate functionally relies on vitest + typecheck + adversarial review only. TD-75 green-streak gate either not wired or has been red for its entire window. **Size:** M (~2-4h investigation; fix size unknown until root cause). **Sequencing:** unblocks gate restoration; pairs with TD-83 (verify branch-protection required checks include E2E). |
| TD-169 | ✅ Shipped · 2026-05-17 · PR #576 | — | — | **`callerError`/`targetError` branches in `changeRole` + `remove` swallow Supabase transport errors without Sentry capture** | Resolution: 4 branches instrumented (changeRole×2, remove×2). PGRST116-filtered: `error.code !== "PGRST116"` → Sentry capture; PGRST116 (legitimate "no rows" from `.single()`) → silent FORBIDDEN/NOT_FOUND as before. +4 vitest cases pin the filter; existing FORBIDDEN/NOT_FOUND mocks in both security + logic test files updated to real PGRST116 shape (semantically always meant "no rows"). Client messages unchanged. |
| TD-171 | ✅ Shipped · 2026-05-17 · PR #578 | — | — | **`memberships.invite` membershipError-on-transport gap** | Resolution: PGRST116-filtered Sentry capture added on `invite.membershipError` (mirrors TD-169 pattern on changeRole/remove). Memberships error-handling arc now uniform across all 5 mutations (invite, accept, changeRole×2, remove×2). Bundled the TD-169 should-fix in same PR (added `remove` PGRST116 test with `not.toHaveBeenCalled()` assertion). |
| TD-172 | ✅ Shipped · 2026-05-17 · PR #580 | — | — | **`memberships.invite` Sentry tag inconsistency** | Resolution: renamed `path: "membership.error"` to `path: "caller.error"` in the invite mutation; test assertion updated to match. Memberships error-handling arc fully uniform across all 5 mutations. |
| ON-49 | ✅ Shipped · PR #108 | — | — | **Shift completion → handoff note prompt** | When a shift transitions to `completed`, show inline prompt (web ShiftList + mobile schedule) for an optional handoff note. Creates a `care_event` with `entry_type='handoff'`. The `handoff` enum value already exists in the DB. |
| ON-50 | ✅ Shipped · PR #106 | — | — | **Weekly digest: medications adherence section** | Add a missed-dose summary to the Sunday Inngest digest. Query `care_events` for `event_type='medication'` last week, surface missed vs given count. `weeklyDigest.ts` already has journal + mood + shifts but no meds section. |
| ON-51 | ✅ Shipped · PR #109 | — | — | **Aide recipient-scoping in invite + team admin** | When inviting as role='aide', show a recipient picker that sets `recipient_id` on the membership row. DB already has `recipient_id` on `memberships` with an index; the invite form and TeamAdmin currently ignore it. |
| ON-52 | ✅ Shipped · PR #101 | — | — | **Care history depth counter on dashboard** | Shows care event count + months of history per team; parallel Supabase queries + `formatCareStats` pure helper + 6 unit tests. |
| ON-53 | ✅ Shipped · PR #100 | — | — | **CareZone alternative landing page** | `/carezone-alternative` hero, CareZone comparison table, medication import preview tool; MarketingNav linked ("CareZone users"). **Note (2026-05-09):** standalone page consolidated into `/about` via PRs #316/#317 — see `apps/web/app/(marketing)/about/page.tsx` (`CompareTable`, `CareZoneMedicationImport`). |
| ON-57 | ✅ Shipped · PR #105 | — | — | **Family referral share link** | Coordinator dashboard button: "Refer Carelog to another family." Generates a shareable `/signup?ref=<orgSlug>` URL (new-org referral, not a team invite). PostHog tracks `referral_shared` + `referral_converted` events. Referral source stored on new org row. Key KPI: 60% referral rate by month 6 (PRODUCT_STRATEGY.md). ~1 day. |
| ON-58 | ✅ Shipped · PR #103 | — | — | **Analytics: onboarding + retention funnel events** | Add PostHog events: `onboarding_step_completed` (step name, elapsed_ms), `first_care_event_created` (elapsed_ms since signup), `team_member_invited` (team_size property). Powers PRODUCT_STRATEGY.md KPIs: "time to first care event < 10 min" + "week 4 retention 70%+." PHI rule: UUID only — no names or emails. ~0.5 day. |

### Code-quality audit (TD-202..214) — opened 2026-05-20

Surfaced by a whole-project `/interpretable` + `/robust` audit (4 parallel read-only auditors over server/jobs, lib/api, components/hooks, supabase/packages/mobile). Severities are the auditors' (`must-fix`/`should-fix`/`nit`) mapped to P1–P3. The three highest-stakes must-fixes (TD-202/203/208) were spot-verified against source on 2026-05-20; the rest are auditor-reported and should be re-verified at pickup (per TD-189 `/verify-row` discipline).

| ID | Status | Owner | Branch / PR | Story | Notes |
|---|---|---|---|---|---|
| TD-202 | 🟢 Ready · **P1 / High — pick first** | — | — | **Stripe webhook swallows handler errors and returns 200 — billing events silently dropped** | **Verified 2026-05-20** at `apps/web/app/api/stripe/webhook/route.ts:43-67`: the event-handler `try { await handlers[event.type]?.(event) } catch (e) { logger.error(...); posthog.capture(...) }` falls through to `return NextResponse.json({ received: true })` (HTTP 200). Stripe treats 200 as successful delivery and **never retries**, so a transient DB failure during subscription provisioning (or an unhandled event type) silently desyncs a paying customer's subscription state — no retry, no alert beyond a log line. The dedup-insert path correctly returns 500 (line ~35), proving the pattern is known; the handler path doesn't. **Fix:** return 500 (`{ error: "handler failed" }`) from the catch so Stripe redelivers; keep the PostHog capture. Add a webhook-handler-throws test asserting non-2xx. **Risk:** MEDIUM (billing correctness; one-line behavior change — confirm idempotency of handlers under redelivery first, they already dedup via `stripe_event` insert). **Size:** S (~1h). **Verify cmd:** `sed -n '43,68p' apps/web/app/api/stripe/webhook/route.ts`. |
| TD-203 | 🟢 Ready · **P1 / High — verify prod exposure first** | — | — | **OCR Inngest functions return a hardcoded stub regardless of `OCR_API_KEY` — real OCR never wired** | **Verified 2026-05-20** at `apps/web/inngest/functions/ocrPrescription.ts:59-62` (and `ocrDocument.ts` mirror): `const apiKey = process.env.OCR_API_KEY; if (!apiKey \|\| !job) return "Lisinopril 10mg…"; // Real OCR call would go here \n return "Lisinopril 10mg…";` — **both** branches return the same stub, so even with the key set the real OCR path never fires; every prescription/document scan stores hardcoded fake data as if parsed. The "Real OCR call would go here" comment masks this from reviewers. **Two coupled robustness gaps in the same files:** (a) `mark-processing` step UPDATEs status unconditionally → an Inngest retry after a later step fails re-stamps a now-`needs_review`/`confirmed` row back to `processing` (add `.eq("status","pending")`); (b) `fetch-job` uses `.single()` but discards `error` → invalid jobId yields `job=null` and the function continues, storing stub text as a real result (add `if (error \|\| !data) throw`). **FIRST ACTION at pickup:** determine whether the scan UI is live in prod — if users can upload scans, this is a data-integrity incident (bump to P1 urgent); if the feature is gated/unexposed it's an unfinished-feature row (P2). **Fix:** wire real OCR (or throw when key is set but unimplemented, instead of silently stubbing) + the two retry/error guards. **Risk:** HIGH if exposed (corrupt medication data) / LOW if gated. **Size:** M (real OCR integration) or S (guard + honest-stub + error handling only). **Verify cmd:** `sed -n '55,63p' apps/web/inngest/functions/ocrPrescription.ts`. |
| TD-204 | 🟢 Ready · **P2 / Medium** | — | — | **Stripe checkout route: undefined `priceId`, non-atomic customer-create, swallowed `.single()` errors** | Auditor-reported (re-verify at pickup), `apps/web/app/api/stripe/checkout/route.ts`: (1) `:78` `priceId` is `string \| undefined` (from env var) passed to Stripe with no null-guard → uncaught TypeError if the env var is missing; add `if (!priceId) return 400`. (2) `:65` `customers.create(...)` then `supabase.update(stripe_id)` with no try/catch → if the DB update fails, a dangling Stripe customer exists with no org link, corrupting retry state; wrap in try/catch or use a DB-first idempotency key. (3) `:27` `memberships`/`organizations` `.single()` discard `error` → a DB error returns `data:null`, masked as 403/404; check `error`, return 500. Same `.single()` error-discard at `apps/web/app/api/stripe/portal/route.ts:24`. **Risk:** MEDIUM (billing entry path). **Size:** S (~1-2h). |
| TD-205 | 🟢 Ready · **P2 / Medium** | — | — | **`buildHistoryExport` discards Supabase query errors → silent empty export snapshots** | Auditor-reported (re-verify), `apps/web/lib/buildHistoryExport.ts:111`: five queries (careEvents, medications, symptomReadings, eolPlan, documents) destructure only `data` and drop `error` → a DB failure yields silently-empty sections in a coordinator's exported care history, indistinguishable from "no data" (a real correctness/trust problem for a medical export). **Fix:** check `error` on each and throw (fail the export loudly) rather than emit a partial snapshot. Also `:85` `SupabaseClient<any>` in the public signature → use `SupabaseClient<Database>`. **Risk:** MEDIUM (data-integrity of a user-facing export). **Size:** S (~1h). |
| TD-206 | 🟢 Ready · **P2 / Medium** | — | — | **Journal hooks swallow mutation/fetch errors → silent local-state corruption, no user feedback** | Auditor-reported (re-verify), `apps/web/hooks/`: (1) `useJournalActions.ts:88` `handleFlag` has no try/catch — a failed PATCH still applies the optimistic flag flip locally, so the UI shows flagged while the server is unflagged, no toast, no rollback. (2) `useJournalActions.ts:66` `handlePost` (online path) uses `finally` not `catch` → network error leaves `posting=false` with no surfaced error. (3) `useJournalData.ts:86` `loadMembers` has no try/catch → fetch failure silently leaves `members=[]` + `currentUserRole` unset. **Fix:** wrap each in try/catch, revert optimistic state on failure, `toast.error`. **Risk:** MEDIUM (silent data divergence in the core journal flow). **Size:** S (~1-2h). |
| TD-207 | 🟢 Ready · **P2 / Medium · overlaps /owasp** | — | — | **SECURITY DEFINER functions missing pinned `search_path` (search_path-hijack hardening)** | Auditor-reported (re-verify), supabase migrations: (1) `20260427000000_sec_high_fixes.sql:30` `claim_outer_circle_slot` sets `search_path = public` but omits `pg_temp` (project idiom elsewhere is `public, pg_temp`). (2) `20260509000000_shift_questions.sql` `shift_questions_immutable_cols()` trigger function lacks `SECURITY DEFINER` + `SET search_path = public, pg_temp`. These are search_path-hijack hardening — **note the overlap with `/owasp`**; route final severity through a security pass. New migration(s) to `CREATE OR REPLACE` with the pinned path + pgTAP. **Risk:** MEDIUM. **Size:** S. |
| TD-208 | 🟢 Ready · **P3 / Low** | — | — | **`accept_invite` activates membership without a ROW_COUNT check → phantom `success=true`** | **Verified 2026-05-20** at `supabase/migrations/20260407000000_atomic_invite_accept.sql`: the token-consume UPDATE correctly uses `GET DIAGNOSTICS … ROW_COUNT` and returns `success=false` on 0 rows, but the subsequent `UPDATE memberships … WHERE id = v_invite.membership_id` sets `v_result.success := true` with **no** ROW_COUNT guard. If that row is missing/stale, the UPDATE matches 0 rows yet the caller is told the membership activated. Low severity in practice (`membership_id` is FK-derived from the invite), but a real defensive gap. **Fix:** `GET DIAGNOSTICS v_rows = ROW_COUNT; IF v_rows = 0 THEN RAISE EXCEPTION 'membership_not_found'; END IF;` + pgTAP case. **Risk:** LOW. **Size:** XS (~20 min). |
| TD-209 | 🟢 Ready · **P3 / Low** | — | — | **`weeklyDigest` can re-send a digest after the 15-min pending-sweep window (idempotency edge)** | Auditor-reported (re-verify), `apps/web/inngest/functions/weeklyDigest.ts:~120`: `email_dispatch_log` INSERT → `resend.emails.send` → `sent_at` UPDATE inside one `step.run` per org. A retry within 15 min hits the unique constraint correctly, but a retry *after* the sweep clears the pending row could re-send. **Fix:** persist `sent_at` atomically in the same INSERT, or tighten/condition the sweep so a sent row is never cleared. **Risk:** LOW (duplicate weekly email; not data-corrupting). **Size:** S. |
| TD-210 | 🟢 Ready · **P3 / Low** | — | — | **Missing CHECK constraints + magic-literal caps on dispatch/payload schemas** | Auditor-reported (re-verify): (1) `supabase/migrations/20260517010000_create_email_dispatch_log.sql:9` — `kind` is unconstrained `text`; add `CHECK (kind IN ('refill','weekly_digest',…))` so an invalid dedup-key namespace can't silently fragment idempotency. (2) `packages/schemas/src/careEvents.ts` — `journalPayload` max 10000 / `handoffPayload` max 5000 are inline literals; extract to named consts kept in sync with the DB CHECK to avoid drift. **Risk:** LOW. **Size:** XS. |
| TD-211 | 🟢 Ready · **P3 / Low** | — | — | **React 19 impurity: `new Date()` inside `useMemo` (stale-clock + purity-lint hazard)** | Auditor-reported (re-verify), matches the documented React-19 gotcha (`.claude/CLAUDE.md` Known Gotchas): `apps/web/components/dashboard/MedCard.tsx:173` (`nowHour` useMemo, empty deps — frozen at mount, misses the midnight boundary) and `:95` (`isPastScheduledTime` calls `new Date()` inside `missedDoses` useMemo). `NowBoard.tsx:185` `referenceTime` is valid (`now ?? new Date()`) but the test-override `now` prop is undocumented at the call site. **Fix:** pass `now: Date` as a prop / `useState(() => Date.now())` per the gotcha; document the `now` override. **Risk:** LOW (correctness at day boundary + CI lint hazard). **Size:** S. |
| TD-212 | 🟢 Ready · **P3 / Low** | — | — | **`burnoutAlert` N+1 dedup query inside per-user fan-out** | Auditor-reported (re-verify), `apps/web/inngest/functions/burnoutAlert.ts:~60`: each at-risk user's `step.run` fires a `care_events` dedup SELECT + push-token fetch → N DB round-trips inside `Promise.all`. **Fix:** batch the dedup check before fan-out (one `.in("org_id", …)` query). **Risk:** LOW (perf only; small N today). **Size:** S. |
| TD-213 | 🟢 Ready · **P3 / Low** | — | — | **Interpretable cleanup batch: misleading names, diverged enums, undocumented WHYs, oversized components** | Auditor-reported readability findings (re-verify; non-behavioral — `/interpretable` invariant-preserving): (a) `apps/web/lib/handoffNarrative.ts` `isJournalEvent` validates any care event, not journal-specific → rename `isCareEventLike` or tighten. (b) `apps/web/lib/deriveOnShift.ts` `LatestMoodInput`/`label` use a `"steady"` mood label that diverges from the canonical `Mood` union in `lib/mood.ts` (`good\|okay\|difficult\|crisis`) → unify or document. (c) `packages/utils/src/index.ts` `getDayStamp()` is UTC while crons may run server-local TZ → rename `getDayStampUTC` or document the contract; `digestMinuteOffset` → `orgDigestJitterMinutes` + JSDoc. (d) `components/VisitSummary.tsx:146` inline `moodMap` with `eslint-disable exhaustive-deps` → hoist to module const, drop suppression; `:123` `WINDOW = 28` → `ADHERENCE_WINDOW_DAYS`. (e) oversized: `MedCard.tsx` (~340 lines, 5 useMemos) + `useJournalActions` (4 concerns) → split (overlaps `/oop`). (f) SQL WHY-comments: `shift_questions_immutable_cols` trigger should list its two mutable columns explicitly; `shifts_handoff_entries` migration should state why caregiver handoff-write bypasses RLS via admin client. **Risk:** LOW (no behavior change). **Size:** M (batch; can split per-file). |
| TD-214 | 🟢 Ready · **P3 / Low** | — | — | **`confirm_ocr_job` concurrent-waiter pgTAP coverage gap (no test for the `FOR UPDATE` race)** | Auditor-reported (re-verify), `supabase/tests/confirm_ocr_job.test.sql` Case 5 note acknowledges the concurrent-waiter path is uncovered but no row tracked it. Complements the shipped TD-174 (which annotated `FOR UPDATE` as load-bearing). **Fix:** add a `pg_blocking_pids`/advisory-lock pgTAP case proving lock-removal regresses. **Risk:** LOW. **Size:** S. |

### New tech-debt (TD-*) — opened 2026-04-14

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-02 | ✅ Shipped · PR #87 | **Dynamic Type + screen-reader audit (mobile)** | scaledFont + accessibilityLabel sweep shipped. Physical device VoiceOver verification deferred to human. |
| TD-03 | 🧑 Needs human | **Sentry source maps upload** | BUILD_STATUS: "source maps pending `SENTRY_AUTH_TOKEN`". Code wired (`apps/web/next.config.ts` Sentry plugin) + runbook documents enable steps (`docs/project-info/runbooks/OBSERVABILITY.md` §1). Only the human action of setting the Vercel env var remains. |
| TD-12 | ✅ Shipped · PR #118 | **Fix missing Dialog + Label UI components** | Created `dialog.tsx` + `label.tsx` wrapping @base-ui/react; unskipped test files. |
| TD-13 | ✅ Shipped · PR #116 | **CommentThread mutation error handling** | Added `onError: () => toast.error(...)` to add/edit/remove mutations; 3 new tests. |
| TD-11 | ✅ Shipped · 2026-04-17 | **data-testid sweep for medication components** | All data-testids already existed in MedicationPanel.tsx + MedicationChecklist.tsx; e2e spec already uses them. No code changes needed. |
| TD-06 | ✅ Shipped · PR #98 | **Add `dark:` variants to ON-44/ON-45 components** | dark: sweep across Comment + TradeRequest components; contrast patch (avatar/badge gray-900+gray-50, fixed hover) committed directly to main. |
| TD-07 | ✅ Shipped · PR #94 | **Alert → Toast sweep** | Replaced 6 `alert()` calls with sonner toasts across JournalClient, settings, subscriptions, TeamAdmin. Invite URL now copies to clipboard before toast. |
| TD-08 | ✅ Shipped · PR #95 | **Supabase types regen + `as any` cleanup** | Regenerated `database.types.ts`; removed 10 `as any` casts from `careEventCommentsRepository.ts`. |
| TD-09 | ✅ Shipped · PR #96 | **ShiftList edit mode** | Added `shifts.update` tRPC mutation + ShiftForm edit-mode props + inline edit panel in ShiftList with `editingShift` state. |
| TD-10 | ✅ Shipped · PR #97 | **JournalClient refactor** | Extracted `useJournalData`, `useOfflineQueue`, `useJournalActions` hooks + `JournalLayout` component. JournalClient.tsx: 624 → 107 lines. |
| TD-14 | ✅ Shipped · PR #132 | **Restore green CI: clear 491 lint errors** | Downgraded `no-explicit-any`, `no-unescaped-entities`, `react-hooks/set-state-in-effect` to warn; hand-fixed 6 remaining errors (`@ts-ignore` → `@ts-expect-error`, targeted a11y disables in `ShiftPopover.tsx`). Contrast script token-drift bug (`#ef4444` vs `#c41a1a`) fixed in same PR. 1010 tests green. |
| TD-15 | ✅ Shipped · PR #131 | **Fix CI infra: lockfile drift + workflow script-name bugs** | (a) `apps/mobile/package.json` had `expo-web-browser` declared without lockfile regen → every CI job failed at `pnpm install --frozen-lockfile`. Fixed by `pnpm install`. (b) `.github/workflows/ci.yml` Typecheck job ran `pnpm typecheck` (typo, root script is `type-check`). (c) Web-tests job ran `pnpm test:coverage` from `apps/web/` (script lives at root). Both fixed by matching the local pre-commit hook pattern (`npx vitest run` from apps/web). |
| TD-16 | ✅ Shipped · PR #132 + #134 | **Clear web typecheck errors + wire CI to catch them** | PR #132 cut `apps/web` tsc errors 147→43 (Next.js 15 Promise params, Supabase types, vitest-globals.d.ts). PR #134 drove remaining 43→0 across 27 files; updated CI workflow to run `cd apps/web && npx tsc --noEmit` instead of silently skipping (root script only covers root). 1111 tests green. |
| TD-17 | ✅ Shipped · PR #141 | **Green mobile Jest suite + CI on PRs** | 7 test files red, 11 tests. Root causes: (1) `scaledFont` missing `Math.round()` → float output; (2) `expo-device` not in pnpm store → virtual mock needed; (3) 5 screen tests had stale empty-state text + schedule trpc mock missing `useUtils`/`shiftTradeRequests`/`completeMutation`; (4) `usePushNotifications` simulator test used dynamic `import()` incompatible with Jest CJS transform; (5) Journal BottomSheet uses Modal+Animated which test-renderer can't pierce — mocked inline. CI `mobile-tests` job already runs on `pull_request:` trigger (confirmed, no yml change needed). |
| TD-20 | ✅ Shipped · PR #140 | **Restore 4 quarantined RLS pgTAP tests** | `ai_conversations_rls`, `education_tip_cache_rls`, `medication_tagging_rls`, `shift_trade_requests_rls` — replaced non-existent `tests.create_supabase_user()` helper with canonical `INSERT INTO auth.users` + `SET LOCAL ROLE` + JWT pattern; fixed invalid (non-hex) UUID literals; corrected `shifts` table column names; `_quarantined-tests/` dir now empty. |
| TD-21 | ✅ Shipped (partial) · PR #148 | **CVE bumps shipped; flip-to-blocking deferred to TD-29** | Bumped Next.js → 16.2.3, protobufjs → 8.0.1 (via root pnpm override), @xmldom/xmldom → 0.9.10 (via root pnpm override). Flipping scanners to blocking surfaced ~25 long-tail transitive findings — reverted to warn-only with TD-29 follow-up. The CRITICAL RCE + DoS bumps still landed. |
| TD-23 | ✅ Shipped · PR #148 | **SHA-pin all workflow action refs + checksum OSV binary** | Pinned 5 actions across both `security.yml` and `ci.yml` to immutable SHAs (with `# v1.2.3` end-of-line comments): `actions/checkout@v4.2.2`, `actions/setup-node@v4.4.0`, `pnpm/action-setup@v3.0.0`, `gitleaks/gitleaks-action@v2.3.9`, `aquasecurity/trivy-action@v0.30.0`. Added `sha256sum -c` against `c52d68f8...` for the OSV binary download — fails loudly on mismatch. |
| TD-29 | ✅ Shipped · PR #165 | **Long-tail transitive vuln triage (followup to TD-21)** | Surfaced when TD-21 flipped scanners to blocking on 2026-04-25; reverted to warn-only after PR #148 merged. Three classes: (1) `apps/web/pnpm-lock.yaml` is a separate lockfile not refreshed by root `pnpm install` — needs `cd apps/web && pnpm install` or unification; (2) `apps/mobile/package-lock.json` is npm-format not pnpm — pnpm overrides don't apply, need `apps/mobile/package.json` direct bumps; (3) root has dompurify / follow-redirects / hono / vite / postcss / uuid HIGH advisories not covered by current overrides. After triage, remove `continue-on-error` from OSV/Trivy/pnpm-audit jobs in `security.yml` (look for `# TD-29` comments). ~0.5 day. |
| TD-22 | ✅ Shipped · PR #147 | **Billing tRPC router (unblocks PP-014)** | `apps/web/server/routers/billing.ts` — `billing.getSubscription` query reads org plan + seat count from `organizations` + `memberships` tables; registered on `appRouter`; 4 tests (happy path, no-membership null, free-plan null, UNAUTHORIZED). No Stripe API call; no new migration. |

### Operational monitoring TDs — opened 2026-04-27

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-73 | ✅ Shipped · PR #227 | **Production rate-limit dashboard** | Vercel + Inngest 429 monitoring with Sentry/Slack alert when 429 rate > 1% in 5-min window. |
| TD-74 | ✅ Shipped · PR #227 | **Weekly digest delivery monitoring** | Inngest `digestDeliveryMonitor` alerts when Sunday send count < 80% of org count. |
| TD-75 | ✅ Shipped · PR #227 | **Weekly E2E green-streak gate** | `scripts/check-e2e-streak.mjs` + `.github/workflows/e2e-streak-gate.yml` block merge queue on >3 consecutive red nightly E2E runs. |

### Wave 5 discovery batch (TD-76..84) — opened 2026-04-27

Surfaced by parallel pre-flight + test-gap audits (`docs/plans/WAVE5_DISCOVERY_REPORT.md`). The Codex adversarial leg of the audit produced no output (TD-84 re-runs it). Total ~10 hr if executed serially; designed to fan out via TDD dispatch in Wave 7+.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-76 | ✅ Shipped · PR #230 | **Regenerate `database.types.ts`** | Drift covered the C1+C2 security migrations; net +285/-3 lines after `npx supabase gen types typescript --local`. Stale types had been masking RLS schema changes at the type-checker level. |
| TD-77 | ✅ Shipped · PR #328 | **Tests for `identityRepository.ts` (Tier 1 — PHI vault)** | Uses `supabaseAdmin` (no RLS protection). Untested cross-org `resolveIdentity(token, org_id)` could leak names/DOB/contact between orgs in a silent regression. New file: `apps/web/server/repositories/__tests__/identityRepository.test.ts`. Test (a) cross-org token rejection, (b) malformed token, (c) expired token. ~2 hr. |
| TD-78 | ✅ Shipped · PR #248 | **Tests for `user.ts` tRPC router (Tier 1 — auth boundary)** | 21 tests in `apps/web/server/routers/__tests__/user.test.ts` covering ctx.user=null → 401, IANA timezone regex, dismissEducationTip date math, updateNotifications. |
| TD-79 | ✅ Shipped · PR #249 | **Tests for `careEventsRepository.ts` (Tier 1 — core PHI write)** | 10 tests in `apps/web/server/repositories/__tests__/careEventsRepository.test.ts` covering validatePayload pre-DB-write, cross-recipient timeline isolation, insertEvent org_id boundary. |
| TD-80 | ✅ Shipped · PR #250 | **Tests for `lib/stripe.ts` (Tier 1 — payment infra)** | 3 tests in `apps/web/lib/__tests__/stripe.test.ts` covering missing-env error, singleton identity, pinned `2026-03-25.dahlia` API version. |
| TD-81 | ✅ Shipped · PR #253 | **Tests for `organizationsRepository.ts` (Tier 2 — team isolation)** | 11 tests in `apps/web/server/repositories/__tests__/organizationsRepository.test.ts` covering cross-org isolation + DB-assigned UUID contract. |
| TD-82 | ✅ Shipped · PR #258 | **RLS test stub for `care_events_client_id` migration** | pgTAP coverage in `supabase/tests/care_events_client_id.test.sql` — 4 scenarios (NULL coexistence, partial unique constraint, global cross-org uniqueness). |
| TD-83 | 🧑 Needs human | **Verify `CI Summary` is in main branch protection** | Pre-flight audit couldn't read protection config (no PAT in shell). Manually verify via GitHub UI: Settings → Branches → main → required checks includes `CI Summary` (per TD-30). If missing, add via API. ~0.25 hr. |

### Post-Wave-9 audit (TD-107..110, UX-107, UX-108) — opened 2026-05-09

Surfaced during the post-Wave-9 cleanup pass: read-only audit of sign-in flow, app-shell SSR auth, marketing/sign-in style divergence, and the lone surviving `TODO` marker.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-107 | ✅ Shipped · 2026-05-09 | **OTP input renders tiny: `text-2xl` + `text-sm` Tailwind class collision** | `apps/web/app/signin/SignInForm.tsx:119` had both `text-2xl` AND `text-sm` on the OTP input — Tailwind last-wins resolved to `text-sm`, so the "large monospace OTP digits" intent rendered small. Looked broken. Fixed in `chore(post-wave-9-cleanup)`: dropped `text-sm`, kept `text-2xl font-mono`, replaced `tracking-widest` with `tracking-[0.4em]` for clearer per-digit spacing. |
| TD-108 | ✅ Shipped · 2026-05-09 | **Multi-org caregiver: `(app)/layout.tsx:23` `.single()` silently nulls `orgId` for users with 2+ memberships** | Replaced `.single()` with `.order("accepted_at", { ascending: true }).limit(1)` so the earliest-accepted membership becomes the primary org. Shipped with Wave 10 (PR #383). |
| TD-109 | ✅ Shipped · 2026-05-09 | **`/signin` doesn't redirect already-authenticated users to `/dashboard`** | Server-side `getUser()` → `redirect("/dashboard")` added to `app/signin/page.tsx` before render, skipping the OTP form for authed visitors. Shipped with Wave 10 (PR #383). |
| TD-110 | ✅ Shipped · PR #384 | **Replace `TODO(UX-24)` placeholder with real pattern aggregation in `PatternsStrip`** | Wired `PatternsStrip` to new `briefs.patterns` tRPC procedure backed by `detectPatterns()` (plural helper added alongside the existing singular `detectPattern`). Drops the 3 hardcoded mocks; renders only when `data.length > 0`. Shipped with Wave 11. |
| UX-107 | ✅ Shipped · 2026-05-09 | **Sign-in / onboarding pages diverge from marketing brand voice** | Migrated `/signin` and `/onboarding` h1s to `.headline-display` with the Fraunces italic-em pattern (`Sign in to <em>CareSync</em>`, `Set up your <em>care team</em>`). Dropped the placeholder colored square from `/signin`. Shipped with Wave 10 (PR #383). |
| UX-108 | ✅ Shipped · 2026-05-09 | **`/signin` is unbranded — no link back to landing for visitors who got there by accident** | New `MarketingNavSlim` (brand-mark only, links back to `/`) mounted on `/signin` and `/onboarding` so users have a way out of the auth flow. Shipped with Wave 10 (PR #383). |
| UX-109 | ✅ Shipped · 2026-05-10 · PR #429 | **Single burnt-orange palette across marketing AND app — retire purple, Sage, and coral** | Owner ask 2026-05-09 (revised 2026-05-10). One brand color: burnt orange shades (primary + tints/shades for surfaces, accents, hover/active). Retire purple `#7c3aed` (marketing), the Sage parlor theme + tokens (PR #351), and tertiary coral `#e76f51`. **Keep semantic tokens unchanged** — `--color-success` (green), `--color-danger` (red), `--color-warning` (amber) stay so error/success states remain readable. Scope: (1) rewrite `--color-primary` + `--color-primary-subtle` + `--color-secondary` + `--color-secondary-subtle` in `apps/web/app/globals.css` to a burnt-orange ramp; (2) drop Sage theme + ThemeSwitcher; (3) audit every raw `#7c3aed` / `#d97706` / `#e76f51` / Sage token reference; (4) regenerate brand assets that bake old hex (favicon, OG images, signin block). Concrete burnt-orange hex ramp TBD (suggest base + 3 tints + 2 shades). Pairs with UX-110 (full dark-mode removal). |
| UX-110 | ✅ Shipped · 2026-05-10 · PR #430 | **Remove dark mode entirely — functionality, components, toggle, and `dark:` variants** | Owner ask 2026-05-09 (revised 2026-05-10). Not a toggle-removal — full retirement. Scope: (1) drop `ThemeSwitcher` component + every mount site (marketing nav, app toolbar); (2) remove `next-themes` provider + dependency; (3) sweep every Tailwind `dark:` variant from `apps/web/app/` and `apps/web/components/`; (4) drop dark-only assets (e.g. SageRail dark sidebar styling from PR #351); (5) confirm `prefers-color-scheme` is not consulted anywhere. Light mode only, forever. Closes UX-111 by construction. |
| UX-111 | 🚫 Won't fix · subsumed by UX-110 | **Dark mode visibly broken on app shell post-login** | Original repro 2026-05-09. Closed 2026-05-10 — UX-110 now retires dark mode entirely, so there is nothing to fix. Kept for audit trail. |
| UX-112 | ✅ Shipped · 2026-05-10 · PR #435 | **Onboarding "Preview your medication import" — side-by-side layout, restyle CTA, add subheader + blurb** | Owner ask 2026-05-10. Today the medication-import preview card stacks vertically with the textarea filling the width, then a lonely "Start your family's log" pill button floating below the card. Asks: (1) split the card into a two-column layout — paste textarea on the left, live preview / explanatory copy on the right; (2) restyle the CTA button to match the new burnt-orange brand (UX-109) with a clearer hierarchy (size, weight, hover affordance); (3) add a short subheader above the button (e.g. "Ready to start logging?") and a one-line blurb explaining what happens on click ("We'll create your care team and pre-populate today's brief from your medication list."). Sequenced AFTER UX-109 so the button color lands once. Touches `apps/web/app/onboarding/` + the medication-import preview component. |
| UX-113 | ✅ Shipped · PR #441 + #443 | **Marketing landing "A shared rhythm" section is missing its illustration at desktop widths** | Discovered 2026-05-10 smoke walk. The "A shared rhythm, not another app to manage" section on `/` has a left column rendered as an empty grey rectangle at 1440px. The caregiver photo IS present on the same section at 375px (mobile) — confirms a viewport-conditional asset that broke at the desktop breakpoint. Likely a `hidden lg:block` sibling pair where the desktop image src is missing/404 OR a CSS background-image whose path no longer resolves. Investigate `apps/web/components/marketing/` + the landing page composition; `grep -rn "shared rhythm" apps/web` to locate. |
| UX-114 | ✅ Shipped · PR #439 | **`/about` has a redundant "Start your family's log" CTA after UX-112** | Discovered 2026-05-10 smoke walk. UX-112 (PR #435) added an in-card CTA inside the "Preview your medication import" card. The pre-existing page-level "Start your family's log" button below the card was not removed, so two identical burnt-orange buttons now stack vertically on `/about`. Decide which to keep — likely the in-card one (closer to the action context) and remove the page-level CTA, OR vice-versa if the page-level one is a sticky bottom bar serving a different role. Touches `apps/web/app/(marketing)/about/page.tsx`. |
| UX-115 | ✅ Shipped · PR #440 | **Dashboard double-FAB ambiguity (Quick log + AI sparkle in same primary orange)** | Discovered 2026-05-10 smoke walk. The dashboard renders two floating action buttons bottom-right: a "+" Quick-log FAB and a sparkle "Open AI Assistant" FAB. Both are filled `--color-primary` burnt orange, identical size, side-by-side. They read as competing primary actions when one is the primary write action and the other is a secondary helper. Differentiate visually: keep Quick log filled-primary, switch the AI assistant to outline / muted / smaller. OR consolidate into a single trigger with a menu. Touches `apps/web/components/QuickLogFab.tsx` (and the AI assistant FAB component). |

### Onboarding bugs (ON-75)

| ID | Status | Story | Notes |
|---|---|---|---|
| ON-75 | 🟡 Pending verification (local) | **Onboarding wizard — care team setup failure was misconfigured local env, not an RLS policy gap** | Root cause amended 2026-05-10 PM. Initial repro showed `Error: Org creation failed: new row violates row-level security policy for table "organizations"`. Diagnosis traced it to `apps/web/.env.local` containing the **production** `SUPABASE_SERVICE_ROLE_KEY` in `sb_secret_*` format, which the local Supabase JWT-only auth rejected — `supabaseAdmin` silently fell back to anon role, then RLS correctly blocked the insert. The route + repository code are correct; the server-side `supabaseAdmin` client bypasses RLS when the key is valid. Resolution: swap local `.env.local` to the JWT from `supabase status` (owner action, hook-protected). **Verify in production** — if production deploy actually has a valid service role key (likely yes), prod is unaffected and this row closes as "config issue". If prod also fails, reopen as a real RLS gap. Defense-in-depth follow-up: surface a clearer error than a generic RLS message when `supabaseAdmin` is misconfigured (file as TD if it recurs). |

### OWASP audit follow-ups (SEC-002..006, TD-131..132) — opened 2026-05-14

Source: `/owasp` full-project read-only audit. Findings at `docs/security/2026-05-14-owasp-audit.md`. 1 High + 3 Medium + 2 Low (1 Info dropped — no action). All file:line cited and grep-verified. Mostly defense-in-depth gaps on an otherwise strong posture (34/34 tables RLS-enabled, no XSS/code-exec sinks, PHI invariant double-enforced).

| ID | Status | Story | Notes |
|---|---|---|---|
| SEC-007 | ✅ Shipped · PR #531 | OCR confirm route audit-log site (FIND-003 part 3) | Append-only `ocr_audit_log` table — PHI-free by construction (SHA-256 of `raw_text` NFC-normalized + `confirmed_field_keys` allowlist text[], never values). Defense-in-depth append-only via BOTH triggers AND `REVOKE UPDATE,DELETE` (covers `session_replication_role='replica'` bypass per cycle-1 Opus review). Service-role-only RLS policy. Audit insert lands BEFORE medication insert; failure returns 500 (fail-loud). Per-attempt semantics — records 409-losers too. 10 pgTAP cases (RLS, append-only, CHECK constraints, FK RESTRICT). Backfill placeholder rows for existing `status='confirmed'` jobs with empty-string hash marker + `backfilled=true`. /opus-on-opus cleared cycle 2 (0/0/1). Follow-ups seeded: TD-144 (atomic confirm RPC), TD-145 (DDL lint CONCURRENTLY annotation). |
| TD-133 | 🟢 Ready · **P3 / Low** | **`ai_conversations` archival Inngest job (90-day cutoff)** | Closes the archival half of audit FIND-007; the row-cap half shipped as TD-132 #491 (DB-level CHECK constraint). Periodic Inngest job under `apps/web/inngest/functions/aiConversationsArchive.ts` that deletes rows older than 90 days. **Materialize when the first write site lands** — currently `ai_conversations` is unused (only `revokeConsent` DELETE at `apps/web/server/routers/ai.ts:246`); nothing to archive yet. Story can sit blocked until an AI feature adds an INSERT. **Risk:** LOW. **Size:** S (1–2h). |
| SEC-008 | 🟢 Ready · **P3 / Low** | **Rotate existing 128/192-bit share tokens to 256-bit** | SEC-006 #492 changed `outer_circle_requests.share_token` and `care_briefs.share_token` DEFAULTs to 256-bit but did NOT rotate existing in-flight tokens. Existing tokens still exceed the 64-bit guessability floor (128-bit minimum is OWASP-compliant) so this is consistency cleanup, not vuln remediation. Implementation: new migration that regenerates each existing `share_token` with `gen_random_bytes(32)`, PLUS a notification email to outer-circle requesters / care-brief owners that their old link is invalidated. **Risk:** MEDIUM (link invalidation is user-visible — requires the email step). **Size:** M (2–3h). |
| SEC-009 | 🟢 Ready · **P2 / Medium** · sequenced after SEC-001 | **Populate Vercel's Development environment tier with local-tier values** | Mitigates the contributing-factor that caused SEC-001 (default `vercel env pull` pulls Production). Operator walkthrough now lives in `docs/project-info/runbooks/SECRETS_ROTATION-runbook.md` §9 ("Populate Vercel's Development environment tier") — landed via PR #498 in the pre-staging-mitigations sprint. After rotation completes, walk that §9 step-by-step (Vercel dashboard → Project → Settings → Environment Variables; for each of the 6 rotated secrets + the 3 Supabase URL/key vars, click "Add for Development" with a local-tier value); then verify via `vercel env pull --environment=development .env.local`. Going forward, the `vepull` alias from `SECRETS_ROTATION.md` §3 grabs SAFE values. Closes the leak-vector permanently. **Risk:** LOW (additive — does not change Production or Preview tiers). **Size:** S (~30 min UI work, no code). Sequenced AFTER SEC-001 actually rotates so you're not splitting attention. |
| TD-137 | ✅ Shipped · PR #520 | JournalCard author lookup: useMemo'd Map<user_id, display_name> hoisted to JournalTimeline | Pure refactor; O(N×M) per-render .find() → O(M) memo + O(1) gets. Path (a) chosen; path (b) (pre-resolve into useJournalData state) deferred. |
| TD-138 | ✅ Shipped · PR #520 | JournalEvent type consolidated into apps/web/types/journal.ts; 5 consumers import canonical | Pure refactor. EntryDetailClient's narrower view (missing actor_id) widens to canonical — Opus pre-merge caught this as 1 byte-different site. |
| TD-139 | ✅ Shipped · PR #513 | PDF export: payload.note/notes fallback when text empty | Shipped via `pickJournalBody` helper in `apps/web/lib/`; 7-case unit test locks precedence text > note > notes. |
| TD-140 | ✅ Shipped · PR #514 | memberships test: filter-contract assertions on getCareTeamForRecipient | Test-only; locks select/eq/or shape on prod query. .not() deferred. |
| TD-141 | ✅ Shipped · 2026-05-20 · PR #661 (read-policy unified via pickJournalBody; optional schema-collapse to single `body` column deferred — /oop divergence finding resolved) | **JournalPayload: consolidate `text`/`note`/`notes` parallel free-text slots into one `body` field** | Surfaced 2026-05-15 by `/oop --from-sprint` review of TD-139 (PR #513). `JournalPayload` at `apps/web/lib/careEvent.ts:49-54` has three parallel optional string fields. In-repo there are now 3 different policies for picking the "primary body": `handoffSummary.ts:148` reads `text` only; `VisitSummary.tsx:395-397` uses `note ?? notes`; `pickJournalBody.ts` uses `text > note > notes`. Long-term: schema migration to a single `body` column on `journal_events`, then call-site sweep + delete the three slots. **Risk:** MEDIUM (migration touches `journal_events` table, needs backfill of `text`→`body` then `note`/`notes`→`body` with precedence). **Size:** M (~half-day; needs separate plan + ADR for the precedence choice). |
| TD-142 | ✅ Shipped · 2026-05-17 · PR #567 | **Collapse parallel invite-acceptance state machines (TS `acceptInvite` vs SQL `accept_invite`)** | Surfaced 2026-05-15 by `/oop --from-sprint` review of the careteam-rls-defense sprint. Both `acceptInvite` (`apps/web/server/repositories/membershipsRepository.ts:163-200`) and the `accept_invite(text, uuid, text)` SQL function (introduced in migration `20260407_atomic_invite_accept.sql`, locked-down by `20260516000000_revoke_accept_invite_from_anon_authenticated.sql`) encode the same state transition: validate token → check expiry → check consumed → check email match → mark consumed + activate membership. The TS version has a known atomicity hole — `Promise.all` of two updates is **not** a real DB transaction (called out in its own comment at `:182-185`: "A Postgres RPC function would be the correct fix"). The SQL version is exactly that, currently dead code defended only by the REVOKE migration. **Decision needed:** (a) flip `acceptInvite` to `supabaseAdmin.rpc('accept_invite', ...)` and delete the TS body — gains atomicity; OR (b) drop the SQL function + migration + pgTAP cases 7–9 entirely — loses a defense layer. Every future invite-rule change (single-use enforcement, audit log, rate limit) currently has to land in two places that disagree on atomicity guarantees. **Risk:** MEDIUM (path-dependent). **Size:** S–M. |
| TD-144 | ✅ Shipped · 2026-05-17 · PR #589 | — | — | **Atomic OCR confirm via SECURITY DEFINER RPC** | Resolution: new `confirm_ocr_job(...)` SECURITY DEFINER function takes `FOR UPDATE` on `ocr_jobs` row and collapses audit insert + medication insert + status flip into one transaction. Sentinel return struct (`success, error`) replaces optimistic-lock count check. Route handler rewrites to thin RPC caller with sentinel→HTTP map. 18 pgTAP assertions cover function metadata + privilege grants + happy path + race-loser contract + 4 sentinel paths + empty-array COALESCE. Also fixed latent bug: prior route passed `created_by` to `medications` but column doesn't exist (PostgREST silently dropped). /oop surfaced TD-174/175/176 (race-test annotation, scan_source backfill, Sentry tag convention). |
| TD-145 | ✅ Shipped · PR #533 | migration-lint: whitelist CREATE INDEX on same-file new tables | `scripts/migration-lint.sh` now pre-scans normalized statements for CREATE TABLE [IF NOT EXISTS] <name>, then skips the CONCURRENTLY check for CREATE INDEX whose ON-target is in that set. Handles schema-qualified + quoted identifiers via shared regex normalization (lowercased, schema prefix stripped, quotes stripped) compared via `grep -Fxq`. Bash discipline: `|| true` after grep -E to survive `set -e`; POSIX ERE only (no -P, no \b). 9-case dev-gate test harness at `scripts/__tests__/migration-lint.test.sh` (NOT wired into CI; CI keeps invoking the production script via `.github/workflows/migration-lint.yml:28`). Closes the SEC-007 post-merge advisory + prevents future "new table + index in one migration" false positives. /opus-on-opus cleared cycle 2 (2MF/3SF/2N → 0/1/2). |
| TD-146 | ✅ Shipped · 2026-05-17 · PR #559 | **Audit prod Inngest cron-firing state — verify the 14 functions actually run** | **Partial finding (2026-05-16, during SEC-001 rotation):** hypothesis #2 confirmed — crons DO fire and show under **Functions → Runs** (weekly-digest had 3 runs in last 24h); the "0 events in 24h" reading on the Events tab was metric semantics (Events tab tracks `inngest.send()` calls, not cron triggers). Inngest app reachable post-rotation (Last seen ticks; manual Invoke succeeds). **Remaining work:** still need per-function firing audit for all 14 functions to confirm none are env-gated dark — especially `rateLimit429Monitor` (every 5 min) and `shiftTradeExpiry` (every 15 min) which dominate the projected ~387 firings/day. Original three hypotheses for posterity: (1) **Prod Inngest app not registered or unreachable** — verify via Inngest dashboard → Apps → carelog → "Last seen" timestamp; if stale, the SDK isn't reaching Inngest cloud. Fix: confirm `INNGEST_SIGNING_KEY` + `INNGEST_EVENT_KEY` are set on prod Vercel env (these were also part of SEC-001 rotation scope) AND that the `/api/inngest` endpoint is publicly reachable from Inngest cloud. (2) **Dashboard 1-day window is reading a narrower metric than total runs** — eyeball whether "Functions" tab shows per-function run counts differently from the top-line "Events". (3) **High-frequency crons silently disabled** — `rateLimit429Monitor` (every 5 min) and `shiftTradeExpiry` (every 15 min) dominate the projected count; check `apps/web/inngest/functions/` for any env-gated short-circuits. **Deliverable:** capture the actual prod cron firing pattern (which functions, how often, last successful run) in a follow-up note appended to the research doc §6.6, OR open targeted bug-fix rows if any cron is dark. **Blocks:** any decision on Inngest-vs-Queues migration (TD-133, ON-70, ON-71) — the cost half of the §6 recommendation is structurally unverifiable until prod firing state is known. **Risk:** LOW (read-only investigation). **Size:** S (~30 min). |

### SEO discoverability (SEO-001..007) — opened 2026-05-09

Roadmap §"SEO discoverability (post-launch)" added 2026-05-09. LAUNCH-003 shipped table-stakes meta + sitemap + JSON-LD; this slate goes deeper for organic discoverability. Tracked as a separate prefix to keep them visible during the post-launch push.

| ID | Status | Story | Notes |
|---|---|---|---|
| SEO-001 | ✅ Shipped · PR #386 | **Per-page `<title>` + meta description rewrite for primary intent keywords** | Every marketing route's metadata rewritten for intent-shaped titles (≤60 char) + descriptions (≤160 char). Shipped with Wave 12 Session A. |
| SEO-002 | ✅ Shipped · PR #386 | **Add `FAQPage` JSON-LD on `/`, `/pricing`, `/about`** | FAQPage JSON-LD added on `/contact` (existing 3-Q&A FAQ) and `/pricing` (new 3-Q&A "Pricing FAQ" section). `/about` skipped — comparison-table-shaped, not FAQ-shaped (would have been schema spam). Shipped with Wave 12 Session A. |
| SEO-003 | 🧊 Deferred | **Add `HowTo` JSON-LD on `/about` and the CareZone-alternative page** | Deferred 2026-05-09: `/carezone-alternative` was consolidated into `/about` via PRs #316/#317; `/about` lacks step-by-step content the schema would describe. Adding the schema without matching visible content = schema spam (Google penalizes). Re-visit when the human writes step-by-step caregiving content. |
| SEO-004 | ✅ Shipped · PR #386 | **`<h1>` hierarchy + internal linking audit** | h1 hierarchy was already clean (every marketing route has exactly one `<h1>`). Internal-linking gap closed: `MarketingFooter` previously linked only Privacy/Terms/Referrers/Contact — added Pricing, About, Trust. Shipped with Wave 12 Session A. |
| SEO-005 | ✅ Shipped · 2026-05-17 · PR #588 | **Core Web Vitals tightening on `/`, `/pricing`, `/about`** | Resolution: scope-honest close. One concrete CWV fix shipped — Geist (primary body font) was missing `display: "swap"` at `apps/web/app/layout.tsx:7` causing FOIT on slow networks; aligned with sibling fonts. Marketing pages have zero `next/image` usage today so "defer below-fold images" + "image priority hints" items are N/A until UX-109 introduces images. Baseline doc at `docs/runbooks/lighthouse-cwv-baseline-2026-05-17.md` locks pre-redesign LCP (61–86ms) for regression detection. Reopen after UX-109. |
| SEO-006 | 🧑 Needs human (re-scoped 2026-05-09) | **Cornerstone content engine — 3–5 articles at `/learn/*`** | Build a thin MDX-based blog at `/learn/*` (Next.js App Router, static-only, no CMS — minimal infra). Three to five long-form articles on caregiver pain points: "Managing medications across a care team", "Sharing shift handoff notes that actually help", "When a parent's care needs grow beyond what one person can do", etc. Each article is internally linked, has its own JSON-LD `Article` markup, and lands in the sitemap. ~8 hr — **6 of those 8 are long-form copy writing where single creative voice matters; flagged 🧑 Needs human.** Infra (~2 hr) can be ready when the human carves out the writing time. |
| SEO-007 | 🧑 Needs human | **Verify in Google Search Console + Bing Webmaster Tools, submit sitemap** | Both consoles require ownership verification (DNS TXT or meta tag — meta tag works since marketing routes are SSG). Ownership tag goes in `(marketing)/layout.tsx`. Then submit `https://care-log.org/sitemap.xml`. Track index coverage weekly for the first month. Mostly setup, but gates SEO-001..006's measurable impact. ~30 min once human is at the keyboard. |

### Lighthouse a11y CI gating gap (TD-87) — opened 2026-04-29

Discovered during `/impeccable critique` post-merge verification of PR #269. The CI Lighthouse a11y workflow ran on the merged main but reported "success" without auditing anything — Vercel preview deployments are auth-gated (HTTP 401), and `scripts/lighthouse-a11y.mjs:27-33` treats 401/403 as a non-blocking skip. Net effect: the a11y score gate has been silently inactive on every preview-driven CI run. Local Lighthouse on the actual marketing routes still passed (96 / 100 / 94 on /, /about, /pricing) so no regression slipped through this time, but the gate is not protecting us.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-87 | ✅ Shipped · 2026-05-18 · PRs #602 / #603 | **Restore Lighthouse a11y gating in CI — path b chosen** | Resolution: 2-PR ship. Path-pick decision (b) recorded in ADR-0005. PR #602 swapped trigger from `deployment_status` → `pull_request` with broad `paths:` filter, build + `pnpm start` + `npx wait-on` (mirrors ci.yml E2E block), audit `/`, `/pricing`, `/about` via `node scripts/lighthouse-a11y.mjs`. Script: N-URL aggregation, removed 401/403 silent-skip, pinned `@lhci/cli@0.15.1`. PR #602 first run surfaced a pre-existing script bug (`@lhci/cli collect --outputPath` is a no-op; lhci writes to `.lighthouseci/lhr-*.json` in cwd) — masked for months by the silent-skip path. PR #603 hot-fix: per-URL tempdir + readdir `.lighthouseci/` + `--numberOfRuns=1`. Verified end-to-end: `/`=100, `/pricing`=96, `/about`=96. NOT a required check in v1 per ADR-0005; promotion needs `merge_group` trigger first. /oop residual: TD-184. |
| TD-170b | ✅ Shipped · 2026-05-18 · PR #605 (investigation) | — | — | **Root-cause E2E chronic-red on main** | Resolution: investigation report at `docs/notes/2026-05-18-td-170b-e2e-investigation.md` — root cause was UX-04x copy-audit trio (PRs #525/#528/#529, all 2026-05-15) renaming UI strings without sweeping E2E. PR #592 (TD-170) only fixed one #528 string. Investigation took ~30 min wall (vs 75-min plan estimate) — H5 (selector drift) HIGH confidence + H8 (multi-selector drift) HIGH confidence; 6 other hypotheses RULED OUT with cited mechanism. |
| TD-170c | ✅ Shipped · 2026-05-18 · PRs #606 / #607 / #608 | — | — | **E2E sweep across UX-04x copy-audit trio (PR #525/#528/#529 string drifts)** | Resolution: 3-PR sed sweep. #606 swept PR #529 (UX-050): "Share update"→"Post to journal" + "Share how today went..." placeholder — 54 callsites across 11 specs. #607 swept PR #528 (UX-049): "Thanks! You're helping out"→"Thanks for stepping in" — 1 callsite. #608 swept PR #525 (UX-048): "No billing history yet"→"No charges yet…" + "No coverage windows defined yet"→"No coverage windows set yet" — 2 callsites. Total: **57 E2E callsites swept across 13 spec files**. Validates investigation H8 ("multi-selector drift from one UI PR") — each of the 3 UX-04x PRs drifted ≥1 string. End state: shards [1/3] + [3/3] solidly green; shard [2/3] now fails on a different non-string regression (see TD-170e). |
| TD-170d | ✅ Shipped · 2026-05-18 · PR #611 | — | — | **E2E string-drift lint rule (prevention for UX-04x class of regression)** | Resolution: `scripts/check-e2e-string-drift.mjs` (193 lines) + 14 unit tests + `e2e-string-drift` CI job gated by `changes.outputs.web == 'true'`. Cross-file move-aware single pass — global ADDED-strings set scoped to changed UI files, per-file REMOVED, filter movers, filter `// e2e:no-references "X"` allowlist, then Node-side fs scan of `e2e/`. Retro-validation against PRs #525/#528/#529 confirmed all 3 historical drifts caught (exit 1). Non-required check in v1 — informational only. /oop residual: TD-185 (shell injection + scope hardening). |
| TD-170e | ✅ Shipped · 2026-05-18 · PR #610 | — | — | **`e2e/medication-tagging.spec.ts:44` strict-mode violation — two `Add medication` buttons** | Resolution: 1-line testid swap. Switched `e2e/medication-tagging.spec.ts:27` from `getByRole("button", { name: /add medication/i })` to `getByTestId("add-medication-btn")`. Line 37 unchanged (exact-match `/^add medication$/i` for the form submit button, correctly distinguishes). Pre-existing latent bug; dual-button UI predated the 2026-05-15 redesign. |
| TD-184 | ✅ Partial · 2026-05-19 · PR #634 (items 1+2) | — | — | **`scripts/lighthouse-a11y.mjs` cleanup — /oop should-fix/nit residual from TD-87** | Resolution: items 1+2 shipped — extracted `rawArgs` constant + split `auditOne` into `probeUrl` + `runLhci` (each owns one failure mode; `{ ok, reason }` discriminated returns). **Item 3 deferred** — JWT-mint composite action between `.github/workflows/lighthouse-a11y.yml` and `ci.yml` touches CI workflows; separate row when picked up. Open as TD-XXX when needed. |
| TD-185 | ✅ Shipped · 2026-05-18 · PR #613 | — | — | **Harden TD-170d drift script — shell injection + scope + allowlist** | Surfaced 2026-05-18 by `/oop --from-sprint` on TD-170d (PR #611). **Must-fix:** `git()` and `grepE2eForString()` interpolated filenames + removed strings into shell command strings (`execSync(\`git ${args}\`)`) — a renamed file with `"`, `$()`, `;`, or backtick in the path becomes command injection. **Should-fix:** (a) `buildAddedStringSet` scoped whole-repo — lockfile/types addition could mask a real removal; (b) allowlist accepted opt-outs in any file (.md/.yml stealth); (c) silent no-match on missing `e2e/` via `grep || true`. **Resolution:** switched `git()` to `execFileSync("git", argv)`; replaced shell grep with in-process fs+regex scan; scoped ADDED-set to UI files only; restricted allowlist to code files; early-exit if `e2e/` missing. 14 unit tests + 2259 vitest tests green. Retro-validation re-confirmed against PRs #525/#528/#529. |

### Extract candidates from /impeccable extract Phase A (TD-88..93) — opened 2026-04-29

| ID | Status | Title | Notes |
|---|---|---|---|
| TD-88 | ✅ Shipped · PR #300 | **Extract `lib/format.ts` — date/time formatter helpers** | `/impeccable extract` Phase A: 8 copy-pasted `formatDate`/`formatTime` functions across `MedCard.tsx:14`, `VisitSummary.tsx:64`, `care/[shareToken]/page.tsx:33`, `api/export/ExportDocument.tsx:93`, `JournalTimeline.tsx:53,308`, `EntryDetailClient.tsx:30`, `BriefEditorial.tsx:37`, plus 12+ inline `toLocale*` calls. Lift to `apps/web/lib/format.ts` exporting `formatLongDate`, `formatShortDate`, `formatTimeOfDay`, `formatDateTime`. Keep `MedCard.formatTime(HH:MM:SS)` separate (different intent — clock string, not ISO) — expose as `formatClockTime(hms)`. ~20 consumer migrations. S+M effort. See `apps/web/extract-candidates.md` candidate #2. |
| TD-89 | ✅ Shipped · PR #298 | **Extract `<FormActionRow>` — Save/Cancel form footer** | `/impeccable extract` Phase A. Lifted to `apps/web/components/ui/FormActionRow.tsx`. Subagent migrated only 2 of 12 audit-listed sites (MedicationPanel, ShiftForm) — the other 10 had subtly non-canonical footers (full-width single Submit, raw `<button>` Cancel, outline-variant Cancel, action-grid not form-footer) and were left untouched to preserve byte-identical DOM. Remaining sites can adopt FormActionRow opportunistically as their forms get touched. |

### Hardening audit (TD-94..104, A11Y-019, UX-053) — opened 2026-04-29

Source: `/impeccable harden` audit against the whole web app on 2026-04-29 (`/tmp/harden-audit-2026-04-29.md`). 184 source files / 13 `(app)` routes scanned. Findings ranked High/Med/Low. The four High-severity items (TD-94, TD-96, TD-97, TD-99) are file-disjoint and TDD-friendly — natural fit for a 4-track `/dispatch` wave. Items already in good shape (reduced motion, mood color+text pairing, focus rings, logical CSS direction, offline queue architecture) intentionally **not** filed.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-94 | ✅ Shipped · PR #304 | **`AbortController` for in-effect fetches + tRPC** | High severity. Zero `AbortController` usages across the entire web app. Every `useEffect`-triggered `fetch` and every tRPC query that fires during navigation continues to completion after unmount; race conditions on fast tab-switching (e.g. between recipients on `/journal/[recipientId]`) can stomp later state with earlier responses. Confirmed site: `apps/web/app/(app)/journal/[recipientId]/JournalTimeline.tsx` raw `fetch(url)` in effect with no signal. Fix: add a `useAbortable` hook in `apps/web/hooks/`, plumb `signal` through all in-effect fetches and tRPC queries that own a long-lived subscription. ~3 hr. |
| TD-95 | ✅ Shipped · PR #305 | **Explicit `Intl.*Format` instances in `lib/format.ts`** | High severity. TD-88 just shipped 11 date/time helpers but they still rely on the runtime locale with `.toLocaleDateString` / `.toLocaleTimeString` and no explicit options. Net `Intl.*Format` usage in app code (excl. comments) = zero. Locale-stable formatting is impossible to test, dates render inconsistently SSR vs. client, and there's no path to localized number/currency. Migrate the helpers to construct cached `Intl.DateTimeFormat` / `Intl.NumberFormat` / `Intl.RelativeTimeFormat` instances with explicit options. Pairs with TD-104. ~2 hr. |
| TD-96 | ✅ Shipped · PR #306 | **Surface every mutation failure in journal panels** | High severity. 6 silent / lossy `catch {}` blocks in journal flows. `JournalTimeline.tsx:106` swallows network errors after an optimistic update with only "Rollback on network error" — no toast, no surface. `OuterCirclePanel.tsx:97` is fully silent. `OcrReviewPanel.tsx:85`, `ExportButton.tsx:61`, `OuterCirclePanel.tsx:87` set local error state but never surface it via toast. User gets no signal that their action failed (especially bad when the optimistic update made it look like the action succeeded). Standardize: every `catch` in a mutation path emits a sonner toast with retry affordance + rolls back optimistic state. ~3 hr. |
| TD-97 | ✅ Shipped · PR #307 | **`disabled={mutation.isPending}` on every form submit** | High severity. ~11 disabled-while-pending guards across all submit handlers. Most submit buttons are not disabled while the mutation is in flight — mashing Submit on a slow connection enqueues N duplicate inserts. Highest risk on the offline-queue path, which already replays writes. Audit every `<form onSubmit>` and every primary action button calling a `useMutation`; add `disabled={mutation.isPending}` + idempotency assertion where the server can't dedupe. Test plan: a single component test per form that simulates 5 rapid clicks and asserts the mutation fired exactly once. ~4 hr. |
| TD-98 | ✅ Shipped · PR #334 | **Text overflow / truncation pass on cards** | Med severity. `apps/web/components/dashboard/MedCard.tsx:192-200`: `<span className="flex-1 …">{med.name} · {med.dose}</span>` has no `min-w-0` / `truncate`. A 60-char drug name (e.g. *"Methylphenidate hydrochloride extended-release 36 mg"*) overflows the card or pushes the Log button off-screen on 320px. The `aria-label` on the Log button also inflates. Same risk: RecipientHeader, ShiftEventCard, JournalTimeline entry titles. Apply `min-w-0 truncate` (or 2-line clamp) and add a 60+ char fixture to the relevant component test. ~2 hr. |
| TD-99 | ✅ Shipped · PR #302 | **Per-route `error.tsx` boundaries** | High severity. Only `/dashboard` and `/journal/[recipientId]` have `error.tsx` boundaries. Settings, Messages, Education, Subscriptions, Billing, Visit Summary, History Export, Team Admin all crash to the root error boundary on any render error — losing in-flight form state. Add per-route `error.tsx` with retry + "go back" affordance to all 8 missing routes. ~2 hr. |
| TD-100 | ✅ Shipped · PR #336 | **Journal timeline cursor pagination + virtualization** | Med severity. `JournalTimeline.tsx` renders all DOM nodes for the recipient — a recipient with 2000 journal entries (a year of daily logs + meds) creates a heavy DOM tree. No `react-window` / `@tanstack/virtual` / cursor on the read path. Add cursor pagination + `IntersectionObserver` "load more" with a 200-entry threshold for client-side render. ~4 hr. |
| TD-101 | ✅ Shipped · PR #333 | **RTL smoke test** | Med severity. 107 tailwind logical-direction utility usages (`ms-/me-/ps-/pe-`) — solid baseline. But: zero `dir="rtl"` test fixture, custom CSS in `globals.css` uses physical properties in spots, app-shell rail is left-anchored without an inline-start variant. Not currently a shipped concern, but if CareSync expands to Spanish-speaking caregivers (a real persona) the editorial Fraunces blocks need an RTL audit. Add an RTL smoke test that loads `/dashboard` with `dir="rtl"` and screenshots the result. ~2 hr. |
| TD-102 | ✅ Shipped · PR #329 | **`mutations.retry: 0` in TrpcProvider** | Med severity. `apps/web/components/providers/TrpcProvider.tsx:16` sets `retry: 1` globally. For idempotent reads this is fine; for the implicit retries on `useMutation` it's silent and may double-write if the request reached the server but the response was lost. Set `mutations.retry: 0` explicitly; add explicit retry buttons on the surfaces that need them (handled by TD-96). ~0.5 hr. |
| TD-103 | ✅ Shipped · PR #331 | **Debounce journal + messages search inputs** | Low severity. No debounce on the journal search/filter inputs (sampled `JournalTimeline` filter row). `useMemo` filtering 200+ entries per keystroke is OK locally but stutters on cheap Android. Wrap journal filter in 200ms debounce; same pass for `/messages` search. Pairs naturally with TD-100. ~1 hr. |
| TD-104 | ✅ Shipped · PR #330 | **`pluralize(count, singular, plural)` helper** | Low severity. Currently zero `count !== 1 ? 's' : ''` ternaries (good — already cleaner than typical) but no helper exists either, so the pattern will reappear. Add `pluralize()` helper using `Intl.PluralRules` (or a thin string fallback); migrate 5–10 sites where "1 entry" / "2 entries" is currently hardcoded. Pairs with TD-95. ~1 hr. |
| A11Y-019 | ✅ Shipped · PR #332 | **SR-only live region for offline-queue + optimistic-update transitions** | Med severity. 23 `aria-live` / `role=status` usages across the app — sounds like coverage but most are decorative. The optimistic-update path in `JournalTimeline.tsx` and the offline-queue replay flow have **no live region** to announce "logged" / "queued offline" / "synced" / "rollback" to a screen reader. Mood color+text pairing rule is already honored everywhere sampled. Add a single SR-only `aria-live="polite"` region that subscribes to offline-queue events and optimistic-update rollbacks. ~1.5 hr. |
| A11Y-020 | ✅ Shipped · PR #438 | **`/signin` "Continue with email" button text fails WCAG AA contrast** | Discovered 2026-05-10 smoke walk. The primary CTA on `/signin` renders white text on `--color-primary-light` (`#E48A4A`) → ~2.5:1 contrast, well below the 4.5:1 AA floor for body text. Other primary CTAs across the app use `--color-primary` (`#D2691E`, ~3.45:1 — still borderline) or `--color-primary-pressed` (`#9C4A14`, 5.67:1 ✓ AA). Fix: change the SignInForm submit button background to `--color-primary-pressed` OR use the `--color-primary` deeper variant with darker text. Audit the rest of the app for the same anti-pattern (any button using `--color-primary-light` as its base background with white text). Touches `apps/web/app/signin/SignInForm.tsx` + sweep. |
| A11Y-021 | ✅ Shipped · PR #446 | **Sweep 30+ white-on-`--color-primary` CTAs across app + marketing for AA contrast** | Surfaced during A11Y-020 implementation 2026-05-10. PR #438 fixed signin only; the sweep grep (`bg-[var(--color-primary)]` paired with white/cream foreground) returned 30+ sites. Categories: marketing CTAs (HeroSection, MarketingNav, PricingCards, ContactForm, ForReferrersPage, HowItWorks), in-app buttons (ShiftsPanel, settings, history-export, dashboard, care-share, HandoffSummary), badges/chips (ThreadList, AppTabBar, TimeRailTimeline, MedCard, MedicationChipBar, TagFilter, SageRail), and AI surfaces (AIPanel, AIChatThread). White-on-`#D2691E` ≈ 3.80:1 — fails 4.5:1 AA for body text but may pass large-text 3:1 floor for badges. Per-site decision: migrate body-text-sized CTAs to `--color-primary-pressed` (`#9C4A14`, ~5.04:1 ✓); keep large-text badges where they pass 3:1. ~3 hr. |
| UX-053 | ✅ Shipped · PR #537 | EmptyState primary-action affordances + passive-site documentation | Audit of 6 EmptyState consumers; none used the component's actionLabel/onAction props. Hoisted CTAs into MedicationPanel ("Add medication", coordinator-gated → setShowForm) and TeamPanel ("Invite a member", canInvite + !showInvite gated → onToggleInvite — `!showInvite` guard prevents desktop dead-click on always-visible Invite form, opus cycle-1 catch). 4 intentionally-passive sites (DocumentVault, JournalTimeline, MedicationLinkedDocs, MedicationRecentEvents) annotated with `// UX-053: passive — <reason>` comments so future audits skip. /opus-on-opus cleared cycle 1 (0/2/1). Existing TeamPanel + MedicationPanel + EmptyState tests stay green (33/33). |

### Test gap stories (TD-24..28) — opened 2026-04-25 from coverage analysis

Snapshot at filing time: web 66.74% / mobile 78.53% / RLS 211 tests across 26 files. These five close the highest-leverage PHI/auth/payment gaps. ~12 hr total. **Target after this batch ships:** web ≥78%, mobile ≥85%, RLS adds 2 dedicated PHI-table files.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-24 | ✅ Shipped · PR #146 | **`care_events_rls.test.sql`** | `care_events` is the most-frequently-written PHI table (every journal entry) and has NO dedicated RLS test file today (only `care_event_comments` does). A cross-recipient SELECT/INSERT leak would be silent in CI. Test coordinator/aide/outer-circle SELECT and INSERT isolation — especially the cross-recipient leak vector. ~2 hr. |
| TD-25 | ✅ Shipped · PR #158 | **`supabaseServer` session-refresh unit test** | New file: `apps/web/lib/__tests__/supabaseServer.test.ts`. Cookie-API regressions from Next.js or `@supabase/ssr` upgrades currently have no regression net (route tests mock the client). Simulate expired `access_token` + valid `refresh_token` in cookies, verify a new session is returned (or 401 thrown cleanly). Silent-break vector for ALL SSR routes. ~3 hr. |
| TD-26 | ✅ Shipped · PR #159 | **`useOfflineWrite` error/retry branch coverage** | Branch coverage is 50% (lines 79-80, 87, 95-96 — the offline retry and error-clear paths). Exactly the code that runs during intermittent connectivity, the most common mobile failure mode. Test: network failure mid-sync (queue not removed), repeated retry on permanent 4xx, queue clear on success. ~2 hr. |
| TD-27 | ✅ Shipped · PR #153 | **Aide cross-recipient scoping integration test** | New file: `apps/web/server/routers/__tests__/careEventsRouter.scope.test.ts`. RLS covers DB-layer isolation but the tRPC `where` clause is untested for cross-org isolation. Use a real local DB; assert `careEvents.list` called with a `recipient_id` the aide is NOT a member of returns empty (or 403), not data. ~3 hr. |
| TD-28 | ✅ Shipped · PR #160 | **`messagingPush` + `educationTipRefresh` Inngest failure tests** | These are the only 2 Inngest functions with zero test coverage (out of 11). `messagingPush` fans out push notifications to potentially all family members — an unhandled `DeviceNotRegistered` error would silently drop the job. Test malformed payload + `DeviceNotRegistered` + Expo API timeout. ~2 hr. |
| TD-30 | ✅ Shipped · PR #149 | **Path-filtered required CI checks (cut rebase wait time)** | 12 required checks fire on every PR including `Mobile — Android debug build` (~3 min) and `RLS pgTAP tests` (~2.5 min) even when no mobile/SQL code changed. With 5 stacked PRs the rebase cascade burns ~30 min of CI per merge cycle. Fix: in `.github/workflows/ci.yml`, scope expensive jobs with `paths:` filters; introduce a single fast `ci-summary` meta-job that always runs and reports the required check name regardless of which downstream jobs fired. Move the 12 specific check names off `required_status_checks` and replace with one `ci-summary`. Branch protection unchanged in spirit — every PR still has to pass — but a docs-only PR finishes in ~30s instead of ~5min. ~2 hr (workflow YAML + branch-protection PATCH). |
| TD-32 | ✅ Shipped · PR #154 | **Run E2E (Playwright) on PR pushes** | E2E was previously only running on push-to-main; PRs got no Playwright coverage. Re-enabled via `pull_request:` trigger; now in CI Summary. |
| TD-35 | ✅ Shipped · PR #187 | **Fix TD-30 path-filter false-skip on lockfile-only PRs** | TD-30's per-job `if:` used `contains(toJSON(github.event.pull_request.changed_files), 'apps/web')` — but `changed_files` is an INTEGER (file count), not a path list, so the predicate was always false and every test job silently SKIPPED on every PR. CI Summary treats SKIPPED as pass, so deps bumps shipped without test verification (e.g. TD-29 #165). Fix: replace with SHA-pinned `dorny/paths-filter@v3` doing real glob-based path matching; new `changes` job exposes `web`/`mobile`/`supabase`/`deps`/`e2e` outputs that downstream jobs gate on via `needs:`. |
| TD-36 | ✅ Shipped · PR #173 | **Mobile lockfile management — pnpm-monorepo compatibility** | Investigation found apps/mobile already in `pnpm-workspace.yaml` + `pnpm-lock.yaml`; the npm-format `package-lock.json` was a stale orphan from a prior `npm install`. Path (a) chosen: deleted the orphan + added to `.gitignore` + documented `pnpm install` requirement in `apps/mobile/CLAUDE.md`. Mobile tests 33 pass / 15 skip (matches baseline); Expo CLI resolves post-install. |
| TD-41 | ✅ Shipped · PR #178 | **PostHog uninitialized in CI breaks every form-submit** | CI doesn't set `NEXT_PUBLIC_POSTHOG_KEY`. Server `lib/posthog-server.ts` constructs `new PostHog(undefined!)` which throws "You must pass your PostHog project's api key" — surfaced as 500 from `/api/onboarding/create` and every other event-capturing route. Browser-side, `posthog.capture()` / `posthog.identify()` calls in `SignInForm` + `OnboardingForm` threw the same message, aborting the submit handler **before** `router.replace('/dashboard')`. Form silently stayed on /signin or /onboarding; `waitForURL` timed out — the visible CI symptom. Fix: server returns no-op stub when no key; client inits with placeholder + `opt_out_capturing()` when no key. Bundles a Next 16 hydration fix in `app/layout.tsx` (anti-FOUC `<script>` was a direct child of `<html>`, hard-failing hydration in React 19). |
| TD-42 | ✅ Shipped · PR #180 | **`ensureCareTeam` helper selector drift — dashboard "View care journal" is a `<p>` not a `<button>`** | `e2e/helpers.ts:91,100,110` waits for `button:has-text("View care journal")`. The dashboard renders that string as `<p className="text-sm text-muted-foreground">` inside a clickable `<Card onClick>` (see `apps/web/app/(app)/dashboard/DashboardClient.tsx:319-323`), so the selector never matches and `ensureCareTeam` times out for any test using it. Slipped in with TD-40 (#177). Fix: replace the three selectors with `text="View care journal"` (or scope to the parent Card heading). Once landed, ai-assistant.spec progresses past `beforeEach` and the remaining 4 consent-modal failures become visible (separate diagnosis). |

### CareSync 2.0 design handoff (UX-054..UX-060) — opened 2026-04-30

Source: second design prototype handoff (`docs/design/caresync-2-0/`, plan at `docs/design/caresync-2-0-plan.md`). The 2026-04-23 prior handoff (UX-14..UX-21) shipped BriefHero + Fraunces typography + Patterns strip + Handoff modal — gap-audited 2026-04-30 to drop those and the marketing hero (already editorial). Net new: Sage palette behind a theme switcher (violet stays default), card-header variants, Now Board, Meds schedule + adherence, Shifts Briefing/Lanes/Now-board, Journal prompted composer + mood spectrum + heatmap, recipient profile card. Six tracks; UX-054 is foundation, UX-055 is utility, UX-056..UX-060 fan out in parallel.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-054 | ✅ Shipped · PR #320 | **Sage palette + ThemeSwitcher (data-theme="sage|hearth|slate" + dark)** | Add Sage parlor tokens (eucalyptus `#5a7a5a`, putty `#f6f4ee`, ochre, clay) and Slate alternate as additional themes; current violet kept as `hearth` and stays default. Wire `data-theme` on `<html>` and a top-right ThemeSwitcher (palette + light/dark) reading/writing `localStorage.theme`. New: `apps/web/components/theme/ThemeSwitcher.tsx`. Touches: `apps/web/app/globals.css` (additive token blocks), `apps/web/app/(app)/layout.tsx`, `apps/web/app/(marketing)/layout.tsx`. **Out of scope:** flipping default to Sage — separate decision row. ~3 hr. |
| UX-055 | ✅ Shipped · PR #321 | **Card header variants (outline / accent left-bar / serif italic title)** | Tinted header is already in `card-header-tinted` pattern; add three additional variants per the prototype: `outline` (border-only), `accent` (left-bar in `--color-primary`), `serif` (Fraunces italic title via `headline-display em`). Expose as `<CardHeaderTinted>`, `<CardHeaderOutline>`, `<CardHeaderAccent>`, `<CardHeaderSerif>` in a new `apps/web/components/ui/CardHeaderVariants.tsx`; CSS classes in `globals.css` `@layer components`. `/tdd-ship` — write component tests asserting class composition + a11y semantics first. ~2 hr. |
| UX-056 | ✅ Shipped · PR #323 | **Today "Now Board" timeline layout** | New dashboard layout variant per prototype: vertical timeline with NOW marker, mood-bordered event cards, grouped Past / Now / Up Next blocks. New: `apps/web/components/dashboard/NowBoard.tsx`. Wire toggle in `DashboardViewToggle.tsx`. Reuses existing `careEvents.timeline` tRPC query. v1 mocks the NOW marker; auto-scroll-to-now is follow-up. ~4 hr. |
| UX-057 | ✅ Shipped · PR #325 | **Meds: per-med day-strip + 7-day adherence chart** | Per the prototype, MedCard gets a 24h day-strip with dose dots positioned by scheduled time and a 7-day adherence row (taken/missed/upcoming). New: `apps/web/components/medications/MedScheduleStrip.tsx`, `apps/web/components/medications/AdherenceChart.tsx`. Touches `apps/web/components/dashboard/MedCard.tsx`. Adherence pulls from existing `care_events` where `event_type='medication'`. Pure-helper `lib/medAdherence.ts` already exists from UX-20 — extend rather than duplicate. ~5 hr. **Owner: Opus (schema-aware).** |
| UX-058 | ✅ Shipped · PR #326 | **Shifts: Briefing handoff + Lanes schedule + Team Now-board** | Three new layouts per prototype. `BriefingHandoff` renders Sleep–Meds–Schedule blocks (replaces the legacy narrative when toggled). `ShiftLanes` is per-person swim-lane timeline with a NOW marker. `TeamNowBoard` groups the team into On-now / Up-next / Later / Off. New files only; existing `ShiftCalendar.tsx` and `HandoffSummary.tsx` (modal from UX-19) remain untouched. Toggle exposed on shifts route. ~6 hr. **Owner: Opus (multiple layouts + schema).** |
| UX-059 | ✅ Shipped · PR #324 | **Journal: prompted 3-question composer + mood spectrum + calendar heatmap sidebar** | Three additive variants. `PromptedComposer` is a 3-question form (today/concern/win) replacing the textarea when "prompted" mode selected; `MoodSpectrum` is a segmented control replacing the badge picker when "spectrum" selected; `MoodHeatmap` is a 5-week calendar in the journal sidebar. New files in `apps/web/components/journal/`. Existing `JournalEntryForm.tsx` and `JournalTimeline.tsx` get a small mode toggle; everything else additive. ~5 hr. |
| UX-060 | ✅ Shipped · PR #322 | **Recipient profile card** | New `apps/web/components/app/RecipientProfile.tsx` per prototype: avatar, name, mood badge, age, conditions, primary caregivers, "About" paragraph. Reads from existing `recipients` + `identity_vault` (PHI rule: surface name through `identityRepository.resolveIdentity` only, never raw). Mounted on a new tab/section of the recipient route. ~3 hr. |

### CareSync 2.0 wiring follow-ups (UX-061..064) — opened 2026-04-30

UX-054..060 shipped the presentational primitives. These four mount them onto their real surfaces. Plan: `docs/plans/plan-c-caresync-2-0-wiring.md`.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-061 | ✅ Shipped · PR #337 | **Wire `<MedScheduleStrip>` + `<AdherenceChart>` into MedCard** | UX-057 shipped both components as pure presentational. Derive a per-day taken/expected series from `care_events` + `medication_schedules` and render the strip + chart inside `MedCard`. New: `apps/web/lib/medAdherenceFromEvents.ts` adapter. Reuse existing `lib/medAdherence.ts`. ~3 hr. **Owner: Opus (schema-aware).** |
| UX-062 | ✅ Shipped · PR #339 | **Mount Shifts BriefingHandoff + ShiftLanes + TeamNowBoard on the shifts route** | UX-058 shipped the three layouts. Add a segmented control on the shifts route (`apps/web/app/(app)/journal/[recipientId]/?panel=shifts` per the gotcha — verify) that switches among Briefing / Lanes / Now-board. Existing `ShiftCalendar.tsx` and `HandoffSummary` modal remain untouched. ~4 hr. **Owner: Opus.** |
| UX-063 | ✅ Shipped · PR #338 | **Mount `<MoodHeatmap>` into JournalLayout sidebar** | UX-059 shipped the heatmap; integration was deliberately deferred. Render in the `JournalLayout` sidebar slot, fed by the existing journal-data hook. ~2 hr. |
| UX-064 | ✅ Shipped · PR #340 | **Mount `<RecipientProfile>` on a discoverable surface** | UX-060 shipped the card. Decide IA: separate `/recipient/[id]/profile` page vs. a tab in the existing journal route. Identity values must flow through `identityRepository.resolveIdentity` server-side; never read `identity_vault` from the client. ~3 hr. **Owner: Opus (PHI-sensitive).** |

### CareSync 2.0 enrichment (UX-065..066) — opened 2026-04-30

UX-062 + UX-064 shipped the surfaces but deliberately deferred narrative + relational data. These rows close those gaps.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-065 | ✅ Shipped · 2026-05-17 · PR #585 | — | — | **BriefingHandoff narrative adapter for the shifts route** | Resolution: pure narrative helpers in `lib/handoffNarrative.ts` (3 functions, 28 table tests) + client `BriefingHandoff.tsx` using existing `careEvents.timeline` tRPC query + Briefing tab in `ShiftsPanel`. PHI-clean (no narrative content to PostHog/Sentry). 34/34 tests green. **Planning lesson:** plan said "4 tabs"; actual ShiftsPanel already had 7 (narrative/week-grid/team-list/questions/calendar/lanes/now), now 8 with briefing. The `overflow-x-auto` fallback was already in place. /oop also flagged tab-role test gap → seeded as TD-173. |
| TD-173 | ✅ Shipped · 2026-05-17 · PR #587 | — | — | **BriefingHandoffPanel test gap: no assertion on tab semantics (`role="tab"`)** | Resolution: added new test file `apps/web/app/(app)/journal/[recipientId]/__tests__/ShiftsPanel.tabs.test.tsx` with 3 assertions: every layout button carries `role="tab"` (including Briefing); tablist has accessible `aria-label`; Briefing tab is a real `<button>` keyboard-reachable element. UTC anchor comment added to `summarizeSleep` overnight filter. JSDoc /oop should-fix downgraded — file header already credited UX-058 + UX-065 correctly. |
| TD-174 | ✅ Shipped · **P3 / Low** | PR #636 | — | **Annotate or test that `FOR UPDATE` is load-bearing in `confirm_ocr_job`** | Surfaced 2026-05-17 by `/oop --from-sprint` on TD-144 (PR #589). The pgTAP test at `supabase/tests/confirm_ocr_job.test.sql` Case 5 calls `confirm_ocr_job` twice in the same transaction to prove the `already_confirmed` branch — but the same test would pass if someone removed the `FOR UPDATE` clause and kept only the post-lock status guard (no concurrent waiters under serializable single-tx). Risk: lock removal regression invisible to CI. **Fix:** either add inline comment in the migration explicitly calling out that `FOR UPDATE` is load-bearing for race-safety + adding the comment to the pgTAP test annotating the coverage gap, OR add a `pg_blocking_pids`/advisory-lock integration test. **Risk:** LOW. **Size:** XS (~15 min for comment route, S ~1h for advisory-lock test). |
| TD-175 | 🟢 Ready · **P3 / Low** | — | — | **Backfill `scan_source='ocr_scan'` on pre-migration confirmed medications** | Surfaced 2026-05-17 by `/oop --from-sprint` on TD-144 (PR #589). The new `confirm_ocr_job` RPC hardcodes `scan_source='ocr_scan'` on medication inserts; the prior route never set `scan_source` so PostgREST used the column default. Medications confirmed via OCR before this migration deployed have a different `scan_source` than those confirmed after — any analytics/UI/query filter segmenting on `scan_source='ocr_scan'` will silently miss the pre-migration cohort. **Fix:** one-time SQL migration that UPDATEs `medications` where a matching `ocr_jobs` row exists with `status='confirmed'` to set `scan_source='ocr_scan'`. **Risk:** LOW (idempotent UPDATE; no schema change). **Size:** XS (~20 min migration + pgTAP). |
| TD-176 | ✅ Shipped · 2026-05-17 · PR #598 | — | — | **Standardize Sentry `component` tag convention for API routes vs tRPC routers** | Resolution: 3-surface convention documented in `docs/project-info/technology/CODE_STANDARDS.md` §Sentry tag convention — tRPC `<router>.<procedure>`, API routes `api.<resource>.<verb>`, Inngest `inngest.<functionName>`. Pre-grep found exactly 2 legacy sites (`apps/web/app/api/ocr/confirm/route.ts` L105+121) — swept `"ocr.confirm"` → `"api.ocr.confirm"`. Doc also calls out the PHI-no-spread rule. |
| TD-178 | ✅ Shipped · 2026-05-19 · PR #632 | — | — | **Replace `identityRepository` error-message interpolation with stable codes** | Resolution: 2 throw sites migrated to `Error("identity_read_failed" \| "identity_create_failed", { cause: error })` form. TD-179 had already fixed the 3rd site (update path uses `error.code`). Tests pin (a) stable code in `.message`, (b) `.cause` reachable for server-side observability, (c) PHI sentinel — no `Jane Doe` / `jane@x.com` / `Key (…)=(…)` fragments survive even when the mock error contains them. **/oop --from-sprint surfaced TD-192** (extend pattern to membershipsRepository 5 sibling sites + normalize update-path contract + Sentry .cause hygiene). |
| TD-179 | ✅ Shipped · 2026-05-18 · PRs #623 / #626 | — | — | **Promote `updateEmergencyInfo` to atomic RPC (eliminate read-merge-write race)** | Resolution: new `public.update_emergency_info(p_org_id, p_recipient_id, p_patch jsonb)` SECURITY DEFINER function with `search_path = public, pg_temp` (CVE-2018-1058 mitigation). Atomic merge via `jsonb_strip_nulls(coalesce(contact_info, '{}'::jsonb) \|\| p_patch)` — shallow top-level merge preserving prior JS semantics. Service-role-only execute; pgTAP coverage 9 cases (3 happy-path merge + cross-org rejection + permission tests + idempotency + null-clear). JS branches on SQLSTATE: `P0002` → `recipient_not_found`, `P0IDF` (user-defined; class P0 is PL/pgSQL user space) → `identity_not_found`. **PHI hotfix (PR #626):** initial catch-all threw `identity_update_failed: ${error.message}` which could leak column values like `Key (email)=(jane@x.com)`. Now drops raw message; surfaces SQLSTATE only (`identity_update_failed: <code>`). Tests assert PHI fragments don't survive. /oop residuals → TD-191. |

| TD-180 | ✅ Shipped · 2026-05-18 · PR #616 | — | — | **Extract `useEditMode` hook to DRY edit-mode + mutation pattern across UX-103b / UX-105b** | Resolution: factory-pattern hook at `apps/web/hooks/useEditMode.ts` (93 lines + 89-line test). Returns `{ isEditing, error, open, cancel, handlers: { onSuccess, onError } }` — caller threads handlers into `useMutation({...})` config, preserving caller-specific side effects (toasts). Adopted in `CareTeamList.tsx` (invite-mutation only; `removeMutation` left untouched, different pattern) + `EmergencyFooterCard.tsx`. **Acceptance flag:** quantitative line-reduction gate not met (−2/−4 vs ≥15 target) because plan's baseline estimate of ~70/~55 lines was inflated — actual edit-mode blocks were ~17/~16 lines. Hook still merged for future-reuse value and centralized state-machine semantics. /oop residuals: TD-186 (cancel-contract leak, onError swallowing, Sentry.setTag gap). |
| TD-181 | ✅ Shipped · 2026-05-18 · PR #617 | — | — | **Extend `carelog/no-phi-in-analytics` ESLint rule to Resend + spread-identifier warning** | Resolution: 3 additions to the rule (1) new `resend.emails.send(payload)` target matcher; (2) walker now recurses into `ArrayExpression` elements that are object literals so `to: [{ email: "..." }]` shapes are inspected; (3) new `spreadIdentifier` messageId emits when a target call receives a bare `Identifier` instead of an inline object literal — caller adds `// eslint-disable-next-line` with justification or refactors to literal. Severity = `error` (not `warn`) because full-codebase sweep produced 0 new errors. **Scope correction:** TD-181's row claimed Sentry.captureException + addBreadcrumb were uncovered; verified they were already covered. Real gaps were Resend (uncovered) and spread-identifier blind spot (already flagged in rule's JSDoc). **Sentinel KEPT:** `refillAlert.test.ts` PHI sentinel does grep-based detection the keys-only ESLint cannot replicate (field-name STRINGS, spread patterns, template interpolation, `html:` presence, substring scan of `resend.emails.send` callsite); comment block added documenting the static/dynamic coverage split. 8 new rule tests (37 → 45). /oop residual: TD-186 (Sentry.setTag/setTags/setExtra/setExtras gap). |
| TD-186 | ✅ Shipped · 2026-05-18 · PR #619 | — | — | **TD-180/181 /oop residuals — cancel-contract, error pass-through, Sentry.setTag coverage** | Resolution: 3 sub-fixes in one PR. (1) `useEditMode` gained `onCancel?: () => void` arg with locked ordering — `setIsEditing(false) + setError(null)` FIRST (via `flushSync` for observable post-close state), then `onCancel?.()`. `EmergencyFooterCard` drops bespoke `handleCancel` wrapper; registers field-reset via `onCancel`. (2) New `apps/web/lib/formatMutationError.ts` whitelist tRPC error codes, strips Postgres constraint suffixes (`_pkey|_key|_idx|_fkey|_check|_excl|_unique`) + Zod field-path prefix per `[\n;]` segment; routes unknown codes to generic "Something went wrong." Wired in both components' `onError`. 17 formatter tests. (3) ESLint rule extended to `setTag/setTags/setExtra/setExtras` — singular-form gets new `forbiddenTagKey` messageId for string-Literal key inspection. 10 new rule tests. All 2302 vitest tests + lint + typecheck green. /oop residuals → TD-188 (constraint-regex over-stripping, `flushSync` removal, TemplateLiteral gap). |
| TD-182 | ✅ Shipped · 2026-05-19 · PR #633 | — | — | **Refactor `getRefillRecipients` role/recipient filter out of PostgREST `.or` string DSL** | Resolution: DB-side `.in("role", ["coordinator", "caregiver", "aide"])` + exported pure `isRefillRecipient(membership, recipientId)` predicate. /opus-on-opus cycle 1 caught must-fix on `.limit(50)` row-set widening (DB role filter required to preserve cap equivalence). 6 new predicate tests including 60-row volume regression. **/oop surfaced TD-193** (extract shared `resolveMembershipsToUsers` helper — same chunked-allSettled pattern appears in `getCareTeamForRecipient`). |
| TD-183 | ✅ Shipped · 2026-05-18 · PR #620 | — | — | **Consolidate week-stamp helpers in `@carelog/utils`** | Resolution: `getWeekStamp` deleted (verified DEAD CODE — zero consumers outside its own tests; row's "weeklyDigest consumes it" hypothesis was wrong). `isoWeekStamp(date: Date): string` hoisted to `packages/utils/src/index.ts` with ISO 8601 §3.4 JSDoc + UTC-only semantics. `refillAlert.ts` swapped to import from `@carelog/utils`; test import swapped. 5 new tests including W53/2026 (verified 2026 IS a 53-week ISO year), midweek, zero-pad, year-edge Sunday, TZ-parity (Pacific vs UTC same instant → same week-stamp). Subagent flagged pnpm+worktree symlink gotcha — bypassed pre-commit with `--no-verify` after stash-comparison confirmed env-cause (CLAUDE.md known gotcha); CI on fresh resolution passed. **Discovery deferred:** `weeklyDigest.ts` has NO idempotency check at all → seeded as **TD-187**. |
| TD-187 | ✅ Shipped · 2026-05-19 · PR #656 | — | — | **`weeklyDigest` idempotency gap — cron retry could re-send digests** | Surfaced 2026-05-18 by `/sprint` Step 4 (TD-183 scoping). `weeklyDigest.ts` lacks any `email_dispatch_log`-style idempotency guard, so an Inngest cron retry (network blip, transient Resend 5xx, manual re-run) could re-send the weekly summary email to every active org. Pattern to copy: ON-71 Phase 2 (PR #599) — `email_dispatch_log` table keyed on `(org_id, kind, week_stamp)`, INSERT before Resend send, refuse duplicates. With `isoWeekStamp` now in `@carelog/utils` (TD-183), the week-stamp source is consistent across `refillAlert` and `weeklyDigest`. **Risk:** MEDIUM (no current evidence of duplicate sends, but the surface is unprotected). **Size:** M (~3h — schema + idempotency check + tests + Sentry on conflict). |
| TD-188 | ✅ Shipped · 2026-05-18 · PR #622 | — | — | **TD-186 /oop residuals — formatMutationError over-strip, useEditMode flushSync, ESLint TemplateLiteral gap** | Resolution: 3 sub-fixes in one PR. (1) `formatMutationError.ts` — added `PG_DIAG_RE` gate (only strip Postgres constraint suffixes when message contains `duplicate key\|violates\|constraint`); tightened regex requires ≥1 internal underscore separator so legitimate `license_key`-style fields aren't stripped; `err.cause?.message` included in gate input only (never copied to output, with 2 PHI sentinel tests proving). (2) `useEditMode.ts` — `flushSync` removed; sequential setters; LOCKED JSDoc replaced with CHANGELOG-style "TD-188 contract migration" comment. Caller-grep verified `EmergencyFooterCard.onCancel` doesn't read `isEditing` synchronously. (3) `no-phi-in-analytics.js` — `forbiddenTagKey` now accepts `TemplateLiteral` with `expressions.length === 0` (catches `` Sentry.setTag(`email`, x) ``); template with interpolation correctly NOT flagged. 23 + 9 + 48 = 80 scoped tests; 2311 vitest total. /oop residuals → TD-191. |
| TD-191 | ✅ Shipped · **P3 / Low** | PR #637 | — | **TD-188/179 /oop residuals — useEditMode onCancel ref-instability + React-warning regex + formatMutationError nit + ESLint asymmetry + pgTAP Case 9 dependency** | Surfaced 2026-05-18 by `/oop --from-sprint` on PRs #622+#623. Five small follow-ups: (1) `apps/web/hooks/useEditMode.ts:98-105` — `cancel` callback depends on `onCancelCallback` in dep array; inline arrows from callers create new function reference per render, invalidating the memoized `cancel`. Document "wrap onCancel in useCallback at the caller" or wrap internally via a ref. (2) `apps/web/hooks/__tests__/useEditMode.test.tsx:147-151` — React-warning regression filter `/react\|warning/i` may miss real React 19 warnings whose first arg doesn't contain those literals (e.g., "Cannot update a component (X) while rendering…"). Tighten to `expect(consoleErrorSpy).not.toHaveBeenCalled()` or match on known prefixes. (3) `apps/web/lib/formatMutationError.ts:126` — cosmetic: `gateInput = \`${raw} ${causeMsg}\`` allocates; use `PG_DIAG_RE.test(raw) \|\| PG_DIAG_RE.test(causeMsg)` to make gate-only contract obvious. (4) `apps/web/eslint-rules/no-phi-in-analytics.js:255-285` — TemplateLiteral handling is asymmetric: only singular setTag/setExtra path covers it; computed template keys like `posthog.capture("ev", { [\`email\`]: x })` escape the recursive walker's `isLiteralKey`. Code-comment the asymmetry (rare in practice; documented surface is singular only). (5) `supabase/tests/update_emergency_info.test.sql:179-186` — Case 9 (lives_ok patch A then results_eq patch B) implicitly depends on Case 6 (line 127-135) having deleted `dnr_status` first. Reordering would silently break. Add explicit pre-state assertion before line 179. **Risk:** LOW. **Size:** S (~1h all 5). |
| 🚩 TD-189 | 🟢 Ready · **P1 / High — pick first** | — | — | **Backlog hygiene upgrades — row template + sharded backlog + pre-flight row-verification skill** | Surfaced 2026-05-18 during a session-retrospective on /sprint pipeline efficacy. Every sprint this session, /opus-on-opus cycle 1 has surfaced 2-3 must-fix items rooted in **vague or stale backlog row descriptions** rather than implementation flaws (examples: TD-181 row claimed `Sentry.captureException` was uncovered → verified already covered; TD-183 row claimed `weeklyDigest` consumed `getWeekStamp` → verified dead code, no consumers; TD-186 onError pass-through would have leaked Postgres constraint names → led to scoping `formatMutationError` mid-sprint). The pattern: rows written days-to-weeks before pickup go stale against the live codebase, and the planning agent re-discovers the truth only at /opus-on-opus cycle 1 — burning 1-2 must-fix → re-review cycles per sprint. **Three coordinated upgrades:** (1) **Row template** — add a "row schema" to BACKLOG.md §11 (Agent contract) requiring every TD/ON/UX row to include: target file paths verified at write-time; one explicit assertion-of-current-state (e.g., "as of 2026-05-18 `getWeekStamp` is unused per grep"); risk rating; size estimate; verification command. Existing rows grandfathered; new rows must conform. (2) **Sharded backlog** — extract per-row content into `backlog/<id>.md` (one file per row), keep `BACKLOG.md` as the at-a-glance index pointing into shards. Eliminates the parallel-PR merge-conflict pattern (today: 90% of multi-PR rebase pain is BACKLOG.md adjacent-row collisions, per CLAUDE.md `## Parallel-dispatch BACKLOG.md conflicts`). Update `/backlog-sync` skill to read/write shards. ADR-0002 amendment required. (3) **Pre-flight row-verification skill** (`/verify-row <id>`) — runs before /sprint Step 4 (implementation-plan): re-greps target file paths against current source, diffs row's claims against actual code, surfaces drift to the planner BEFORE the plan is written. Returns "row is current" / "row is stale: <diffs>" / "row is wrong: <evidence>". `/sprint` skill change: between Step 3 (Gate 1) and Step 3b, fire `/verify-row` on each row in the chunk; any non-current row blocks until user acknowledges or row is rewritten. **Risk:** MEDIUM (touches BACKLOG-as-SoT contract; sharding migration is high-blast-radius). **Size:** M (~half-day with planning; possibly 2-track wave: sharding migration + template/skill in parallel). **Plan:** `docs/plans/2026-05-18-td-189-backlog-hygiene-upgrades.md`. Flagged ASAP per session retrospective. |
| TD-190a | 🟢 Ready · **P2 / Medium** · blocked-by TD-189 | — | — | **Multi-session skill: `/dispatch-sprint-session` — spawn worker session with handoff** | Surfaced 2026-05-18 during retrospective on parallel-session driver pattern (alternative to in-session subagent dispatch). Driver-session skill that spawns a worker session (via `claude -p` or external terminal), assigns a pre-reserved chunk, hands off context (plan path, branch names, scope contract), and returns a `session-id`. Worker writes status to `.claude/state/multi-sprint/<session-id>.json` for the driver's `/sprint-board` to observe. **Why blocked by TD-189:** worker sessions consume sharded backlog rows; building against today's monolithic `BACKLOG.md` requires re-work after sharding lands. **Why a project skill, not user-level:** Carelog-specific session-spawn conventions (worktree paths, branch naming, Vercel-deploy-cadence counter handling). **Risk:** MEDIUM (process orchestration; spawning sessions has real cost — Opus driver running continuously is subscription burn). **Size:** S (~2h once TD-189 lands). |
| TD-190b | 🟢 Ready · **P2 / Medium** · blocked-by TD-189 | — | — | **Multi-session skill: `/sprint-board` — read-only dashboard over worker sessions** | Surfaced 2026-05-18 with TD-190 family. Read-only dashboard skill that lists all active worker sessions, their stage (`planning`/`wave`/`oop`/`blocked`), elapsed time, current PRs. Driven from `.claude/state/multi-sprint/*.json` files written by workers. Replaces "ask each session where it's at" — driver polls one file path. **Why blocked by TD-189:** session state files reference backlog row IDs; coupling to sharded format simplifies the schema. **Risk:** LOW (read-only). **Size:** XS (~45 min). |
| TD-190c | 🟢 Ready · **P2 / Medium** · blocked-by TD-189 | — | — | **Multi-session skill: `/reserve-chunk` — atomic claim with timeout** | Surfaced 2026-05-18 with TD-190 family. Driver-side atomic claim mechanism on backlog row IDs to prevent two parallel sessions from picking the same chunk. Writes `.claude/state/multi-sprint/reservations.json` with `{td-189: {session: abc123, claimed-at: <ts>}}`. Blocks other `/reserve-chunk` calls on the same id. Releases on session completion or 2h timeout (whichever first). **Hardest part:** atomicity — `flock` on the JSON file vs. SQLite-backed lock. Document the choice in the skill. **Why blocked by TD-189:** row-IDs as lock keys assume the sharded backlog's stable per-row identity. **Risk:** MEDIUM (correctness-critical for multi-session; bugs cause double-work). **Size:** S (~2h). |
| TD-190d | 🟢 Ready · **P3 / Low** · blocked-by TD-189 | — | — | **Multi-session skill: `/serialize-close-out` — driver-owned PR merge serializer** | Surfaced 2026-05-18 with TD-190 family. Driver-owned wrapper that workers ping with "ready to close"; driver picks ONE to merge `BACKLOG.md` changes first, rebases the others, merges in serial order to prevent §0 row-count race. **CAVEAT:** likely collapses to a `/backlog-sync` re-run once TD-189 sharded-backlog lands — sharding makes `BACKLOG.md` updates atomic per-shard rather than adjacent-row, so the row-count race vanishes. Re-evaluate scope after TD-189 ships: this may downgrade to "delete; subsumed by post-sharding /backlog-sync." **Risk:** LOW. **Size:** XS post-TD-189 if needed at all. |
| TD-192 | ✅ Shipped · 2026-05-20 · PR #657 | — | — | **Extend TD-178 stable-error-code pattern across the repository layer + normalize update path + Sentry cause hygiene** | Surfaced 2026-05-19 by `/oop --from-sprint` on the TD-178/182/184 sprint (findings S1 + S2). Three coordinated cleanups: **(a)** `apps/web/server/repositories/membershipsRepository.ts:23, 80, 161, 219, 232` — 5 sibling throw sites still use `throw new Error(\`<context> failed: ${error?.message}\`)`. Same PHI defense-in-depth concern as TD-178 (`Memberships fetch failed: …`, `getCareTeamForRecipient failed: …`, `getRefillRecipients failed: …`, `Membership creation failed: …`, `Invite token creation failed: …`). Migrate each to `throw new Error("<stable_code>", { cause: error })` with matching test updates. **(b)** `apps/web/server/repositories/identityRepository.ts:145` — `updateEmergencyInfo` still throws `identity_update_failed: ${code}` with the code suffixed to `.message`. T1 (TD-178) shipped `identity_read_failed` / `identity_create_failed` as code-only message + `cause:` form. Normalize on the cause-bearing form so all 3 identityRepository throw sites have the same contract. Update test at line 451 to assert `err.cause.code === "23505"` instead of `err.message === "identity_update_failed: 23505"`. **(c)** Sentry serialization hygiene — `Error("…", { cause: pgError })` preserves the raw Postgres error object via `.cause`. Default Sentry serialization walks `cause` and can surface column values to log aggregators. Either (i) strip `cause` before `Sentry.captureException`, OR (ii) install a `beforeSend` hook that walks `cause` and redacts message/detail fields. Document the choice in `apps/web/sentry.server.config.ts` + add a sentinel test. **Risk:** LOW. **Size:** S (~1.5h all 3 — 5 throw sites + 1 normalize + 1 Sentry config). |
| TD-193 | 🟢 Ready · **P3 / Low** | — | — | **Extract `resolveMembershipsToUsers` shared helper — deduplicate chunked-allSettled rate-limit pattern** | Surfaced 2026-05-19 by `/oop --from-sprint` on the TD-178/182/184 sprint (finding S3). `getCareTeamForRecipient` (lines 84-115) and `getRefillRecipients` (lines 166-194) both own identical `CARE_TEAM_CHUNK_SIZE=8` chunking around `Promise.allSettled` + per-row `supabaseAdmin.auth.admin.getUserById` + `console.warn("…: getUserById rejected", reason)` on rejected settlement. Same Auth Admin rate-limit policy (50/s/project); two implementations. **Fix:** extract `resolveMembershipsToUsers<T>(rows, mapFn): Promise<T[]>` that owns the chunking + allSettled + warn-on-reject contract. Caller passes a mapper `(row, user) => T | null`. `CARE_TEAM_CHUNK_SIZE` becomes the helper's private constant. Existing tests pin both functions' output shapes; both should pass post-extraction. **Risk:** LOW (pure refactor; same behavior). **Size:** S (~1h — extract + adapt 2 callers + add helper unit test). |
| TD-194 | 🟢 Ready · **P2 / Medium** | — | — | **Adopt `sessions-state-update.sh` in `/sessions-prune` + `/sprint --session` skills (atomic state-file writes)** | Surfaced 2026-05-19 during session s2 of the TD-178/182/184 sprint. Concurrent multi-session edits to `.claude/state/claims.json` + `.claude/state/sessions/INDEX.md` were not atomically protected — a session reading the file, modifying in-memory, and Writing back could clobber a concurrent session's row written in the meantime. Same-machine race confirmed (session s1 added a TD-189 claim mid-sprint while s2 was active; s2's Write would have clobbered it without explicit Read-then-merge discipline). **Race fix already shipped 2026-05-19** as `~/.claude/scripts/sessions-state-update.sh` (mkdir-based mutex + jq atomic update → tmp file → mv; refuses claim conflicts via exit 3; self-heals stale locks). Documented in `~/.claude/docs/multi-session-setup.md` §"Atomic state-file updates". **Adoption gap:** the `/sessions-prune` skill still uses direct file Write for the release path; the `/sprint --session` skill at Gate 1 also writes claims directly. Migrate both to invoke the script so same-machine concurrent sessions inherit the protection automatically. **Cross-machine sessions** still use the PR-based claim pattern from the original design (script is same-machine only — file lock doesn't propagate across hosts). **Risk:** MEDIUM (touches multi-session contract; bugs cause silent claim loss). **Size:** S (~1.5h — 2 skill updates + smoke tests with simulated concurrent runs). |
| TD-195 | ✅ Shipped · 2026-05-20 · PR #641 | — | — | **Conditional E2E in CI — skip Playwright shards when diff is component-isolated** | Surfaced 2026-05-19 by operator after the parallel-sprint test revealed CI E2E shards eating ~8 min × 2 PRs on diffs that touched only `apps/web/hooks/*`, `apps/web/lib/*`, `apps/web/eslint-rules/*`, and `supabase/tests/*` — none of which can affect E2E. **Fix:** add a `dorny/paths-filter@v3` step early in `.github/workflows/ci.yml` that classifies the diff into buckets (`e2e-required`, `vitest-only`, `pgtap-only`, `docs-only`). Then guard the 3 Playwright shard jobs + `e2e-string-drift` job with `if: needs.detect-changes.outputs.e2e-required == 'true'`. **Force-run E2E** allowlist: any change under `apps/web/app/**`, `e2e/**`, `apps/web/middleware.ts`, `apps/web/lib/supabase/**`, any tRPC router file (`apps/web/server/routers/**`), `package.json`/`pnpm-lock.yaml`, `.github/workflows/ci.yml` itself. **Backstop:** the nightly `e2e-nightly.yml` (when created — TD-87 streak gate already references it) catches what the per-PR gate missed. Today's CI Summary job needs updating to treat `skipped` E2E as success when the gate fired, NOT as failure. **Risk:** MEDIUM (false negative = bug ships to main; mitigated by nightly + force-run allowlist breadth). **Size:** S (~1h — paths-filter step + 4 `if:` gates + CI Summary tweak + 1 test PR per bucket to verify). |
| TD-197 | ✅ Shipped · 2026-05-20 · PR #642 | — | — | **tRPC router Zod-schema snapshot tests — fast contract-drift detection at the unit-test layer** | Surfaced 2026-05-19 by operator as the intermediate step between TD-195 (conditional E2E) and TD-196 (full Pact-style contract testing). Goal: keep CI iterations fast (after TD-195 lands) WITHOUT losing the API contract-drift safety net E2E currently provides. **Implementation:** new vitest spec under `apps/web/server/routers/__tests__/schema-snapshot.test.ts` that walks every tRPC router, extracts each procedure's input + output Zod schemas via `zod-to-json-schema` (or similar), and snapshots the JSON shapes to a checked-in baseline at `apps/web/server/routers/__tests__/__snapshots__/api-schemas.snap.json`. PR fails if a snapshot changes; review prompt forces the author to (a) bump a version field in the consumer-deps signal (`apps/web/server/api-version.ts` or similar), OR (b) explicitly acknowledge a breaking-change with a sibling consumer update. **Why this is enough today:** tRPC types already catch breaking changes at compile time within the monorepo, BUT compile-time types don't track which consumers have actually been redeployed against which API version — snapshots make the drift explicit and reviewable. **Coverage scope (v1):** input/output schemas only (the wire contract); error-code enums; tRPC procedure types (query / mutation / subscription). Out of scope v1: middleware behavior, RLS scoping (covered by pgTAP + E2E). **Pairs with TD-195:** when conditional E2E skips a PR, this snapshot test still runs (it's a vitest spec, runs in every CI bucket except docs-only) — so confidence is maintained. **Risk:** LOW (additive). **Size:** M (~3h — walker + snapshot infra + baseline capture + 1 intentional-drift test fixture). |
| TD-196 | 🧊 Deferred · **P3 / Low** · _revisit when mobile splits from web on independent Expo OTA cadence_ | — | — | **Consumer-driven contract testing (Pact / similar) between web + mobile tRPC consumers and the API provider** | Surfaced 2026-05-19 by operator alongside TD-195 (conditional E2E). **Why deferred today:** Carelog's tRPC stack already provides compile-time type contracts between `apps/web` (Next.js client) and the server router; `apps/mobile` (Expo) currently shares the same monorepo client and deploys in lockstep with web — no independent-consumer compatibility risk yet. tRPC types catch the same breaking-change cases Pact would, at zero runtime cost. **Revisit when:** `apps/mobile` starts independent Expo OTA updates that ship without a web redeploy (e.g., mobile-only feature, mobile shipping a different API version than current production web). At that point the consumer/provider relationship becomes async and contract tests pay off — Pact-style consumer-driven contracts let mobile pin the API shape it depends on without the web team having to redeploy in lockstep. **Intermediate option (if needed before mobile splits):** tRPC router Zod-schema snapshot tests under `apps/web/server/routers/__tests__/`, asserting input/output shape per router; PR fails if a snapshot changes without a sibling-bump signal. Lower setup than Pact, catches the breaking-change case at the unit-test layer. **Risk:** LOW (deferred — no work until revisit). **Size:** Pact route M (~half-day setup + ongoing maintenance). Snapshot-test intermediate S (~3h). |
| TD-198 | ✅ Shipped · 2026-05-20 · PR #649 | — | — | **CI velocity (Track A) — split vitest into 3 parallel project-jobs + pgTAP paths-filter fast-skip** | Surfaced 2026-05-19 by operator after TD-195/197 landed. Current `web-tests` job runs `npx vitest run` over all 3 vitest projects (browser/node/a11y from `apps/web/vitest.config.ts`) serially within a single CI job (~36s locally; longer in CI cold-cache). Browser project has `fileParallelism: false` by design (single chromium instance). **Fix part 1 — split:** change `web-tests` job in `.github/workflows/ci.yml` into a matrix job `vitest: { project: [web, node, a11y] }` running `npx vitest run --project=${{ matrix.project }}`. Net effect: 3 jobs run in parallel; wall time drops from ~36s to ~max(web, node, a11y). **Fix part 2 — pgTAP fast-skip:** the existing `rls-tests` job at line 250 of ci.yml runs `supabase test db` unconditionally on every PR. Add an `if: needs.changes.outputs.supabase == 'true' || needs.changes.outputs.deps == 'true'` gate (paths-filter already classifies `supabase` per TD-195 — verify outputs include it; if not, extend the filter). Mirrors TD-195 pattern. **Force-run allowlist for pgTAP:** any change under `supabase/**`, `pnpm-lock.yaml`, `.github/workflows/ci.yml`. **Risk:** MEDIUM (false negative on RLS = bug ships; mitigated by force-run allowlist breadth + matrix `fail-fast: false` so a flake in one project doesn't cancel the others). **Size:** S (~1.5h — 1 ci.yml edit). **Track:** A of parallel-4-ci-velocity sprint. |
| TD-199 | ✅ Shipped · 2026-05-20 · PR #648 | — | — | **CI velocity (Track B) — Turborepo cache config for typecheck + lint tasks** | Surfaced 2026-05-19 by operator alongside TD-198. `turbo.json` currently defines `build` and `dev` tasks but NOT `typecheck` or `lint`, so root-level `turbo run typecheck` falls through to per-workspace `package.json` invocations with NO cache. **Fix:** add `tasks.typecheck` and `tasks.lint` to `turbo.json` with `inputs: ["src/**", "*.ts", "*.tsx", "*.mjs", "package.json", "tsconfig.json", "eslint.config.mjs"]` and `outputs: []` (since both are check-only, no artifacts to cache — just the success signal). Add root `package.json` scripts `typecheck` and `lint` that invoke `turbo run typecheck` and `turbo run lint` so CI and local dev get cache hits on re-runs. **Scope strictly limited to:** `turbo.json` + root `package.json` only — do NOT modify `apps/web/package.json` or `.github/workflows/ci.yml` (those are owned by Tracks E [unused] and A respectively). **Speedup:** primarily local dev re-runs; CI benefits arrive when Track A's ci.yml converts to `turbo run`, but Track B does not require that. Inert-but-not-breaking until consumed. **Risk:** LOW (additive config; no behavior change). **Size:** XS (~30 min — turbo.json + 2 root script entries + 1 README note). **Track:** B of parallel-4-ci-velocity sprint. |
| TD-200 | ✅ Shipped · 2026-05-20 · PR #650 | — | — | **CI velocity (Track C) — vitest config tuning for node + a11y projects** | Surfaced 2026-05-19 by operator alongside TD-198. `apps/web/vitest.config.ts` configures 3 projects (web/node/a11y). The **node** project has no `pool` override — vitest 4 defaults to `forks` which carries per-file process-spawn overhead. The **a11y** project defaults the same and uses jsdom (10 test files, all short). **Fix:** add `pool: "threads"` + `poolOptions: { threads: { singleThread: false, maxThreads: undefined } }` to the node project's `test:` block; do the same for a11y. Browser project (with `fileParallelism: false`) untouched. **Scope strictly limited to:** `apps/web/vitest.config.ts` only — no other files. **Speedup:** node project's ~70 test files start-up time drops (no per-file fork); a11y similar. Independent of Track A — Track A's matrix split still gains from this within each project job. **Risk:** LOW (pool change should be transparent; node tests have no fork-specific state). **Size:** XS (~30 min — config edit + verify suite green). **Track:** C of parallel-4-ci-velocity sprint. |
| TD-201 | ✅ Shipped · 2026-05-20 · PR #647 | — | — | **CI velocity (Track D) — Playwright workers + fullyParallel tuning** | Surfaced 2026-05-19 by operator alongside TD-198. Root `playwright.config.ts` controls per-shard concurrency. Currently ci.yml's 3 Playwright shards each run with default workers (typically `undefined` → CPU count). On 2-core GHA runners that's ~2 workers/shard = 6 effective parallel browsers across the 3 shards. **Fix:** explicitly set `workers: process.env.CI ? 2 : undefined` and `fullyParallel: true` in `playwright.config.ts`. Confirms intended parallelism and prevents surprise CPU-count fluctuations on different runner SKUs. **Scope strictly limited to:** `playwright.config.ts` (root) only. **Speedup:** modest — primarily stabilizes existing 3-shard wall time; small reduction if any shard had a serial-by-default project. **Risk:** LOW (pure config tweak; flake risk if workers > 2 on resource-constrained runners — but explicit `workers: 2` is conservative). **Size:** XS (~30 min — config edit + 1 verification PR). **Track:** D of parallel-4-ci-velocity sprint. |
| TD-177 | 🟢 Ready · **P3 / Low** | — | — | **Populate Vercel `development` env so `vercel env pull` produces a working `apps/web/.env.local`** | Surfaced 2026-05-17 during tmux-preflight. Running `vercel env pull apps/web/.env.local` from a clean checkout returns only 9 vars and is missing: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_PRICE_MONTHLY`, `STRIPE_PRICE_ANNUAL`, `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_EMAIL`, `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`. Net effect: every fresh-machine / fresh-worktree dev bootstrap requires manual filling from `supabase status -o env` + scattered notes; new contributors blocked. **Fix:** (1) populate the missing keys in Vercel project `carelog` → Settings → Environment Variables → Development scope (Supabase URL/anon should point at local `127.0.0.1:54321` defaults via a documented post-pull override, OR add a `dev` env layer); (2) document the `SUPABASE_SERVICE_ROLE_KEY` swap (Vercel pull returns the hosted-DB key, local Postgres needs the demo `SERVICE_ROLE_KEY` from `supabase status -o env`) — add a README note in `apps/web/` or `docs/project-info/runbooks/`. **Risk:** LOW (Vercel dashboard config only; no code change). **Size:** S (~30 min — populate ~12 keys + write the runbook note). |
| UX-066 | 🧊 Deferred · superseded by UX-103/104/105 | **RecipientProfile enrichment — mood / caregivers / About** | Filed 2026-04-30 as a single 5-hr row. UX-103 (CareTeamList adapter), UX-104 (likes/dislikes schema), UX-105 (emergency info schema) split the same surface into 3 implementable rows on 2026-05-01 — those are now the canonical work items. Strike here once a successor row ships. |

### CareSync handoff (frozen design, UX-077 follow-up) — opened 2026-05-01

Source: `docs/caresync-handoff/` (clickable React+Babel design prototype, frozen via constants in `app.jsx`'s `DESIGN` object). Plan: `docs/design/caresync-handoff-plan.md`. 28 stories shipped on 2026-05-01 (PRs #349..#365); see §7. UX-077 below is the only remaining row — deferred pending product decision.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-077 | 🟢 Ready | **Today route default flip — TimeRailTimeline as default, NowBoard behind `?layout=board`** | Deferred during 2026-05-01 PM session: there is no separate Today route — the dashboard already has a 3-way `DashboardViewToggle` (single / stacked / now). Flipping the default to TimeRailTimeline (or adding it as a 4th option) needs a product decision on whether the timeline view should replace one of the existing three or live alongside. The presentational components (`TimeRailTimeline`, `TimelineFilterChips`) shipped in #361 and are ready to wire once direction is decided. |

### CareSync handoff follow-ups (UX-095..UX-106) — opened 2026-05-01 PM

After the 28-story handoff session shipped (#349..#365), an honest audit revealed several presentational components are built + tested but not yet visible to users — they're orphaned (never mounted) or mounted with empty states pending data plumbing. These rows close those gaps. Plan: `docs/design/caresync-handoff-followups-plan.md`. Three waves: (8) Brief surface mount + adapters — no schema. (9) Shifts data — 2 of 3 need migrations. (10) Profile data — 2 of 3 need migrations. (11) UX-077 + default-flip decisions.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-095 | ✅ Shipped · PR #368 | **Brief surface mount** | Mounted SleepSparkline + ComingUpRows + ShiftQuoteNote + OnShiftSidebar + PatternCard into DashboardClient. |
| UX-096 | ✅ Shipped · PR #370 | **sleepFromEvents adapter** | Pure helper deriving 7-night sleep buckets from `care_events`. |
| UX-097 | ✅ Shipped · PR #369 | **comingUpEvents adapter** | Next ≤5 scheduled events helper for `<ComingUpRows>`. |
| UX-098 | ✅ Shipped · PR #369 | **deriveOnShift adapter** | Current/next caregiver + latest mood join helper. |
| UX-099 | ✅ Shipped · PR #370 | **detectPattern helper** | Threshold-driven 7d pattern signal for `<PatternCard>`. |
| UX-100 | ✅ Shipped · PR #374 | **ShiftWeekGrid blocks adapter** | `buildShiftWeekGridBlocks` helper + ShiftsPanel "Week" tab wire. |
| UX-101 | ✅ Shipped · PRs #375/376/377 | **Shift narrative-handoff schema + tRPC + dashboard mount** | Split into 3a (migration + pgTAP + types), 3b (upsertHandoff/getLatestHandoff + ShiftsPanel wire), 3c (ShiftQuoteNote on dashboard). |
| UX-102 | ✅ Shipped · PRs #378/379 | **shift_questions schema + tRPC + Questions tab wire** | Split into 4a (table + RLS + immutability trigger + 15 pgTAP tests) and 4b (list/create/resolve tRPC + composer + resolve flow in ShiftsPanel). |
| UX-103 | ✅ Shipped · PR #451 | **CareTeamList adapter on profile route** | See §7 "UX-103 Wire CareTeamList — `getCareTeamForRecipient`…". §1 row was a stale duplicate of the §7 entry; promoted by `/backlog-sync` stale-Ready cleanup 2026-05-15. |
| UX-104 | ✅ Shipped · PR #452 | **Recipient likes/dislikes adapter** | See §7 "UX-104 Wire LikesDislikesList — `getRecipientPreferences`…". §1 row was a stale duplicate. |
| UX-105 | ✅ Shipped · PR #453 | **Emergency info adapter** | See §7 "UX-105 Wire EmergencyFooterCard — pure `parseEmergencyInfo`…". §1 row was a stale duplicate. |
| UX-106 | 🧑 Needs human | **Decision: flip default app shell to SageRail** | UX-068c shipped SageRail + SageTopBar behind `?shell=sage` opt-in. Decide whether to (a) flip the default and demote AppTabBar to legacy, (b) keep opt-in until further user testing, or (c) ship SageRail as the desktop chrome and keep AppTabBar mobile-only. ~1 hr exec once decided. |

### CI regression — opened 2026-04-30

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-106 | ✅ Shipped · PR #343 | **Fix `e2e/export.spec.ts:81` — toast text drift after TD-96** | TD-96 (PR #306) standardized mutation-error toasts and changed `ExportButton`'s catch-block toast from `"Export failed. Please try again."` to `"The export didn't finish. Try again, or pick a smaller date range."`. The Playwright spec at `e2e/export.spec.ts:99-100` still asserts the old text → "export error message shown when API returns non-OK" fails on every PR with `Error: element(s) not found / Locator: getByText('Export failed. Please try again.')`. Mergify lets PRs through despite the failure, but the regression net is permanently red and any *new* export regression would now go undetected. Fix: update the spec's locator to match the current sonner toast (`"The export didn't finish."` or a tighter regex). Also worth checking for other catch-block toasts whose copy was rewritten by TD-96 and whose tests weren't updated. ~0.5 hr. |

### Tier 1/2 server testing sweep — Plan A (TD-77..82, TD-87)

Existing rows above. Plan: `docs/plans/plan-a-tier12-tests.md`. Six file-disjoint test files + one CI workflow tweak.

### Web hardening sweep — Plan B (TD-98, TD-100..104, A11Y-019)

Existing rows above. Plan: `docs/plans/plan-b-web-hardening.md`. Seven mostly-disjoint hardening tracks across truncation, pagination, RTL, retry config, debounce, pluralize, SR-only live region.

### Roadmap features (ON-64..68) — opened 2026-04-25

From `docs/project-info/product/ROADMAP.md` Phases 3–5. Greenlit 2026-04-25 to add to Ready queue. Sequencing rationale per ROADMAP.md §"Feature sequencing rationale".

| ID | Status | Story | Notes |
|---|---|---|---|

### Roadmap-promised features missing from BACKLOG (ON-70..74) — opened 2026-05-09

Surfaced by the 2026-05-09 audit (`docs/audits/2026-05-09-roadmap-and-harness-audit.md` §H): ROADMAP promises these features but no Ready row tracked them. Filed so they don't silently slip out of scope.

| ID | Status | Story | Notes |
|---|---|---|---|
| ON-70 | 🟢 Ready | **Coverage request board (`coverage_windows` table + claim flow + gap detector)** | Per ROADMAP Phase 2. New `coverage_windows` table separate from `shifts` — gap detection runs on coverage_windows, not shifts (decouples "what we scheduled" from "what was actually covered"). Coverage request flow: caregiver posts when can't make a shift; team members see it and can claim; gap detector alerts coordinator on uncovered windows. **Schema work + RLS — pgTAP coverage required.** ~5 days. |
| ON-71 | ✅ Shipped · 2026-05-17 · PR #599 (+ Phase 1: da3cbb9) | **Refill alerts Inngest job (`supply_days_remaining ≤ 7` nightly)** | Resolution: 2-phase ship. Phase 1 (audit-trail) shipped in da3cbb9 (`feat(p3-04)`). Phase 2 (PR #599) added the email notification: batched-per-(org,recipient) weekly Resend dispatch, new `email_dispatch_log` table with composite UNIQUE on (kind, dedup_key) + pending-row sweep for idempotency, new `getRefillRecipients` repository helper, pharmacy contact pre-populated in plain-text body (XSS surface closed by text-only emails). Threat-model (1 Crit + 4 High + 6 Med) fully mapped with live PHI sentinel test verifying no drug_name/pharmacy/to-address leaks to Sentry. /oop seeded TD-181/182/183 + ON-71c (opt-out) + ON-71d (bounce handling). |
| ON-72 | 🧊 Deferred | **Prescription label scanning OCR pipeline** | Per ROADMAP Phase 3. Mobile camera → Apple Vision/Google ML Kit OCR → LLM parse → `ocr_jobs` status: needs_review → caregiver confirms → medication created. Never auto-creates without human confirmation. **Blocked by:** stable medication catalog (Phase 3 prereq before OCR makes sense). |
| ON-73 | 🧊 Deferred | **Burnout tracker** | Per ROADMAP Phase 4. Weekly check-in: how are YOU doing? Sleep, stress, support. If scores trend bad, surface respite resources. **Blocked by:** roadmap rationale says "needs 2-3 mo of data to show meaningful trends" — defer until families have that history. |
| ON-71c | 🟢 Ready · **P3 / Low** | — | — | **Refill alert email opt-out preference** | Surfaced 2026-05-17 by `/owasp` pre-plan threat model on ON-71 Phase 2 (finding M3, deferred). Coordinators + caregivers have no way to opt out of refill alert emails today; `List-Unsubscribe` header included as defense-in-depth in PR #599 but reply-to-unsubscribe is unmonitored. **Fix:** add a notification-preferences row keyed on `(user_id, kind="refill_alert")`; check before `resend.emails.send` in `refillAlert.ts`; respect List-Unsubscribe inbound (manual handling via Resend webhook if needed, or surface in Settings → Notifications). **Risk:** LOW. **Size:** M (~3h — schema + check in dispatch + Settings UI + tests). |
| ON-71d | 🟢 Ready · **P3 / Low** | — | — | **Refill alert bounce / soft-fail handling** | Surfaced 2026-05-17 by ON-71 Phase 2 threat model (deferred). Resend webhook can notify on bounced sends; today refillAlert leaves the `email_dispatch_log` row with `sent_at IS NOT NULL` even if Resend later bounces. **Fix:** add Resend webhook handler that updates `email_dispatch_log` with bounce status, surface bounced-address count in `/admin` dashboard, file row for hard-bounce auto-disable per address. **Decision gate:** only ship if real bounce volume surfaces — file-and-watch row, not auto-actioned. **Risk:** LOW. **Size:** S (~1.5h once volume justifies). |
| ON-74 | 🟢 Ready | **Full history export (PDF / structured)** | Per ROADMAP Phase 4. Every journal entry, medication log, shift record, symptom reading. Formatted for a doctor or new care facility. Trust feature: families who can export everything are more likely to put everything in. ~3 days. **Pairs with /visit-summary work (UX-20) — share components.** |

### Audit-remediation follow-ups (TD-111) — opened 2026-05-09 PM

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-111 | ✅ Shipped · 2026-05-17 · PR #584 | — | — | **Skill dedup phase 2 — triage divergent project skills** | Resolution: 3 deleted (`dispatch`, `schema-dump`, `tdd-ship` — outdated vs global), 3 renamed (`live-test → live-test-carelog`, `ship-story → ship-story-carelog`, `test-gaps → test-gaps-carelog` — carelog-specifics preserved), 5 kept (`deploy-autopilot`, `expo`, `mobile-ui`, `review`, `sentry-triage` — no global counterpart), 0 promoted. 2 of the 13 row-listed skills (`backlog-dispatch`, `ollama`) were already absent from disk. Triage doc: `docs/project-info/runbooks/skill-dedup-2026-05-17.md`. |
| TD-112 | ✅ Shipped · 2026-05-09 | **`/backlog-sync` skill: stale-Ready detector** | Surfaced 2026-05-09: TD-78..82 sat as `🟢 Ready` for ~weeks despite the 5 referenced "New file: …" paths already existing on main from PRs #248–#258. The current skill walks `git log` for un-shipped commits but never cross-checks that Ready-row file references still represent missing work. Wave 13-B preflight caught it; the skill should have. **Add a step:** for each `🟢 Ready` row, grep the row text for `New file:\s*\`([^`]+)\`` patterns; if the path exists on disk, surface as `STALE_READY` candidate in the report (do not auto-flip — fall through to human or follow-up sync to confirm scope match). Also extend to `apps/web/.../path.ts` mentions inside the row body. Implementation lives at `~/.claude/skills/backlog-sync/SKILL.md` (user-level skill). ~30 min. |
| TD-113 | ✅ Shipped | **Tests for `careEventCommentsRepository.ts` (Tier 1 — PHI-adjacent)** | Comment threads on care events are caregiver narrative. No direct repository test. New file: `apps/web/server/repositories/__tests__/careEventCommentsRepository.test.ts`. Test: (a) cross-org boundary on `listForEvent`, (b) author-id stamping on `add`, (c) `update` rejects callers who didn't author, (d) `softDelete` flips visibility but preserves audit. ~1.5 hr. Surfaced by `docs/audits/2026-05-10-state-and-gaps.md` §3. |
| TD-114 | ✅ Shipped · PR #418 | **Tests for `medicationTaggingRepository.ts` CRUD (Tier 2)** | `medicationTagging.precision.test.ts` covers the matching algorithm only. The repository CRUD path (insert tag, remove tag, list-by-event) has no direct coverage. New file: `apps/web/server/repositories/__tests__/medicationTaggingRepository.test.ts`. ~1 hr. Surfaced by `docs/audits/2026-05-10-state-and-gaps.md` §3. |
| TD-115 | ✅ Shipped | **Tests for `shiftTradeRequestsRepository.ts` (Tier 2 — auth-adjacent state machine)** | Shift-trade flow has multiple state transitions (pending → claimed → approved/declined). RLS fences DB; the helper layer needs tests for who can transition which state. New file: `apps/web/server/repositories/__tests__/shiftTradeRequestsRepository.test.ts`. Cover: (a) coordinator-only approve, (b) claim is open to any team member with recipient access, (c) double-claim race surfaces a clear error. ~1.5 hr. Surfaced by `docs/audits/2026-05-10-state-and-gaps.md` §3. |
| TD-116 | ✅ Shipped | **Tests for `routers/moodEntries.ts` (Tier 1 — PHI write path)** | Mood logging tRPC router. No direct router test. New file: `apps/web/server/routers/__tests__/moodEntries.test.ts`. Cover: (a) auth boundary (`ctx.user = null` → 401), (b) `create` validates org/recipient membership, (c) `list` cross-recipient isolation, (d) timestamp validation rejects future dates. ~1.5 hr. Surfaced by `docs/audits/2026-05-10-state-and-gaps.md` §3. |
| TD-117 | ✅ Shipped · PR #433 + #436 | **Automated PHI-invariant test for analytics call sites** | Discovered 2026-05-10 PostHog usage assessment. The PHI rule (`posthog.identify()` and `posthog.capture()` use UUID only — never email, name, phone, dob, ssn) is currently enforced ONLY by inline `// PHI` comments + reviewer eyeballs. 37 call sites across 20 files; one slip ships a P0 leak. Need automated enforcement: either (a) custom ESLint rule that AST-walks `posthog.identify(...)`, `posthog.capture(...)`, `Sentry.setUser(...)`, `Sentry.setContext(...)` calls and fails the lint gate if any property key matches `/^(email\|name\|phone\|dob\|ssn\|first_name\|last_name\|full_name\|address\|zip\|street\|city)$/i`, OR (b) vitest test that grep-walks the same call sites and asserts properties shape against an explicit allowlist. Approach (a) is preferred — runs on Edit hook + CI lint, catches PRs at write-time not test-time. ADR-0001 codifies the rule; this is its enforcement. |
| TD-118 | ✅ Shipped · PR #442 | **Defense-in-depth: detect misconfigured `SUPABASE_SERVICE_ROLE_KEY` and surface a clearer error than the RLS message** | Discovered 2026-05-10 smoke walk. When `SUPABASE_SERVICE_ROLE_KEY` is missing, expired, or in the wrong format (e.g., `sb_secret_*` production-style key dropped into a local dev env), `supabaseAdmin` silently falls back to the anon role. The first server-side write — typically `POST /api/onboarding/create` — then 500s with `Org creation failed: new row violates row-level security policy for table "organizations"`. This is what users (and devs) see; it points at RLS instead of the real cause (env config). Two-part fix: (1) at server boot, validate the JWT is decodable and its `role` claim is `service_role`; if not, log a CRITICAL warning and refuse to start. (2) when `supabaseAdmin` operations fail with an RLS-class error, wrap in a clearer error: "Supabase admin client is not authenticated as service_role — check SUPABASE_SERVICE_ROLE_KEY env." Pairs with `docs/project-info/runbooks/SECRETS_ROTATION.md` Part 2 ("local Supabase JWT process"). ~1 hr. |
| TD-119 | ✅ Shipped · PR #447 + #448 | **Wire `wrapAdminError()` into existing `supabaseAdmin` write call sites** | Surfaced during TD-118 implementation 2026-05-10. PR #442 introduced `wrapAdminError()` (in `apps/web/server/supabaseAdmin.server.ts`) but only documented it as opt-in — the existing 30+ admin write sites still throw the raw PostgrestError. Sweep `grep -rn "supabaseAdmin" apps/web/server apps/web/app/api` and migrate any site that does `if (error) throw error` to `if (error) throw wrapAdminError(error)`. Routers in scope: `memberships`, `shiftTradeRequests`, `historyExport`, `notifications`, `benefits`, `outerCircle`, `burnout`, plus API route handlers. Pure mechanical refactor — no behavior change for the happy path. ~1.5 hr. |
| TD-120 | ✅ Shipped · PR #522 | **Defense-in-depth refactor of `getCareTeamForRecipient` — session-scoped supabase + RLS gate** | See §7 entry "2026-05-15 — careteam RLS defense-in-depth pair (TD-120/129)". |
| TD-121 | ✅ Shipped · PR #518 | getCareTeamForRecipient: .limit(50) cap + chunked Promise.allSettled (in-flight=8) + console.warn on blips | Three-fix bundle — Opus pre-merge review caught that `.limit(50)` alone doesn't bound concurrency (50 = the rate limit). Chunked-await at 8 holds in-flight under the limit even with concurrent loads. Cache-based mitigation deferred — separate row when needed. |
| TD-122 | ✅ Shipped · PR #511 | **Restructure `makeListChain` mock in membershipsRepository.test to not depend on `.not()` call count** | See §7 entry "2026-05-15 — Adversarial-gate cleanup pair". |
| UX-103b | ✅ Shipped · 2026-05-17 · PR #595 | **Edit affordance for CareTeamList on recipient profile** | Resolution: coordinator-only edit mode on CareTeamList wires existing `memberships.invite`/`memberships.remove` mutations (no router changes). New props: `editable`, `orgId`, `recipientId`, `currentMembershipId`. Two-click confirm on remove; self-remove button hidden client-side; server-side enforcement preserved at `memberships.ts:291` (self) + `:318` (last coordinator). `router.refresh()` on success (recipient profile is a server component). 19/19 vitest green. /oop surfaced TD-178/179/180 follow-ups. |
| UX-104b | ✅ Shipped · PR #539 | Edit affordance for LikesDislikesList on recipient profile | New `recipients` tRPC router with `updatePreferences` mutation (coordinator-gated, follows eolPlan.upsert pattern: assertCoordinator + recipient-in-org check + supabaseAdmin update). Strict zod schema (trim+min+max=120, array cap=50); intentionally asymmetric with the permissive read-side schema. LikesDislikesList split into top-level component returning ReadOnlyView when !canEdit (existing 6 tests pass without tRPC provider) + nested EditableView holding the mutation + `router.refresh()` on save. Profile page fetches caller's membership.role and passes canEdit. /opus-on-opus cleared cycle 2 (1MF/3SF/2N → 0/0/0); cycle-1 MF was MoodVal-style stale-prop drift (resolved via initial-prop snapshot + router.refresh strategy). Branch-slip recovery via update-ref noted in plan. |
| UX-105b | ✅ Shipped · 2026-05-17 · PR #596 | **Edit affordance for EmergencyFooterCard on recipient profile** | Resolution: 3-layer ship. New `identityRepository.updateEmergencyInfo` (read-merge-write of 3 emergency keys via service role; preserves untouched contact_info keys). New `recipients.updateEmergencyInfo` tRPC mutation gated by `assertCoordinator`. EmergencyFooterCard now takes `editable`/`recipientId`/`orgId` props, renders inline edit form with permissive phone regex `/^\+?[\d\s\-()]{7,20}$/`. PHI-hardened: 2 sentinel tests assert no name/phone leak into Sentry on zod-failure or repo-error paths. 49/49 vitest green across 3 test files. /oop surfaced TD-178/179/180 follow-ups (stable error codes, jsonb_set RPC, useEditMode hook). |
| TD-123 | ✅ Shipped · PR #456 | **Stop Vercel preview blocking auto-merge — conditional + required-check demote** | Part (a) shipped via PR #456 (conditional preview — Vercel only deploys when diff touches `apps/web/app/**`, `apps/web/components/**`, or `public/**`). Part (b) was a no-op — branch-protection inspection 2026-05-10 PM-3 confirmed Vercel is NOT in `required_status_checks` (the original BLOCKED state was driven by `needs-phi-review` label, see TD-127 for that protocol). Row was a stale duplicate sitting Ready despite both parts resolved; closed by `/backlog-sync` stale-Ready cleanup 2026-05-15. |
| TD-124 | ✅ Shipped · PR #458 | **Extend `supabase/seed.sql` to create a working coordinator + recipient + memberships row** | Shipped 2026-05-10 PM-4. Three layers of breakage: (1) `supabase/config.toml` had `db.seed.enabled = false`; (2) `ON CONFLICT (email)` on `auth.users` was invalid (no unique constraint on email); (3) `ON CONFLICT (org_id, user_id, recipient_id)` on memberships was rejected because user_id is nullable in the unique index. Rewrote seed end-to-end: identity_vault first then RETURNING token, accepted_at on memberships, SELECT-or-INSERT for auth.users, dropped ON CONFLICT on memberships, EXCEPTION WHEN others THEN RAISE. |
| TD-125 | ✅ Shipped · PR #459 | **Silence Sentry 429s in local dev** | Shipped 2026-05-10 PM-4. Added `enabled: process.env.NODE_ENV === "production" \|\| process.env.SENTRY_FORCE_ENABLED === "true"` to all 3 Sentry config files (client/server/edge). `Sentry.init` still called so instrumentation references (`captureRequestError`, `captureRouterTransitionStart`) wire correctly. |
| TD-126 | 🧊 Deferred · 2026-05-10 PM-4 | **`priority` prop on the marketing landing hero image (LCP fix)** | **Deferred — premise wrong.** Investigation 2026-05-10 PM-4 (chrome-devtools performance trace): `HeroSection` is text-only (no `<Image>`); `hero-4.png` lives in `HowItWorks` below the fold. Adding `priority` to it would actively hurt LCP. Local LCP measured at **132ms** (well under the 2.5s "good" threshold), CLS 0.00, no render-blocking. The original Next.js console warning that triggered this row was a stale dev-time hint that no longer reflects current page structure. If a real LCP regression surfaces in production CrUX data, file a sharpened follow-up against the actual LCP element (likely the `headline-display` h1 + Fraunces variable font preload), not against `hero-4.png`. |
| TD-127 | ✅ Shipped · PR #460 | **Document `needs-phi-review` label removal protocol — or auto-validate** | Shipped 2026-05-10 PM-4. Path (a) chosen — wrote `docs/adr/0003-phi-review-label-protocol.md` codifying two paths: Path A (human review + scope comment + label removal) and Path B (orchestrator self-clear, narrow exception when diff touches none of analytics SDKs / identity_vault / PHI-class migrations). Self-clear forbidden when any subagent on the wave touched files in those categories. Future enhancement (b) AST-walk auto-validate left for follow-up. |
| UX-107 (triage) | ✅ Shipped · 2026-05-10 | **Visit recorder UI status triage** | Decision: schema-ahead-of-UI **by design** per PR #348 ("First slice of the Phase 4 visit recorder. Lands the storage contract so follow-up rows can wire mobile recording, the Inngest Whisper → Claude pipeline, and the web playback/edit surface"). Outcome: kept the migration on main; updated `ON-55` §5 row to reflect the actual shipped/remaining split (data-model done, UI deferred); fixed ROADMAP Phase 4 visit-recorder paragraph (was "TBD" → now documents the schema-ahead-of-UI design + ON-55 link). No new ON-NN row needed; ON-55 already covers the UI surface. Note: this row collided with the shipped sign-in-voice UX-107 (line 129); ID reused unintentionally. |

Source: external design prototype (CareSync Prototype.html) handed off as enhancement spec on 2026-04-23. Triaged into 8 actionable stories; configurability surface (theme switcher, density/radius pickers, grain overlay, multiple hero variants, multiple dashboard layouts) deliberately cut to UX-22 to preserve a single opinionated look. Crisis/SOS scoped separately as UX-23. Real pattern aggregation deferred to UX-24 (UX-18 ships with mocks).

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-14 | ✅ Shipped · PR #125 | **Command palette (⌘K)** | Modal triggered by ⌘K (Cmd+K mac, Ctrl+K elsewhere) from any logged-in screen. Sections: **Jump to** (routes), **Log** (med/mood/meal/BP/note/visit), **People** (ping member), **Admin** (settings, invite). Fuzzy search (simple `includes()` is fine for v1). Esc closes, ↑↓ navigates, Enter submits. New: `apps/web/components/CommandPalette.tsx` + test; hotkey listener mounted in `AppShellClient`. **People section omitted** (no `team.list` route or member-profile page found — revisit when those exist). 18 tests added. |
| UX-15 | ✅ Shipped · PR #126 | **Quick-log FAB** | Floating action button bottom-right of main content (not sidebar). Click expands to options: Meds, Mood, BP, Note, Meal, Hydration. Wired actions navigate to existing journal panels (`/journal/[recipientId]?panel=...`); meal + hydration shipped as **disabled "Coming soon"** (no schema yet — do not invent). Mounted in `AppShellClient` (every `(app)` route). 17 tests added. **Will conflict with UX-14 (#125) on `AppShellClient.tsx` — trivial additive resolve at second-merge.** |
| UX-16 | ✅ Shipped · PR #128 | **Fraunces + Geist type system** | Plumbing-only: added Fraunces + Geist Mono via `next/font/google`; exposed `--font-display`/`--font-body`/`--font-mono` in `@theme inline`; added 3 utility classes (`.headline-display`, `.headline-display em`, `.eyebrow-mono`) in `@layer components`. Italic em pattern is **scoped** to `.headline-display em` — no global `em` rule, existing literal `<em>` usage unchanged. `--font-sans` still resolves to Geist (no body-text regression). 6-assertion smoke test in `lib/__tests__/typography-tokens.test.ts`. **No existing component refactored** — UX-17 + UX-21 adopt the tokens. |
| UX-17 | ✅ Shipped · PR #138 | **Editorial dashboard refactor: BriefHero + MedCard + MoodCard** | Two-col layout (1.6fr/1fr). BriefHero card: blurred primary-subtle blob, mono pill eyebrow ("Today's brief · auto-generated 7:02a"), Fraunces 26 paragraph, status pills row. MedCard: check-style rows, strikethrough+60% opacity when taken, "Log" soft button when not. MoodCard: 13-bar sparkline (today in `--color-primary`, rest in `--color-primary-subtle`), Fraunces 28 mood label. Re-uses existing dashboard data; presentation-only refactor. Depends on UX-16. ~2 days. |
| UX-18 | ✅ Shipped · PR #127 | **Patterns strip in Journal** | Horizontal-scroll row of pastel cards above journal feed surfacing AI insights ("Eleanor more anxious on Tuesdays", "Sleep drops 90m after PT", "Mood highest when Priya visits"). v1 ships scaffold + 3 hardcoded mock patterns + tap-to-detail (`?filter=mood` query param). Real aggregation deferred to UX-24. New: `apps/web/components/journal/PatternsStrip.tsx` + test. Mounted at top of `JournalLayout` journal-destination block. 15 tests added. |
| UX-19 | ✅ Shipped · PR #129 | **Shift Handoff: "What did I miss?" view** | TopBar "What did I miss?" button (mounted in `JournalLayout` — no standalone TopBar exists) opens modal with 5 sections: Meds, Moments, Appointments, Concerns, Thanks. 24h/48h/72h period selector. Pure summary builder in `lib/handoffSummary.ts` with 18 tests; component with 12 tests. Uses existing `careEvents.timeline` tRPC query, client-side window filter. v1 manual trigger; auto-detect on `last_seen` deferred. Schema dump found: `entry_type` enum = journal/medication/shift/appointment/symptom/task/expense/handoff. **Will conflict with UX-18 (#127) on `JournalLayout.tsx` — trivial additive resolve.** |
| UX-20 | ✅ Shipped · PR #130 | **Print-friendly visit summary** | Dashboard "Generate visit summary" button → authenticated `/visit-summary` route (no token; caregiver prints, doesn't share). 6-section printable layout: patient info (PHI from `identity_vault` per P4-03 pattern), meds + adherence %, vitals SVG sparklines, symptoms, journal highlights, blank questions textarea. Uses `window.print()` — no `@react-pdf/renderer` needed. `lib/medAdherence.ts` pure helper with 14 tests; component with 18 tests. **Will conflict with UX-19 button placement if we add a Visit Summary button to TopBar later — but currently mounted on Dashboard, so no overlap with #129.** |

### Launch readiness (LAUNCH-*) — Phase 6 · opened 2026-04-27

| ID | Status | Story | Notes |
|---|---|---|---|
| LAUNCH-001 | 🧑 Needs human | **App Store launch — TestFlight QA + App Store Connect listing** | Run internal TestFlight cycle (≥1 week, ≥3 real-device testers). Complete App Store Connect listing: description, keywords, screenshots (iPhone 6.7″ + 5.5″), app preview video optional. iOS privacy nutrition label. Android Play Console parity (listing + privacy). Human-gated: EAS production build must be complete first. |
| LAUNCH-002 | ✅ Shipped · PR #225 | **EAS production build profile + OTA gating** | Finalized `eas.json` production profile + channel pinning + `runtimeVersion` policy; release runbook at `docs/project-info/runbooks/MOBILE_RELEASE.md`. |
| LAUNCH-003 | ✅ Shipped · PR #226 | **Web go-live SEO/OG meta + sitemap + structured data** | Added `<meta og/twitter>` to all marketing pages, `sitemap.ts`/`robots.ts`, Organization + SoftwareApplication JSON-LD on landing. |
| LAUNCH-004 | ✅ Shipped · runbook | **Observability hardening** | All four sub-items closed: TD-73 rate-limit dashboard (PR #227), TD-74 digest delivery monitor (#227), TD-75 E2E green-streak gate (#227), Sentry SDK + source-maps plugin wired (PR #309 + earlier). 319-line runbook at `docs/project-info/runbooks/OBSERVABILITY.md`. The remaining `SENTRY_AUTH_TOKEN` env var is human-gated and tracked separately as TD-03. |
| LAUNCH-005 | 🧑 Needs human | **Compliance / legal — privacy policy, ToS, BAA, data retention** | Publish privacy policy + ToS at stable URLs (linked from signup + footer). Obtain BAA from Supabase (HIPAA) and Resend if processing PHI in email bodies. Document data-retention and deletion runbook (how to honor right-to-erasure requests). Human-gated: legal review required. |

Rules: mark `✅` when done; list `**Blocked by:**` if a prerequisite is still open; one story per `###`; stay under ~4 hrs of work.

All items below are independent (no shared-state conflicts) — agents may fan out in parallel.

### ON-15 — Mobile: accessibility audit (iOS Dynamic Type + VoiceOver)
**Status:** ✅ Shipped (code complete; physical device VoiceOver verification deferred to human)

**Why:** Mobile uses fixed `fontSize` throughout; never tested against 200% Dynamic Type or VoiceOver navigation order.
**Work:** Run app under max Larger Accessibility Sizes on journal/medications/schedule; migrate fixed sizes to `PixelRatio.getFontScale()` capped at 1.5×. VoiceOver-complete a medication-log flow end-to-end. File follow-up ON-XX for issues deferred.
**AC:** app usable at 200% DT on 3 key screens; VoiceOver finishes the med-log flow.
**Size:** ~1 day. **Blocked by:** nothing.

---

### OOP wave A adversarial-gate follow-ups (TD-129/130) — opened 2026-05-14

Source: Sonnet adversarial gates flagged two must-fixes on the OOP wave A PRs that did not land before `gh pr merge --auto --squash` fired. Both are narrow, single-file fixes with verifiable acceptance.

| ID | Status | Story | Notes |
|---|---|---|---|
| TD-129 | ✅ Shipped · PR #523 | **OOP-001 pgTAP — role-grant test discipline + REVOKE `accept_invite` from anon/authenticated** | See §7 entry "2026-05-15 — careteam RLS defense-in-depth pair (TD-120/129)". |
| TD-130 | ✅ Shipped · PR #510 | **PDF export — restore `typeof string` narrowing on `payload.text`** | See §7 entry "2026-05-15 — Adversarial-gate cleanup pair". |

---

### OOP refactor (OOP-*) — opened 2026-05-14

Source: `/oop` Phase 1 + Phase 2 audit. Plan doc: `docs/plans/2026-05-14-oop-refactor.md`. Fragments in `.claude/state/oop-phase{1,2}-{web,mobile,supabase,cross}.md`. **Invariant-preserving** — no external behavior changes. Suggested sprint sequencing in the plan doc §"Recommended sprint order".

| ID | Status | Story | Notes |
|---|---|---|---|
| OOP-001 | ✅ Shipped · PR #469 | **pgTAP for `invite_tokens` atomicity + expiry** | Promoted to §7 2026-05-14. |
| OOP-002 | ✅ Shipped · PR #468 | **Enable RLS + pgTAP on `journal_reactions`** | Promoted to §7 2026-05-14. |
| OOP-003 | 🧊 Struck (2026-05-14) | **pgTAP for messaging triad — `message_threads`, `message_thread_members`, `messages`** | Verified 2026-05-14 re-audit: `supabase/tests/messaging_rls.test.sql` already exists on main with `select plan(12)` and 13 assertion lines. Coverage is not zero. Original premise false; strike the row. |
| OOP-004 | ✅ Shipped · PR #475 | **OCR job state machine + row-level lock** | Promoted to §7 2026-05-14. |
| OOP-005 | ✅ Shipped · PR #470 | **`CareEvent` payload discriminated union + type guards** | Promoted to §7 2026-05-14. Follow-up: TD-130. |
| OOP-006 | ✅ Shipped · PR #476 | **Brief headline classifier → strategy chain** | Promoted to §7 2026-05-14. |
| OOP-007 | 🧊 Struck (2026-05-14) | **RLS helper `is_org_coordinator(uuid)` to dedupe ~23 inline checks** | Verified 2026-05-14 re-audit: `grep -rEc "EXISTS\(\s*SELECT.*FROM\s+(public\.)?memberships" supabase/migrations/` returns **0**. Helper `user_is_org_coordinator()` already defined at `supabase/migrations/20260327234330_core_schema.sql:344` and consumed by policies in core_schema.sql + shifts_schema_align.sql. Original premise false; strike the row. |
| OOP-008 | ✅ Shipped · PR #474 | **`care_briefs.recipient_id` immutability trigger** | Promoted to §7 2026-05-14 (narrowed per re-audit; `visit_recordings` already had its trigger). |
| OOP-009 | ✅ Shipped · PR #481 | **Mobile `Offline Queue` mutation routing via `PayloadMutator`** | Promoted to §7 2026-05-14. |
| OOP-010 | ✅ Shipped · PR #479 | **Mobile `SecureStore` key registry** | Promoted to §7 2026-05-14. |
| OOP-011 | ✅ Shipped · PR #480 | **Mobile tRPC `useMutationWithRefresh` composable** | Promoted to §7 2026-05-14. 6 of 7 named screens refactored; `schedule/index.tsx` honestly skipped as fully-divergent. |
| OOP-012 | ✅ Shipped · PR #478 | **`HandoffSummary` name hydration via `NameResolver` injection** | Promoted to §7 2026-05-14. |
| OOP-013 | ✅ Shipped · PR #482 | **Mobile push notification `NotificationRouter` registry** | Promoted to §7 2026-05-14. |
| OOP-014 | ✅ Shipped · PR #483 | **Mobile `useSyncStatus` singleton + AppState-aware polling** | Promoted to §7 2026-05-14. |
| OOP-015 | ✅ Shipped · PR #477 (PR1 only) | **Stripe webhook switch → handler-per-event map** | Promoted to §7 2026-05-14. PR1 (handler-map refactor) merged; PR2 (Sentry routing swap, behavior-changing) deferred — captured as a TD-* candidate during /closeout. |

---

## 3. Platform parity (PP-*)

Full table + stories: `docs/project-info/product/PLATFORM_PARITY.md`. Active items are listed in §1 above. Remaining:

| ID | Priority | Story | Status |
|---|---|---|---|
| PP-002 | P2 | Mobile: onboarding wizard (first-run flow) | ✅ Shipped · PR #92 |
| PP-003 | P2 | Mobile: read-only subscription view + "manage on web" CTA | ✅ Shipped · PR #93 |
| PP-005 | P2 | Web: push notifications (browser Push API) | ✅ Shipped · PR #85 |
| PP-006 | P1 | Android prebuild + boot verification | ✅ Shipped · PR #90 |
| PP-007 | P1 | Android: push notification verification (FCM token + deep-link tap) | ✅ Shipped · PR #99 — needs `google-services.json` from Firebase for live FCM verification |
| PP-008 | P1 | Android: app-links verification (`assetlinks.json`, autoVerify) | 🧑 Needs human — `assetlinks.json` on live domain + EAS SHA-256 |
| PP-009 | P2 | Android: visual QA pass (screenshot every screen vs iOS) | 🟢 Ready — `scripts/android-visual-qa.sh` written; run when Android emulator available |
| PP-010 | P2 | Android: document-share intent verification | ✅ Shipped · 2026-04-17 — 17 unit tests cover Android `Alert.alert` picker path; fixed stale empty-state assertion |
| PP-011 | P2 | Offline behavior spec + write-queue for journal entries | ✅ Shipped · PR #88 |
| PP-012 | P3 | Consolidate URL scheme (`yourcarelog://` ↔ brand `carelog`) | ⏳ |
| PP-013 | 🧊 P3 | Wear OS companion | Parked for v2 |

---

## 4. Accessibility (A11Y-*)

Full plan + scoring: `docs/project-info/technology/ACCESSIBILITY.md`. Active in §1. Remaining:

| ID | Priority | Story |
|---|---|---|
| A11Y-011 | ✅ Shipped · PR #119 | **Web button aria-label sweep** | All 4 targets already WCAG 2.2 AA compliant — no code changes needed. |
| A11Y-012 | ✅ Shipped · PR #224 | **Flag button: add `type="button"` + contextual `aria-label`** | JournalTimeline flag button now has `type="button"` and aria-label including entry timestamp. |
| A11Y-013 | ✅ Shipped · PR #224 | **TeamAdmin "Remove" button: `type="button"` + member-identifying `aria-label`** | Remove button now has `type="button"` and `aria-label={`Remove ${member.display_name ?? member.email}`}`. |
| A11Y-014 | ✅ Shipped · PR #224 | **TeamAdmin delete-org button: add `type="button"`** | Delete org button now has `type="button"`. |
| A11Y-015 | ✅ Shipped · PR #228 | **AppTabBar tab buttons: add `type="button"`** | Desktop tab list shipped in #224 sweep; mobile tab strip closed in #228 (was the only remaining gap from the audit). |
| A11Y-016 | ✅ Shipped · PR #224 | **QuickLogFab action buttons: add `type="button"`** | Both FAB trigger + menu-item buttons now have `type="button"`. |
| A11Y-017 | ✅ Shipped · PR #224 | **JournalLayout: replace `user.email` with `display_name` in sticky header** | Sticky header now reads `user.user_metadata.display_name ?? user.email`. |
| A11Y-018 | 🧑 Needs human | **Physical-device VoiceOver verification (residual from TD-02)** | Code-complete scaledFont + accessibilityLabel sweep shipped in TD-02 (PR #87). Physical-device VoiceOver end-to-end of the medication-log flow has not been verified — requires a real iPhone. See TD-02 in §7 for context. |

---

## 5. Large features (multi-day)

### ON-54 — Free tier definition + soft gates · ~1 day
**Status:** 🧑 Needs product decision before coding
**Why:** PRODUCT_STRATEGY.md lists "free tier: limited (TBD)." BillingBanner soft-gate pattern is already wired. Needs a product decision on the limits (e.g. max 30 events/month, no history export, no document vault, 1 care recipient) before enforcement code can be written.
**Work once limits are decided:** BillingBanner gates on event creation, history export, and vault upload. No paywall — upgrade prompt only.
**Blocked by:** 🧑 Brady decides free-tier limits.

### ON-55 — Visit recorder · ~3 days
**Status:** 🧊 Deferred (UI surface; data-model first slice shipped 2026-05-01 in PR #348)
**Why:** Audio note at a doctor visit → Whisper transcription → Claude structured extraction → `care_event` tagged to the appointment. Roadmap classifies this under Phase 4 (depth + retention).
**Shipped already (PR #348):** `visit_recordings` table + `visit_recording_status` enum + `'visit_note'` entry_type + RLS via `user_can_access_recipient()` + pgTAP regression net.
**Work remaining:** **(a) server** — tRPC router (`visitRecordings.create/list/confirm`); **(b) pipeline** — Inngest function (Whisper → Claude structured extraction → `care_event` insert); **(c) mobile** — `expo-av` recording + Supabase Storage upload; **(d) web** — playback + structured-fields review screen.
**Blocked by:** Phase 1–6 features fully stable; sufficient data volume to validate the use case (per #348's deferral criteria).

### ON-69 — Visit recorder (struck 2026-05-09 — duplicate of ON-55)
**Status:** 🧊 Deferred · duplicate of ON-55
**Note:** ON-55 covers the same Visit Recorder Phase 7 scope. Keeping ON-55 (lower ID).

### ON-56 — Data stewardship commitment page · ~0.5 day
**Status:** ✅ Shipped · PR #102
**Why:** PRODUCT_STRATEGY.md says "publish before first paying users." Builds trust with a population burned by CareZone's shutdown. Commitment: 12 months notice before shutdown, full data export always available, data never sold, no ads ever.
**Work:** New marketing page at `/trust`. Link from footer + signup flow. Plain language, no legalese. ~0.5 day.
**AC:** Page live at stable URL; linked from site footer and onboarding.

### ON-59 — Inngest cron health monitoring · ~1 day
**Status:** ✅ Shipped · PR #110 (Sentry) + PR #111 (cron_runs timestamps)
**Why:** Weekly digest, refill alerts, burnout check-ins, and gap detector run as Inngest functions. If they fail silently, families miss digest emails and medication refill warnings — core retention and safety features. No monitoring exists today.
**Work:** Wrap each Inngest `serve()` handler catch block with `Sentry.captureException`. Add an Inngest event-stream webhook that fires to a `/api/inngest/monitor` route and logs failures to Sentry. Optionally add a `/api/health/crons` endpoint that returns last-run timestamp for each cron.
**AC:** Sentry receives an exception when any Inngest function throws. Oncall can see last-run timestamps for digest + refill + burnout + gap-detector.

### ON-60 — Referrer resource page `/for-referrers` · ~1 day
**Status:** ✅ Shipped · PR #107
**Why:** PRODUCT_STRATEGY.md identifies social workers, hospital discharge planners, elder law attorneys, and geriatric care managers as the highest-leverage GTM channel — one referrer who sends 2 families/month is worth more than 1,000 social media followers. There is currently no page targeting this audience.
**Work:** New marketing page at `/for-referrers`. Explains: what Carelog does, how to refer a family (share link), what families get. Includes a downloadable 1-page reference card (PDF). No commission language (conflicts with social worker ethics). ~1 day.
**AC:** Page live; includes share link + downloadable PDF; linked from main nav footer.

---

### ON-79 — Task CRUD + web UI (instructions + clickable checklist + pin-to-shift) · ~2 days
**Status:** 🔴 Blocked by ON-77
**Why:** Phase 7 Task epic (ROADMAP §Phase 7). The web surface for the task loop: create / list / assign / complete tasks, render free-text instructions and a clickable checklist, and pin a task to a shift (current or future). Role-gated completion per ON-77's permission model.
**Work:** **(a) server** — `tasks` tRPC router (`create`, `list` by recipient/shift/assignee, `update`, `complete`, `assign`), each gated by the ON-77 completion-permission predicate; PHI rule applies (no recipient PII into analytics). **(b) web UI** — task list + detail panel following `.claude/rules/ui-standards.md` (tinted CardHeader, click-to-open create form, clickable checklist toggles persisting to `checklist` jsonb), a shift picker (current + upcoming shifts) for pinning, and role-aware action gating (hide Complete for roles without the capability). **(c)** tests — router unit tests + component tests.
**AC:** Coordinator can create a task with instructions + checklist and pin it to a shift; an authorized caregiver can check off items and mark it done; an unauthorized role sees no Complete affordance and the server rejects the call. Vitest + typecheck green.
**Blocked by:** ON-77 (data model + permission predicate).

### ON-80 — Mobile task flow (create + on-call receive + complete) · ~2 days
**Status:** 🔴 Blocked by ON-77, ON-79
**Why:** Phase 7 Task epic. The driving flow is mobile-first: a coordinator raises a task from her phone; an on-call caregiver receives it, completes it in the field, and updates the app. Mirrors ON-79's capabilities on Expo/React Native.
**Work:** Reuse the shared tRPC `tasks` router from ON-79 (no new server work). Expo screens: task list (filtered to my assigned/on-call tasks), task detail with checklist toggles, create-task form, and a complete action. Respect the same role-gating. Follow `apps/mobile/CLAUDE.md` conventions.
**AC:** From the mobile app, an authorized user can create a task pinned to a shift and an assignee can complete it; state round-trips with the web surface. Mobile test suite green.
**Blocked by:** ON-77 (model), ON-79 (shared router + UX parity reference).

### ON-81 — Configurable task notifications (email + in-app; on-call routing) · ~2 days
**Status:** 🔴 Blocked by ON-77, ON-78
**Why:** Phase 7 Task epic. Close the loop: notify on task lifecycle events (created / assigned / completed), with per-org/per-task configurability. On-call routing — when a task request arrives during an on-call shift's window, notify that shift's `assigned_to`. The requester is notified on completion.
**Work:** Reuse the live Resend + `email_dispatch_log` (idempotency) + Inngest infra (no new external dependency for v1). **(a)** notification-preference config (which events notify whom; default sensible). **(b)** dispatch on task create/assign/complete; on-call resolution = find the `on_call` shift covering `now` for the recipient and target its assignee. **(c)** an in-app notification surface (reuse existing in-app notification pattern if present, else a lightweight unread list). **(d)** PHI rule — task titles/instructions may contain PHI; email bodies must follow the same plain-text/no-PII-to-analytics discipline as weekly digest + refill alerts; sentinel test. Push notifications are explicitly out of scope here (layer in once mobile launches — separate row).
**AC:** Creating/assigning/completing a task fires the configured notifications exactly once (idempotency-logged); a task raised during an on-call window notifies the on-call caregiver; the requester gets a completion notification. PHI sentinel test passes.
**Blocked by:** ON-77 (task lifecycle events), ON-78 (on-call shift type for routing).

### ON-82 — Future-shift pinning + scheduled / recurring tasks · ~2 days
**Status:** 🔴 Blocked by ON-77, ON-79, ON-81
**Why:** Phase 7 Task epic. Tasks are known in advance (appointments, medication pickups, a granddaughter's soccer game) and must pin to *future* shifts and surface/remind ahead of time. The ON-77 `shift_id` FK already permits pinning to any shift; this row adds the future-dated UX, the surfacing-when-the-shift-arrives behavior, recurrence, and ahead-of-time reminders.
**Work:** **(a)** create-task UX for picking a future shift (or a date that resolves to a covering shift). **(b)** recurrence model for repeating tasks (reuse the `shifts.recurrence` jsonb pattern for consistency). **(c)** reminder dispatch via Inngest ahead of `due_at` (configurable lead time), reusing ON-81's notification plumbing. **(d)** the upcoming-tasks surface on the dashboard/shift view.
**AC:** A task pinned to a shift two weeks out surfaces on that shift when it arrives and fires a reminder at the configured lead time; a recurring task regenerates per its rule. Tests green.
**Blocked by:** ON-77 (FK + due_at), ON-79 (create UX), ON-81 (reminder dispatch plumbing).

### ON-83 — Recipient-initiated task requests · ~1–2 days
**Status:** 🔴 Blocked by ON-77, ON-79
**Why:** Phase 7 Task epic. The flow includes the care recipient ("dad") raising a request directly — but recipients are not necessarily app users today. This row builds the recipient-initiated entry path and wires `requested_on_behalf_of` (from ON-77).
**Work:** **Open design decision (resolve in plan):** (a) a lightweight authenticated recipient interface (smallest scoped surface to raise a request), vs. (b) a proxy "logged on behalf of recipient" path where a coordinator records a recipient-originated request and the `requested_on_behalf_of` field attributes it, vs. (c) a tokenized request link the recipient can use without a full account. Whichever path, the resulting task routes to the on-call caregiver via ON-81. Respect PHI + RLS scoping.
**AC:** A recipient-originated request becomes a task attributed to the recipient (`requested_on_behalf_of` set) and routes/notifies per the on-call rules. RLS prevents a recipient from seeing other recipients' data. Tests green.
**Blocked by:** ON-77 (`requested_on_behalf_of`), ON-79 (task creation surface).

### ON-84 — Task completion verification layer (photo / confirmation) · ~1–2 days
**Status:** 🧊 Deferred (ship the core task loop first, then validate before adding proof-of-completion)
**Why:** Phase 7 Task epic. Optional proof-of-completion attached to a task when it's marked done — a photo or confirmation note, à la Lime scooter / DoorDash drop-off. The verification contents ride along in the completion notification to the requester. Configurable per task (some tasks need proof; most don't).
**Work (when un-deferred):** `task_verifications` (or a `verification` jsonb + Supabase Storage object refs on `tasks`); a per-task "requires verification" flag; mobile/web capture-on-complete (reuse the `expo-av`/Storage upload pattern from the visit recorder, ON-55); include the artifact reference in ON-81's completion notification. PHI: stored media may contain PHI — Storage bucket RLS + signed URLs only.
**AC (when un-deferred):** A task flagged "requires verification" cannot be completed without an attached photo/note; the requester's completion notification surfaces the verification contents; media is access-controlled. Tests green.
**Blocked by:** ON-79 (completion flow) — un-defer after the core loop ships.

---

## 6. Deferred UI polish (UX-*) — intentionally parked

From `BACKLOG_UI_REDESIGN.md`. Ordered by impact.

### Shipped
- **UX-12** — ✅ Shipped · PR #120 — Empty states for AIChatThread + EntryDetailClient; MessageCircle + FileX icons, token-based styling.
- **UX-13** — ✅ Shipped · PR #117 — AIPanel: submit button disabled + Loader2 spinner while pending; onError shows sonner toast.

### Deferred
- **UX-08** — Storybook component library (post-launch, when component count warrants).
- **UX-09** — Visual regression testing (Percy/Chromatic or Playwright screenshot diffs).
- **UX-11** — Onboarding flow redesign — low traffic, functional as-is.
- **UX-22** — Configurability surface from 2026-04-23 design spec: theme switcher (sage/slate/rose), density picker (compact/comfortable/airy), radius picker (sharp/soft/pillowy), grain overlay, 5 hero variants, 3 dashboard layouts. Cut deliberately to preserve a single opinionated "editorial, calm, dignified" look. Revisit only if A/B data shows real demand.
- **UX-23** — Crisis/SOS mode. Subtle red SOS button → emergency contacts (911, primary doctor, family) + current meds + allergies + DNR/advance directive + one-tap "notify care circle." Parked separately from UX-14..21 batch — needs scoping with legal, accuracy guarantees on meds list, contact failover, and audit-log behavior before any code lands.
- **UX-24** — Real pattern aggregation for Journal Patterns strip. Replaces UX-18 hardcoded mocks with actual SQL/AI aggregation over `care_events` (mood-by-day-of-week, sleep-vs-event correlation, mood-vs-visitor correlation). Schedule after UX-18 ships and we have real production usage to validate the patterns are useful.

### Token-drift cleanups from UI review 2026-04-27 (UX-025..036)

11 of 12 shipped via PR #224 polish sweep (verified 2026-04-27 against current main). UX-035 still pending — BriefHero hardcoded mock content and `TODO(UX-24+)` comment confirmed present.

| ID | Status | Story | Notes |
|---|---|---|---|
| UX-025 | ✅ Shipped · PR #224 | **Extract shared `MOOD_STYLES` constant using `var(--color-mood-*)` tokens** | `apps/web/lib/mood.ts` created; consumed by EntryDetailClient + JournalEntryForm + SymptomPanel. |
| UX-026 | ✅ Shipped · PR #224 | **Add `--color-danger-subtle` token + replace `bg-red-50` in DangerZone and TeamAdmin** | Token added to `globals.css`; DangerZone + TeamAdmin updated. |
| UX-027 | ✅ Shipped · PR #224 | **QuickLogFab "Soon" badge: replace `bg-gray-100` with token** | Now uses `bg-[var(--color-surface)] border border-[var(--color-border)]`. |
| UX-028 | ✅ Shipped · PR #224 | **`RoleBadge`: replace raw amber/gray Tailwind with tokens** | RoleBadge tokenized; tests updated. |
| UX-029 | ✅ Shipped · PR #224 | **`ExpensePanel` and `DocumentVault` category badges: token-backed color map** | Both panels now use shared token map. |
| UX-030 | ✅ Shipped · PR #224 | **`MedicationPanel` PRN badge: use `--color-secondary-subtle`** | PRN badge at MedicationPanel.tsx:321 now uses secondary-subtle tokens. |
| UX-031 | ✅ Shipped · PR #224 | **Add `--color-success-subtle` + `--color-warning-subtle` tokens for BriefHero pills** | Both tokens added to `globals.css`; BriefHero references them. |
| UX-032 | ✅ Shipped · PR #224 | **`EolPlanner`: replace `border-red-50` + `text-red-600` with danger tokens** | EolPlanner now uses `border-[var(--color-danger)]/30` and `text-[var(--color-danger)]`. |
| UX-033 | ✅ Shipped · PR #224 | **`ErrorBoundary`: replace raw gray classes with design tokens** | ErrorBoundary now uses surface/ink/muted tokens. |
| UX-034 | ✅ Shipped · PR #224 | **DashboardClient: replace inline SVG chevron with `lucide-react` `ChevronRight`** | DashboardClient.tsx:332 now uses `<ChevronRight />`. |
| UX-035 | 🧊 Struck (2026-05-09) | **Gate `BriefHero` mock content behind feature flag or skeleton** | Verified 2026-05-09: `apps/web/components/dashboard/BriefHero.tsx` is now wired to `trpc.briefs.dashboardSummary` and `trpc.briefs.latestForRecipient` — no hardcoded mock content, no `TODO(UX-24+)` comment remains. Wave 9 (`BriefSection` mount + adapters) closed this gap. Strike the row. |
| UX-036 | ✅ Shipped · PR #224 | **Move `CommentItem`/`CommentThread` dark-mode overrides to `globals.css` tokens** | No `dark:bg-gray-*` matches remain in CommentItem/CommentThread. |
| UX-041 | ✅ Shipped · PR #507 | **Journal cards: surface author identity** | See §7 entry "2026-05-15 — Journal & meds quick-win trio". |
| UX-042 | ✅ Shipped · PR #505 | **Journal top bar: show recipient name not org name** | See §7 entry "2026-05-15 — Journal & meds quick-win trio". |
| UX-043 | ✅ Shipped · PR #506 | **`MedicationChecklist`: format scheduled times + design tokens** | See §7 entry "2026-05-15 — Journal & meds quick-win trio". |
| UX-044 | ✅ Shipped · PR #516 | Leave-org placebo button + modal removed; honest copy points to coordinator/support | Removal path chosen after 2 Opus review cycles surfaced that `/team/admin` redirects non-coordinators and doesn't let coordinators self-remove — no link option would have worked. Real leave-org flow is now its own backlog candidate when ready. |
| UX-046 | 🟡 Spike | **Clinician-readable share surface — does PRODUCT.md's promise have a route?** | `/impeccable critique` 2026-04-29 surfaced a category gap: PRODUCT.md says "a doctor's appointment starts with a real summary." `/care/[shareToken]` is the outer-circle volunteer-claim form (meals, errands), not a clinician share. `/visit-summary` is gated behind authentication and produces a print-only artifact the caregiver must physically deliver. The "real summary for the doctor" promise has no shipped surface. Spike: investigate whether (a) `visit-summary` should also produce a time-limited token URL the caregiver emails to the doctor, (b) a new `/clinician/[shareToken]` route is warranted, or (c) the existing `/brief/[shareToken]` flow already serves the use case if briefs default to clinician-friendly content. Founder/PM decision before any code. |
| UX-048 | ✅ Shipped · PR #525 | **Empty states polish** | See §7 entry "2026-05-15 — UX-048 empty-state copy polish — PR #525". |
| UX-049 | ✅ Shipped · PR #528 | **Auth + onboarding voice pass** | See §7 entry "2026-05-15 — copy-audit-trio finish (UX-049/050)". |
| UX-050 | ✅ Shipped · PR #529 | **Journal mood + entry-form copy** | See §7 entry "2026-05-15 — copy-audit-trio finish (UX-049/050)". |
| TD-143 | ✅ Shipped · PR #535 | Mood-label map dedup: MOOD_LABELS + MOOD_KEYS extracted to lib/mood.ts | Path (a) chosen. Canonical `MOOD_LABELS: Record<Mood, string>` + `MOOD_KEYS = [...] as const satisfies readonly Mood[]` tuple in `apps/web/lib/mood.ts`. Five UI surfaces (RecipientProfile, SymptomPanel, JournalEntryForm, MoodSpectrum, PromptedComposer) dedup'd; `server/routers/moodEntries.ts` keeps the digest-tone `okay: "Settled"` via explicit spread (`{ ...MOOD_LABELS, okay: "Settled" }`) with comment naming the rationale (vs prior stealth divergence). Array sites derive via `MOOD_KEYS.map(...)`. /opus-on-opus cleared cycle 2 (1MF/3SF/2N → 0/1/2): cycle-1 must-fix was MoodVal=Mood|"" type narrowing (resolved). /oop --from-sprint clean (0/1/2). |
| UX-051 | 🧊 Deferred (2026-05-09) | **Legacy `care-log.org` → `caresync.app` cleanup** | Deferred 2026-05-09: `hello@caresync.app` and `privacy@caresync.app` are not yet provisioned (DNS + inbox). Replacing displayed addresses now would ship broken support links to paying users. Re-visit once DNS lands. Audit findings preserved: `apps/web/app/(app)/subscriptions/page.tsx:188`, `team/admin/TeamAdminClient.tsx:159`, plus marketing surfaces (privacy/terms/contact pages). Resend `from:` in `app/api/contact/route.ts` is also affected — separate Resend domain-verification step. |

---

## 7. Shipped (compact log)

### 2026-05-20 — TD-198/199/200/201 parallel-4-ci-velocity sprint close — PRs #649 / #648 / #650 / #647
First 4-track `/parallel-sprint-plan` run, executed across s2/s3/s4/s5 (one worktree per track, file-disjoint config edits). All four merged within ~1h.
- **TD-198 (PR #649, s2, Track A):** `ci.yml` — `web-tests` split into `[web, node, a11y]` matrix (`fail-fast: false`, Chromium gated to the `web` browser leg); `rls-tests` `if:` extended with `|| deps == 'true'` so workflow/lockfile edits re-run pgTAP. `ci-summary` left unchanged (already skipped-as-pass + matrix-aggregating). Scoped opus review (1 round) verified browser-project identity, matrix aggregation, `--project=` syntax.
- **TD-199 (PR #648, s3, Track B):** `turbo.json` + root `package.json` — `type-check`/`lint` tasks gained `inputs` + `outputs: []` for cache hits; `type-check` `dependsOn: ["^build"]` removed (wasteful, blocked FULL TURBO).
- **TD-200 (PR #650, s4, Track C):** `apps/web/vitest.config.ts` — `pool: "threads"` on the node project only; a11y left on default (jsdom + threads is fragile), browser `fileParallelism: false` untouched.
- **TD-201 (PR #647, s5, Track D):** `playwright.config.ts` — `fullyParallel: true` + `workers: process.env.CI ? 2 : undefined`.
- **Combined effect:** web-unit-test wall time drops from serial-over-3-projects to max-of-3 parallel jobs; turbo caches type-check/lint; node vitest sheds per-file fork overhead; Playwright parallelism made explicit.
- **Required-check regression caught + fixed during close:** TD-198's matrix rename (`Web — unit tests` → `Web — web/node/a11y`) orphaned the branch-protection required contexts on skip — a skipped matrix job emits one check named `Web — ${{ matrix.project }}`, so the three required contexts never report and every docs-only/backend-only PR sat permanently `BLOCKED` (Vercel rate-limit was a red herring). Fixed by re-pointing branch protection at the always-running `CI Summary` aggregate (which treats skipped-as-pass) instead of the path-gated granular `Web —*` / `RLS pgTAP tests` / `Mobile —*` contexts. Required set now: Lint, Typecheck, the 4 security scanners, CI Summary.
- **Process notes:** (1) Each track's claim + outcome.json lived only in its own worktree's `.claude/state/` copy, never in the main checkout — close cleanup was worktree-level (no main claims.json/INDEX.md rows to remove). Third parallel sprint in a row to hit this state-fragmentation gap. (2) Pre-commit `--no-verify` on the config-only diffs per documented pnpm+worktree gotcha. (3) This close PR rebased onto the Phase 7 epic seed (#651) — a `git checkout --theirs` mis-resolution briefly clobbered the epic rows; caught and rebuilt from origin/main before push.

### 2026-05-20 — TD-195 + TD-197 parallel-2-ci-velocity sprint close — PRs #641 / #642
Second `/parallel-sprint-plan` run, executed across s2 (Track A) + s3 (Track B) with `MAX_SESSIONS=3` (s1 still parked on TD-189). Both PRs merged within 4 min of each other (#642 00:37Z, #641 00:41Z).
- **TD-195 (PR #641, s2):** new `e2e-required` paths-filter bucket added to `.github/workflows/ci.yml` (`changes.outputs` + `dorny/paths-filter@v3` filter); re-gated E2E matrix job and `e2e-string-drift` job on `e2e-required || deps || e2e`, dropping the broad `web` reference. `web-tests` job's `web ==` gate intentionally preserved (unit tests still run on broad web changes). New `docs/runbooks/ci-gating.md` documents the allowlist + maintenance protocol. Positive-patterns-only — paths-filter v3 negation is brittle; accepted false-positives are `__tests__/` subdirs under allowlisted paths. Backstops: nightly E2E on main (TD-87), merge_queue + push-to-main run E2E unconditionally.
- **TD-197 (PR #642, s3):** `apps/web/server/routers/__tests__/schema-snapshot.test.ts` + `__snapshots__/api-schemas.snap.json` (checked-in JSON baseline) + `test-helpers/schema-walker.ts` (pure walker, unit-testable). Walks `appRouter`, extracts each procedure's input + output Zod schemas via `zod-to-json-schema`, snapshots one entry per `<router>.<procedure>` keyed by procedure type (query/mutation/subscription). Drift triggers test failure with field-path + API_VERSION bump hint. New `apps/web/server/api-version.ts` exports `API_VERSION = "1.0.0"`. ADR-0006 documents the convention + known-lossy patterns (`z.record(z.unknown())`, `z.lazy`, `z.brand`). Pairs with TD-195 — when conditional E2E skips, this snapshot test still runs.
- **/opus-on-opus:** 3 plan-level cycles + 2 Track-A-scoped cycles (Track A); final must-fix=0.
- **Process lessons:** (1) Plan file initially uncommitted in main worktree; s3 included it in PR #642 but as a rewritten Track-B-only re-seed (TD-195 framing dropped, despite Track A being live). Plan should be committed by `/parallel-sprint-plan` itself, not deferred to executors. (2) s3 didn't write `outcome.json` before the session closed; `/parallel-sprint-close` had to synthesize Track B's outcome from PR metadata. Same skill ergonomics gap as the prior parallel-sprint close — operator-closed-externally pattern needs harness handling. (3) Track-executor cwd discipline: orchestrator's main shell cwd persisted after `cd worktrees/...`; first Edit attempts wrote into main worktree, not s2's. Required mid-flight relocation of ci.yml + ci-gating.md to the correct worktree. (4) Pre-commit `--no-verify` on s2 justified by documented pnpm+worktree gotcha (diff was YAML+MD only). §0 Ready: 19 → 17.

### 2026-05-18 — TD-170b/c E2E gate restored after 3-day chronic-red — PRs #605 / #606 / #607 / #608
Second sprint of 2026-05-18. `/sprint td-170b-e2e-investigation` (investigation only, doc-only deliverable) followed by 3 inline fix PRs as drift discoveries piled up. End state: E2E shards [1/3] + [3/3] solidly green; [2/3] now fails on TD-170e dual-button (pre-existing latent bug, not a TD-170 regression).
- **TD-170b investigation (PR #605):** report at `docs/notes/2026-05-18-td-170b-e2e-investigation.md`. Root cause = UX-04x copy-audit trio (PRs #525/#528/#529 all 2026-05-15) renaming UI strings without sweeping E2E. Investigation took ~30 min wall (vs 75-min plan estimate). H5 + H8 HIGH-confidence; 6 hypotheses RULED OUT with cited mechanism. 2 /opus-on-opus cycles (1 caught a mechanically-impossible H1, the JWT-mint-from-SEC-001 hypothesis).
- **TD-170c sweep (PRs #606 + #607 + #608):** 3 sequential fix PRs as new drifts surfaced. #606 PR #529 (UX-050) — 54 callsites across 11 specs (the big one). #607 PR #528 (UX-049) — 1 callsite. #608 PR #525 (UX-048) — 2 callsites. **Total: 57 E2E callsites swept across 13 spec files.** Each iteration was guided by reading the actual CI failure log first (per CLAUDE.md "diagnose first" rule).
- **/oop on TD-170b:** skipped (doc-only sprint; no production module).
- **Lesson learned:** investigation should have inspected ALL shards' failure logs in one pass, not just shard 1. Caught only PR #529's drift initially; PRs #528/#525 needed two additional firefighting rounds. Updated process documented as TD-170d (lint rule prevention) — flagged P2/Medium as the prevention-of-recurrence work.
- **New rows opened:** TD-170d (lint rule prevention, P2), TD-170e (medication-tagging strict-mode violation, XS pre-existing latent).
- **Sprint state file deleted; sprint --close pending after this PR lands.**

### 2026-05-18 — TD-87 Lighthouse a11y CI gate restored (path b) — PRs #601 / #602 / #603
First sprint after midnight. `/sprint td-87-lighthouse-a11y-gate` with a path-pick spike up front. The gate had been functionally disabled for months — Vercel preview auth-gated → script 401/403 silent-skip → exit 0 on every CI run.
- **PR #601 (chore/plans):** archive sweep — moved two orphan ON-71 Phase 2 plan files from yesterday into `docs/plans/archive/` to clear the working tree for sprint preflight.
- **TD-87 path decision:** path (b) Playwright-served local build chosen over (a) Vercel-preview-password-off and (c) post-deploy production audit. Rationale in new **ADR-0005**.
- **PR #602 (chore/td-87 main):** rewrote `.github/workflows/lighthouse-a11y.yml` (trigger `pull_request` + broad `paths:` filter, build + `pnpm --filter web start` + `npx wait-on` mirroring `ci.yml:283–311`, node 22, action SHA-pins per TD-23). Rewrote `scripts/lighthouse-a11y.mjs` — N-URL aggregation, removed 401/403 silent-skip, pinned `@lhci/cli@0.15.1`, preserved `[]→["http://localhost:3000"]` default for local dev. NOT a required check in v1.
- **PR #603 (fix/td-87 hot-fix):** PR #602's first real audit run crashed with `ENOENT` reading the report. Diagnosed: `@lhci/cli collect --outputPath` is a no-op flag; lhci writes to `.lighthouseci/lhr-*.json` in cwd. The original script (predating PR #602) was always broken this way; the 401/403 silent-skip masked it. Fix: per-URL tempdir + `readdirSync('.lighthouseci')` + `--numberOfRuns=1`. PR #602 auto-merged anyway because the new `audit` check is non-required (correct ADR-0005 behavior).
- **End-to-end verification:** PR #603 audit job ran 2m5s and reported `/`=100, `/pricing`=96, `/about`=96 — all ≥ 90 threshold.
- **/owasp pre-plan:** skipped (workflow/docs config; no auth/payment/PHI surface).
- **/opus-on-opus on plan:** 2 cycles. C1 4 must-fix (wait-on regression vs hand-rolled poll, missing merge_group policy, incomplete paths filter, acceptance not executable) → all patched → C2 0 must-fix + 2 should-fix + 2 nit → all patched inline → SHIPS.
- **/oop --from-sprint on shipped files:** 0 must-fix · 2 should-fix · 2 nit. 1 should-fix was a false-positive (`finally` runs on early-return per JS spec). Real residuals seeded as **TD-184** (rawArgs DRY + auditOne split + JWT-mint composite-action dedup).
- **Other rows opened this sprint:** **TD-170b** for the chronic-red E2E investigation that PR #592 didn't fully resolve. Cascade audit at `docs/audits/2026-05-18-roadmap-cascade-audit.md` confirms the roadmap is fully cascaded — no new ON-* work needed from there.
- **Process lesson:** the planned regression-detection proof (inject `button-name` defect) was effectively executed by reality — the script's `ENOENT` was a real regression and the gate caught it on first run. Pure defect-injection is now redundant; future sprints with new CI gates can rely on first-real-run as the proof if the path filter forces the workflow to fire on the introducing PR.

### 2026-05-17 — ON-71 Phase 2 (refill email dispatch) + TD-176 Sentry tag convention — PRs #599 / #598
End-of-day Sprint 3 of 3 tonight. Backlog → plan → threat-model → opus-on-opus → wave → /oop loop with a substantial security-relevant feature.
- **ON-71 Phase 2 (PR #599):** the missing email half of the refill alert. Phase 1 (da3cbb9) already detected low supply and inserted audit `care_event` rows but never notified anyone. PR #599 adds: batched-per-(org, recipient) plain-text Resend dispatch, new `email_dispatch_log` table with composite UNIQUE on `(kind, dedup_key)` + pending-row sweep (>15 min) for step-crash recovery, new `getRefillRecipients` repo helper resolving emails via chunked `auth.admin.getUserById`, pharmacy contact pre-populated in body, `List-Unsubscribe` header included as defense-in-depth.
- **TD-176 (PR #598):** Sentry `component` tag convention codified in `CODE_STANDARDS.md` (tRPC `<router>.<procedure>`, API `api.<resource>.<verb>`, Inngest `inngest.<fn>`). 2 sites swept in `apps/web/app/api/ocr/confirm/route.ts` (pre-grep confirmed no other legacy sites). T1 (ON-71 P2) used the documented `inngest.refillAlert` tag from the start.
- **/owasp pre-plan threat model:** 1 Critical + 4 High + 6 Medium + 3 Low. All C+H+M findings mapped to acceptance lines in the plan; live PHI sentinel test verifies `refillAlert.ts` source contains no `drug_name`/`pharmacy`/`to:`/spread refs in Sentry calls.
- **/opus-on-opus on plan:** 2 cycles. C1 had 3 must-fix (step-boundary failure recovery missing, RLS not default-deny, helper not pre-greped) → all patched → C2 clean (0 must-fix).
- **/oop --from-sprint:** 0 must-fix, 3 should-fix (PostgREST `.or` string unreadable, week-stamp duplication with ISO-bug in `weeklyDigest`, file-grep PHI test should be ESLint rule). All 3 + the threat-model-deferred items seeded as TD-181/182/183 + ON-71c/d.
- **No drift this sprint** — pre-flight grep on row IDs (lesson recorded from Sprint 1 today) caught nothing already-shipped. Process improvement working.

### 2026-05-17 — UX-103b + UX-105b recipient-profile edit affordances + TD-170 E2E fix — PRs #595 / #596 / #592
End-of-day sprint with the standard backlog → plan → opus-on-opus → wave → /oop loop. **Net 5 row closures** (3 fresh ships + 2 backlog-drift catches).
- **UX-103b (PR #595):** CareTeamList edit mode — invite + remove via existing `memberships` mutations, coordinator-gated. Two-click confirm on remove; self-remove client-hidden (server enforces at `memberships.ts:291`/`:318`). New props: `editable`, `orgId`, `recipientId`, `currentMembershipId`. 19/19 vitest. `router.refresh()` for invalidation.
- **UX-105b (PR #596):** EmergencyFooterCard edit mode — DNR/primary contact/hospital. New `identityRepository.updateEmergencyInfo` (read-merge-write, preserves untouched contact_info keys) + new `recipients.updateEmergencyInfo` tRPC with `assertCoordinator` gate. PHI-hardened: 2 sentinel tests (`"JANE-DOE-SENTINEL"` / `"+15551234567"`) prove no leak into Sentry on zod-failure or repo-error paths. PHI grep verifies no `input`/`patch`/`ctx` spreads into Sentry calls. 49/49 vitest. jsonb_set RPC deferred to TD-179.
- **TD-170 (PR #592):** Earlier today — 2-line E2E helper fix (`getByRole({ name: /^Sign in$/ })` → `name: /Verify code/`) restoring auth-gated Playwright path. Selector drift from PR #528 (UX-049). Hypothesis #5 not in the seeded ranking — fix took the empirical Playwright-trace route, not the ranked hypothesis route.
- **/opus-on-opus on plan:** 2 cycles. C1: 3 must-fix / 4 should-fix / 3 nit → C2: 0 must-fix after patching (router-location discovery via grep, server-enforcement cite, PHI sentinel test added).
- **/oop --from-sprint:** 0 must-fix, 2 should-fix, 3 nit, 3 refactor-opportunity → seeded as TD-178/179/180 (others rolled into those rows).
- **Backlog drift caught + cleaned:** UX-109 (#429), UX-110 (#430), UX-112 (#435) — all shipped 2026-05-10 but never closed in §1; flipped to ✅ Shipped this sync. Lesson: future `/sprint` Step 2 should grep `git log` for the proposed row id BEFORE planning, not after writing the plan.

### 2026-05-17 — TD-144 atomic OCR confirm RPC + SEO-005 Geist FOIT fix + TD-173 tab-role test — PRs #589 / #588 / #587
Three independent ships across one sprint marathon:
- **TD-144 (PR #589):** new `confirm_ocr_job(...)` SECURITY DEFINER function takes `FOR UPDATE` on `ocr_jobs` and collapses audit + medication + status flip into one transaction. Eliminates the medication-duplication race in the prior 3-statement pattern. Sentinel return struct, 18 pgTAP assertions (function metadata, privilege grants, happy path, race-loser contract via `already_confirmed` proxy, 4 sentinels, empty-array COALESCE). Route handler shrinks to thin RPC caller + sentinel→HTTP map. Latent bug also fixed: prior route passed nonexistent `created_by` column to `medications`. Two pgTAP cycles needed in CI (fixture used wrong column name, then plan count off by 3). /oop surfaced TD-174/175/176.
- **SEO-005 (PR #588):** scope-honest close. Marketing pages have zero `next/image` usage today so "defer below-fold images" is N/A. One concrete fix: Geist primary body font was missing `display:"swap"` causing FOIT on slow networks. Baseline doc at `docs/runbooks/lighthouse-cwv-baseline-2026-05-17.md` locks pre-UX-109 LCP (61–86ms) for regression detection.
- **TD-173 (PR #587):** new `ShiftsPanel.tabs.test.tsx` — 3 assertions covering `role="tab"` on every layout button (incl. Briefing), tablist `aria-label`, Briefing tab is real `<button>`. UTC anchor comment added to `summarizeSleep` overnight filter. JSDoc /oop should-fix downgraded — file header already credited UX-058 + UX-065 correctly.

### 2026-05-17 — 4-track non-memberships parallel wave — PRs #582 / #583 / #584 / #585
`/sprint` 4-track parallel dispatch off the post-arc backlog. Three doc/harness tracks (A/B/C) plus one substantive feature (D).
- **TD-153a (PR #582):** `docs/runbooks/post-sec-001-happy-path.md` §2 reconciled to current prod (passwordless OTP, dashboard-as-primary surface, correct Settings tabs, no Stripe Checkout). Anchor scan: no live-test consumers. Path B (rebuild screens) explicitly deferred.
- **TD-147 (PR #583):** Two canonical decision-doc surfaces codified — ADR for architecture, UX_DECISIONS for product/language. Cross-referenced header notes added; plan-embedded "Guiding decisions" archived with disposition of all 5 entries. Root CLAUDE.md §Decision docs added.
- **TD-111 (PR #584):** Skill dedup phase 2 — 3 deleted (`dispatch`, `schema-dump`, `tdd-ship`), 3 renamed (`live-test/ship-story/test-gaps` → `*-carelog`), 5 kept (no global counterpart), 0 promoted. 2 plan-listed skills already absent from disk.
- **UX-065 (PR #585):** Pure narrative helpers (`lib/handoffNarrative.ts`, 28 tests) + client `BriefingHandoff.tsx` (`careEvents.timeline` tRPC, tinted CardHeader, loading + retry states) + Briefing tab on ShiftsPanel. 34/34 tests green; PHI clean; no `Date.now()`/`Math.random()`; no raw hex.
- **/owasp pre-plan:** skipped — no auth/middleware/migration surfaces; PHI display in D is product, not analytics.
- **/opus-on-opus (cycle 2 clean):** 3 must-fix in cycle 1 patched (D data-fetch gap, C runbook path, C promotion verify); cycle 2 → 0 must-fix + 1 cosmetic should-fix patched inline.
- **/oop --from-sprint on D:** 2 must-fix downgraded by reviewer's own re-reading (plan-vs-reality mismatch on tab count is informational; the other 2 must-fix reduced to nits on re-examination by the reviewer). Real residual: TD-173 seeded for tab-role test gap + 2 should-fixes (JSDoc UX-058 drift, UTC fragility comment).
- **Process lesson:** Parallel subagents collided on the shared working tree without `git worktree` isolation. Track D stalled at commit; Track B used git plumbing (`hash-object`/`read-tree`/`commit-tree`) to bypass the pre-commit hook race — functionally benign on doc-only but procedurally wrong. Track C had to commit twice. Next wave MUST use `git worktree add <project>/worktrees/<slug>` per CLAUDE.md.

### 2026-05-17 — TD-172 invite Sentry tag-key consistency (arc complete) — PR #580
`/sprint td-172` direct wave (XS, ~5 min). 6th and final sprint in the memberships error-handling thread (TD-167→168→169→171→172).
- **TD-172 (PR #580):** Renamed `invite` Sentry path tag from `"membership.error"` → `"caller.error"` for cross-router alert-dashboard consistency. 1-line code change + matching test assertion. After this lands, all 5 memberships mutations share an identical Sentry tag shape across all error branches.
- **/oop depth pass:** skipped deliberately. 4-line cosmetic close on a should-fix already triaged in the prior /oop run; value-vs-deploy-cost ratio was negligible. Documented as the explicit rationale for the skip per /sprint Step 7b.

### 2026-05-17 — TD-171 invite-mutation transport-error capture + memberships error-handling arc complete — PR #578
`/sprint td-171` direct wave (XS, ~10 min). Fourth sprint in a row on `apps/web/server/routers/memberships.ts`; closes the error-handling arc.
- **TD-171 (PR #578):** Added PGRST116-filtered Sentry capture on `invite.membershipError` mirroring TD-169's changeRole/remove pattern. After this lands, all 5 memberships mutations (`invite`, `accept`, `changeRole`×2, `remove`×2) share: consistent Sentry tag shape, generic client messages, `cause:` preservation, PGRST116 filter where applicable. `accept` correctly skips PGRST116 (RPC error semantics differ from `.single()`).
- **Bundled /oop should-fix from TD-169:** added `remove` PGRST116-no-rows test with `not.toHaveBeenCalled()` assertion (locks the filter against future regressions; mirrors the `changeRole` PGRST116 test).
- **/owasp pre-plan:** skipped — same surface as TD-167/168/169, threat model still current.
- **/opus-on-opus:** skipped — 1:1 mirror of established TD-167/168/169 pattern.
- **/oop --from-sprint:** 0 must-fix. 1 should-fix → seeded as TD-172 (tag-key consistency: `path: "membership.error"` vs `caller.error` elsewhere). 1 nit (same finding as the should-fix).

### 2026-05-17 — TD-169 transport-error Sentry capture on memberships callerError/targetError — PR #576
`/sprint td-169` direct wave (XS, ~15 min). Third sprint in a row on `apps/web/server/routers/memberships.ts`.
- **TD-169 (PR #576):** Added `Sentry.captureException(wrapAdminError(err), { tags: { component, path: "caller.error" \| "target.error" } })` to the pre-auth lookup branches in `changeRole` + `remove` (4 sites). Filter: `error.code !== "PGRST116"` — PGRST116 is PostgREST's "0 rows from `.single()`" sentinel, which IS the legitimate "no membership"/"no target" path. Transport/SQL failures (connection failures, etc.) all carry other codes and get captured. Client-facing FORBIDDEN/NOT_FOUND messages unchanged. +4 security tests pin PGRST116-no-capture vs transport-yes-capture; existing FORBIDDEN/NOT_FOUND mocks in security + logic test files updated to real PGRST116 shape (the bare `{ message: "not found" }` mocks were semantically modeling "no rows" but missing the code field production Supabase always returns).
- **Preflight discovery:** Main CI chronically red — last 50 runs are 0 success, every auth-gated Playwright spec timing out on `page.waitForURL`. Seeded as **TD-170** (P1 investigation, PR #575); sprint E2E precondition explicitly waived with user ack.
- **/owasp pre-plan:** skipped — same surface as TD-167/168, threat model still current.
- **/opus-on-opus:** skipped — 1:1 mirror of TD-167/168 pattern.
- **/oop --from-sprint:** 1 must-fix — `invite.membershipError` has the structurally identical gap (called out in PR body as out-of-scope; /oop confirmed it warranted its own row). Seeded as **TD-171**. 1 should-fix (missing `not.toHaveBeenCalled()` assertion on `remove` PGRST116 test) + 1 nit (comment consistency on 3 of 4 new branches) deferred to future cleanup.

### 2026-05-17 — TD-168 changeRole/remove error-leak follow-up + Vercel preview-skip — PRs #572 / #573
`/sprint td-168` direct wave (XS, ~20 min) + infra hotfix to stop free-tier deploy-quota burn.
- **TD-168 (PR #572):** Three raw-Postgres-`error.message` echo sites in `apps/web/server/routers/memberships.ts` swapped to generic client messages (`"Failed to change role"` / `"Failed to remove member"`). `changeRole.updateError`, `remove.countError`, `remove.deleteError` → `Sentry.captureException(wrapAdminError(error), { tags: { component: "memberships.<mutation>", path: "<op>.error" } })` + `cause: rawError`. +3 vitest cases pinning generic message + Sentry tag shape.
- **Vercel preview-skip (PR #573):** Added `vercel.json` with `ignoreCommand` `bash -c '[[ "$VERCEL_GIT_COMMIT_REF" == "main" ]] && exit 1 || exit 0'`. Stops per-PR preview deploys (CI + adversarial review gates merge, not preview URLs; Vercel free tier was being burned).
- **/owasp pre-plan:** skipped — same surface as TD-167, threat model still current.
- **/opus-on-opus:** skipped — TD-168 is a 1:1 mirror of TD-167's already-reviewed pattern.
- **/oop --from-sprint:** 0 must-fix on TD-168. 1 should-fix surfaced as TD-169 (callerError/targetError branches discard Supabase transport errors silently in changeRole+remove). 1 nit (accept fallthrough missing `cause:` field; Sentry has it; cosmetic).

### 2026-05-17 — Invite-accept error-leak fix (TD-167) + rate-limit design comment (TD-155) — PRs #569 / #570
`/sprint td-167-155-error-leaks` 2-track direct wave.
- **TD-167 (PR #569):** Five raw-Postgres-`error.message` echo sites swapped to generic client messages. HTTP route `apps/web/app/api/invite/[token]/accept/route.ts` (RPC-error / sentinel-fallback / catch branches) + tRPC `apps/web/server/routers/memberships.ts:88-128` accept-mutation (RPC-error + fallthrough). All Sentry-side captures use `tags: { component, path }`. Documented sentinels (`not_found`, `email_mismatch`, `already_used`) keep their user-facing copy; unknown sentinels fire a Sentry capture so a new SQL failure mode surfaces. +3 route tests + 1 router test + 1 router test updated.
- **TD-155 (PR #570):** Comment-only. Documents the deliberate IP-keying choice in `apps/web/lib/rateLimit.ts`. Investigation closed it as no-op — live-test 429s came from Supabase Auth's own send-rate limiter, not carelog middleware. SignInForm calls `supabase.auth.signInWithOtp` client-direct; the carelog middleware guards `auth/verify` (where IP-keying defends against rotating-IP brute force on a single victim's 6-digit code).
- **/owasp pre-plan:** none (surface previously threat-modeled in TD-142 as T8; this PR closes T8).
- **/opus-on-opus (Sonnet):** 2 cycles. Cycle 1: 2 must-fix (missed M1 `memberships.ts:122-124` fallthrough leak, M2 `route.ts:43` `?? data.error` fallback) + 3 should-fix + 1 nit → patched. Cycle 2: 0 must-fix + 1 should-fix (sentinel-unknown Sentry capture coherence) + 1 nit → patched.
- **/oop --from-sprint:** 1 must-fix surfaced — same class of leak in `changeRole` + `remove` mutations (3 more sites in the same file, out of scope for TD-167). Seeded as TD-168. 2 should-fix + 1 nit captured inline.

### 2026-05-17 — TD-142 invite-acceptance state-machine collapse — PR #567
`/sprint td-142-invite-rpc-collapse` 1-track direct deletion-only PR. Discovery in planning: both production callers (tRPC `memberships.ts:89` and HTTP `app/api/invite/[token]/accept/route.ts:20`) already invoked `supabaseAdmin.rpc("accept_invite", ...)` directly; the racy TS `acceptInvite` in `membershipsRepository.ts:163-200` was dead code. The "collapse" became a clean delete: -279 lines / +126 lines (mostly retrospective doc comments).
- Atomicity hole eliminated before resurrection — the `Promise.all` of two non-atomic UPDATEs is gone.
- SQL function (`supabase/migrations/20260407000000_atomic_invite_accept.sql`) + REVOKE migration (`20260516000000`) are now the single source of truth.
- Retrospective comments in the repository file point the next reader at the SQL function, REVOKE migration, and both live callers.
- **/owasp pre-plan (Sonnet fallback):** 1 Critical / 2 High / 4 Medium / 3 Low. T1 Critical (race) resolved by deletion. T2 High (email-mismatch normalization) verified — both live callers source email from JWT. T3 High (anon/authenticated RPC access) verified via REVOKE migration already on main. T8 Med (route error-echo) deferred → TD-167.
- **/opus-on-opus (Sonnet):** 1 cycle. 1 must-fix (acceptance-criterion grep self-inconsistency) + 2 should-fix + 2 nits → all patched.
- **/oop --from-sprint:** 1 must-fix (T8 route error-echo — pre-existing on main, not regressed) + 1 nit. Must-fix promoted to TD-167; nit (comment aging) folded into this commit.

### 2026-05-17 — Live-test tail (TD-166, TD-154, TD-159) — PRs #563 / #564 / #565
`/sprint live-test-tail-2026-05-17` 3-track direct wave closing the small live-test follow-up tail.
- **TD-166 (PR #563):** `apps/web/app/(app)/dashboard/DashboardClient.tsx` — added `setLoading(false)` before pending_invite + pendingPlan-success early returns. /oop must-fix from prior sprint addressed. +2 vitest cases (14 total file passes).
- **TD-154 (PR #564):** `apps/web/app/signin/SignInForm.tsx` — `friendlyOtpError()` now returns "That code didn't work. Check the digits or send a new code." on both "expired" AND "invalid"/"not match" branches. Eliminates the self-reinforcing failure loop where a wrong-code user was told the code expired and re-requested, invalidating their valid code. Security-equivalent (no wrong-vs-expired oracle). +1 updated + 1 net-new vitest case (8/8 file passes).
- **TD-159 (PR #565):** `apps/web/app/(app)/settings/page.tsx` — new client-side `HistoryExportLink` component gates on `role=coordinator AND accepted_at IS NOT NULL`, returns null for non-coordinators. "(coordinators only)" sub-label removed. Server-side gate at `history-export/page.tsx` unchanged (defense-in-depth). +4 vitest cases (12/12 file passes).
- **/opus-on-opus (Sonnet fallback):** 2 cycles. Cycle 1: 1 must-fix (test count, reviewer misdiagnosed — 12 is correct) + 2 should-fix + 2 nits → patched the legit findings. Cycle 2: 1 must-fix (acceptance-count phrasing) + 1 should-fix (TD-166 test file scope) + 1 nit → all patched.
- **/oop --from-sprint:** 1 must-fix + 1 should-fix were both timing artifacts (reviewer hit main BEFORE #565 merged). 1 substantive should-fix (inner `catch {}` swallow on pendingPlan failure) is mitigated by TD-165's outer try/finally — captured inline, no new backlog row. 1 nit (TD-154 duplicate string literals — left as-is for clarity per inline comment).

### 2026-05-17 — TD-146 cron audit + observability follow-ups (TD-164, TD-165) — PRs #559 / #560 / #561
`/sprint td-146-followups-2026-05-17` 4-track direct wave (3 PRs + 1 no-op) closing the P1 cron-audit blocker and the two /oop-flagged observability follow-ups from the prior sprint.
- **TD-146 (PR #559):** Source-level audit of all 14 Inngest functions (8 cron + 6 event). Confirmed no dark crons — high-freq crons (`rateLimit429Monitor` 5min, `shiftTradeExpiry` 15min) have no env-gates. Projected ~388 cron firings/24h = ~23% of Hobby tier monthly quota. **Inngest-vs-Queues migration decision (TD-133, ON-70, ON-71) is unblocked.** Doc at `docs/research/2026-05-17-td-146-inngest-cron-audit.md`; §6.7 pointer added to `inngest-vs-queues.md`. Operator dashboard verification for per-function 24h run counts deferred (~10 min, §3 step list).
- **TD-164 (PR #560):** UUID-validate `recipientId` extracted from pathname before treating it as routable. Non-UUID segments fall through to the no-recipient code path (items disabled, hint banner shown). PHI-safe Sentry breadcrumb on failed validation emits only `{ matched, valid, segmentLength }` — no raw value, no pathname. 4 new vitest cases (29 total file passes).
- **TD-165 (PR #561):** Wraps DashboardClient data fetch in try/catch/finally. Catch fires only for thrown rejections (Supabase data-errors still flow via inline `result.error` per TD-152). `setLoading(false)` moved into finally. `Sentry.captureException` tagged `path=network`. New vitest case (12/12 dashboard file passes).
- **TD-163 (closed no-op):** Stale-screenshot row resolved — `.gitignore:38` already covers `**/__tests__/**/__screenshots__/`; the "orphan" PNGs were local-only vitest artifacts, never tracked.
- **/opus-on-opus (Sonnet fallback):** 2 cycles. Cycle 1: 2 must-fix (try/catch redundancy claim wrong; TD-163 had 2 PNGs not 1) / 2 should-fix → all patched (anchor verified via Read; reviewer's "redundant" claim was wrong — only inner Stripe block was guarded, main fetch was unguarded). Cycle 2: 1 must-fix (`rawValue` phantom binding in breadcrumb spec) / 1 should-fix (stale risks note) → patched.
- **/oop --from-sprint:** 1 must-fix (pre-existing on main, not regressed by sprint), 2 should-fix, 2 nit. Must-fix promoted to TD-166 (`setLoading(false)` unreachable on `pending_invite` + `pendingPlan` redirect paths). Should-fixes captured inline for next DashboardClient touch.

### 2026-05-17 — Observability follow-ups (TD-148, TD-162) + TD-152 in-review — PRs #555 / #556 / #557
`/sprint observability-followups-2026-05-17` 3-track direct wave focused on closing the observability gap that hid TD-149/150 in production.
- **TD-148 (PR #555):** `apps/web/package.json` `inngest ^3.54.0` → `^3.54.2`, clears the Inngest dashboard "v3 TS SDK Security Alert" without crossing into 4.x. 139 inngest-related tests stay green.
- **TD-162 (PR #556):** `apps/web/server/routers/ai.ts:113,120,129` `count: "exact"` → `count: "estimated"` — same pattern that caused TD-149's 503 on DashboardClient. Bidirectional grep verified (`exact=0`, `estimated=3`). No new test (single-mode swap, no behavior change).
- **TD-152 (PR #557, in review):** `Sentry.addBreadcrumb` at QuickLogFab click handler + `Sentry.captureException` on `countResult.error` / `earliestResult.error` in DashboardClient. Cycle-1 must-fix from reviewer: original plan had `try/catch` wrapping `Promise.all` — would never fire because Supabase JS client returns errors via `result.error` rather than throwing. Plan + impl corrected to `result.error` inspection per the actual client semantics. 23/23 QuickLogFab + 11/11 DashboardClient vitests pass; PHI rule respected (UUID-only in payload, no recipientId/name in Sentry data).
- **/opus-on-opus:** 2 cycles. Cycle 1: 2 must-fix (try/catch trap; TD-162 grep one-sided) / 3 should-fix → all patched. Cycle 2: 0 must-fix / 1 should-fix / 1 nit → patched, clean.
- **/oop --from-sprint:** Reviewer hit main pre-#557 merge — two "must-fix" items were timing artifacts that resolve by #557 landing. Two legitimate should-fixes promoted to §1: TD-164 (validate recipientId as UUID pre-router.push) and TD-165 (try/catch around Promise.all for network-failure path, complementing TD-152's data-error path).

### 2026-05-17 — Live-test P1s sprint (TD-149, TD-150, TD-151) — PRs #551 / #552 / #553
`/sprint live-test-p1s-2026-05-17` direct-implementation 3-track wave restoring two basic user actions broken by post-SEC-001 prod state (auth OTP delivery + dashboard journal write).
- **TD-149 (PR #553):** `apps/web/app/(app)/dashboard/DashboardClient.tsx:166` `count: "exact"` → `count: "estimated"` on `care_events`. Root cause documented in `docs/research/2026-05-17-td-149-care-events-head-503.md`: PostgREST `count=exact` on an RLS-protected table forces `SELECT count(*)` to evaluate `user_can_access_recipient()` per row → statement timeout → 503. H1 (missing index) and H2 (RLS function error) rejected with rationale; H3 (call-site change to `estimated`) accepted. No migration. 9/9 DashboardClient vitests stay green.
- **TD-150 (PR #552):** `apps/web/components/QuickLogFab.tsx` — replaced no-op `router.push("/dashboard")` fallback (when no `recipientId` in URL) with explicit disabled state + aria-described hint ("Open a recipient's journal first to log"). Previous "silent menu close" behavior was test-asserted; test suite restructured into a 4-test `describe` block covering hint visibility, disabled state, no-navigation, aria-describedby wiring. 21/21 QuickLogFab vitests pass.
- **TD-151 (PR #551):** New `docs/runbooks/td-151-dmarc-sender-hardening.md` operator runbook for DMARC TXT publication on `care-log.org` + optional Supabase Auth From-address swap off `noreply@`. Also commits the previously-untracked Cowork-driven happy-path runbook (`docs/runbooks/post-sec-001-happy-path.md`) with a top-of-file note pointing at the DMARC mitigation.
- **/opus-on-opus:** Sonnet reviewer fallback per global policy. Cycle 1 (3 must-fix / 4 should-fix / 2 nit) → all must-fixes patched (CONCURRENTLY index removed, Track 2 reframed from "modal" to "navigation", DashboardClient.tsx call site added to scope). Cycle 2 (1 must-fix / 3 should-fix / 2 nit) → must-fix patched (EXPLAIN target corrected to `count(*)` not `SELECT 1`); 2/3 should-fixes addressed inline (zero-recipient assumption gating + loading-state guard + merge-staleness note).
- **/oop --from-sprint:** 0 must-fix / 2 should-fix / 2 nit. Both should-fixes promoted to backlog as TD-162 (apply TD-149 fix to `ai.ts:113,120,129`) and TD-163 (delete stale `QuickLogFab` screenshot artifact). Nits captured inline for next QuickLogFab touch.

### 2026-05-16 — SEC-001 production secrets rotation (operator action)
Operator walked `docs/runbooks/deploy-fix-and-live-test.md` end-to-end. Rotated all 5 secrets in order — Upstash → Resend → Inngest (signing + event together) → Supabase → Stripe (last). For Inngest, created a dedicated **Branch environment** with its own keys for Vercel's Development tier so production keys never leak to non-prod (SEC-009 pattern). Supabase: project was migrated by Supabase off legacy `eyJ…` JWTs onto the new `sb_secret_*` / `sb_publishable_*` format mid-rotation; weekly-digest kept failing with `Invalid API key` then `Legacy API keys are disabled` until both `SUPABASE_SERVICE_ROLE_KEY` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` Production tiers were swapped to the new format. Stripe webhook rolled with the 7-day grace window (no signature-failure gap); live-mode endpoint not yet provisioned so verification against test-mode endpoint only. `.env.local` regenerated via `vercel env pull --environment=development` against project root; git-history scrub confirmed `apps/web/.env.local` never committed. **Follow-ups surfaced (not yet seeded):** Vercel env vars `SUPABASE_SECRET_KEY` / `SUPABASE_PUBLISHABLE_KEY` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_JWT_SECRET` / `SUPABASE_ANON_KEY` / `SUPABASE_URL` are unused by `apps/web/lib/env.ts` and should be pruned; the regenerated `.env.local` ended up at project root instead of `apps/web/` (link directory mismatch).

### 2026-05-15 — ON-76 Inngest billing-tier verification (operator action)
Operator captured: tier = **Hobby**, 30-day count not visible on free tier, last-24h runs = 0. Result appended to `docs/research/2026-05-15-inngest-vs-queues.md` §6.6. Surprising finding: zero events in 24h contradicts the Cowork doc's worked estimate of ~387 cron firings/day, suggesting prod cron firings may not actually be occurring (Inngest app not registered, app unreachable from Inngest cloud, or high-frequency crons silently disabled). Seeded TD-146 to audit prod cron-firing state — the cost half of the §6 Inngest-vs-Queues recommendation is structurally unverifiable on Hobby tier until that audit closes. Inngest-cluster Ready rows (TD-133, TD-142, TD-144, ON-70, ON-71) stay blocked pending TD-146.

### 2026-05-15 — UX-104b LikesDislikesList edit affordance — PR #539
Sprint `ux-104b-likes-dislikes-edit` (`/sprint` full pipeline, single-track direct). New `recipients` tRPC router with `updatePreferences` mutation following the `eolPlan.upsert` pattern (assertCoordinator → recipient-in-org check → supabaseAdmin update). Strict zod input: per-item `trim().min(1).max(120)`, arrays capped at 50; intentionally asymmetric with the permissive read-side `PreferencesSchema` (write-strict, read-permissive is correct for jsonb). LikesDislikesList converted to client component with subcomponent carve-out: top-level returns ReadOnlyView when !canEdit so existing 6 tests stay green without a tRPC provider; nested EditableView holds `useMutation` + `router.refresh()` on save. Profile page fetches caller's `membership.role` in the existing Promise.all and passes `canEdit` + `orgId` + `recipientId` to the component. /opus-on-opus 2 cycles (1MF/3SF/2N → 0/0/0): MF1 (stale-prop drift) resolved via explicit "useState seeded from initial-prop snapshot + `router.refresh()` post-save" strategy. SF3 (subcomponent carve-out promoted to required shape) prevents existing tRPC-less tests from breaking. PHI: not applicable per row (preferences are free-text food/activity strings, not identity-bound). 11 new tests (4 component + 7 router); full suite 2144/2148 green. Recovery note: initial commit landed on local `main` due to a stale git index lock; recovered with `git branch -f` + `git checkout` + `git update-ref refs/heads/main origin/main` — local-only pointer moves, `origin/main` untouched.

### 2026-05-15 — UX-053 EmptyState primary-action affordances — PR #537
Sprint `ux-053-empty-state-affordances` (`/sprint` full pipeline, single-track direct). Audit of 6 EmptyState consumers; 0 used the component's `actionLabel`/`onAction` props. Hoisted CTAs into MedicationPanel ("Add medication", coordinator-gated → `setShowForm(true)`) and TeamPanel ("Invite a member", `canInvite && !showInvite` gated → `onToggleInvite`). The `!showInvite` guard prevents a desktop dead-click: TeamPanel's invite form is always-visible at `lg:block` and `onToggleInvite` is a true toggle — opus cycle-1 catch. 4 intentionally-passive sites (DocumentVault, JournalTimeline, MedicationLinkedDocs, MedicationRecentEvents) annotated with `// UX-053: passive — <reason>` comments. /opus-on-opus cleared cycle 1 (0/2/1). Also shipped `docs/research/2026-05-15-cowork-research-questions.md` — handoff doc for an upcoming Cowork research session (primary: Inngest vs Vercel Queues; alts: Supabase atomic patterns for TD-142/144; Vercel AI Gateway adoption).

### 2026-05-15 — TD-143 mood-label map dedup — PR #535
Sprint `td-143-mood-labels-dedup` (`/sprint` full pipeline, single-track direct). Path (a) from the row: extract canonical `MOOD_LABELS: Record<Mood, string>` + `MOOD_KEYS` tuple (`as const satisfies readonly Mood[]`) in `apps/web/lib/mood.ts`. Five UI surfaces dedup'd; `server/routers/moodEntries.ts` keeps digest-tone `okay: "Settled"` via explicit spread + comment (vs prior stealth divergence). /opus-on-opus 2 cycles (1MF/3SF/2N → 0/1/2): cycle-1 must-fix was `MoodVal = Mood | ""` type narrowing — resolved by typing derived arrays as `{value: Mood; label: string}[]` and verifying `Mood` is assignable to `MoodVal` at every `setMood` call site. /oop --from-sprint clean (0/1/2). WeeklyMoodBars + MoodHeatmap correctly excluded (different MoodBucket type / lowercase aria-text).
✅ **TD-143** (PR #535) `apps/web/lib/mood.ts` (+MOOD_KEYS, MOOD_LABELS, moodLabel exports), `apps/web/server/routers/moodEntries.ts` (digest spread+comment), `apps/web/components/app/RecipientProfile.tsx` (inline map → import), `apps/web/app/(app)/journal/[recipientId]/{SymptomPanel,JournalEntryForm}.tsx` (array derivation), `apps/web/components/journal/{MoodSpectrum,PromptedComposer}.tsx` (label from canonical). 80/80 targeted tests + 2133/2137 full vitest green.

### 2026-05-15 — TD-145 migration-lint whitelist for same-file new-table indexes — PR #533
Sprint `td-145-migration-lint-whitelist` (`/sprint` full pipeline, single-track direct implementation). Closes the SEC-007 post-merge `Lint risky DDL patterns` advisory + prevents future "new table + index in one migration" false positives. /opus-on-opus 2 cycles to clear (2MF/3SF/2N → 0/1/2): cycle-1 must-fixes were ON-extractor regex underspecification + CREATE TABLE capture clarification (operate on normalized $STATEMENTS, not raw file); should-fixes were named regression-fixture (`20260408000001_shifts_schema_align.sql` — has non-CONCURRENT idx_shifts_recipient_time + idx_shifts_status on pre-existing shifts table), `-P` portability, CI-invocation cite (`.github/workflows/migration-lint.yml:28` keeps invoking production script; new test harness is dev-gate only).
✅ **TD-145** (PR #533) `scripts/migration-lint.sh` adds LOCAL_TABLES pre-scan + per-statement ON-target check; outer `CREATE … INDEX` gate protects against CREATE POLICY/TRIGGER misread. Bash hardening: `|| true` after grep -E (no-match exit-1 vs `set -e`), POSIX ERE only. `scripts/__tests__/migration-lint.test.sh` (new, 9 cases: existing-table flag retained, new-table whitelist, CONCURRENTLY pass, safe-migration bypass, mixed allow+flag, quoted ident, schema-qualified, real SEC-007 + shifts spot-checks). No CI workflow change — test harness is dev-gate.

### 2026-05-15 — SEC-007 OCR confirm audit-log site — PR #531
Sprint `security-audit-pair` (`/sprint` full pipeline, narrowed at Gate 1 to SEC-007 only; SEC-008 deferred). Closes FIND-003 part 3 (SEC-004 closed parts 1+2 via PR #493). New append-only `ocr_audit_log` table — PHI-free by construction (SHA-256 of `raw_text` NFC-normalized + `confirmed_field_keys` allowlist text[], never values, plus `field_count`, `org_id_snapshot`, `user_id` FK). Defense-in-depth append-only via BOTH triggers AND `REVOKE UPDATE,DELETE` from PUBLIC + authenticated/anon/service_role (cycle-1 Opus catch: covers `session_replication_role='replica'` bypass scenario). Service-role-only RLS policy. /opus-on-opus 2 cycles to clear (2MF/3SF/2N → 0/0/1): cycle-1 must-fixes were REVOKE-alongside-triggers + 409-race documentation (scoped pre-existing → TD-144); should-fixes were COALESCE+filter contradiction annotation + pgTAP errcodes (P0001/23514/23503/42501). /oop --from-sprint clean (0/2/3).
✅ **SEC-007** (PR #531) `supabase/migrations/20260516010000_create_ocr_audit_log.sql` (new table + RLS policy + triggers + REVOKE + backfill DO block for existing `status='confirmed'` jobs with empty-string-SHA-256 marker + `backfilled=true` discriminator). `supabase/tests/ocr_audit_log_rls.test.sql` (10 pgTAP cases: RLS enabled, anon/auth SELECT returns 0, service_role SELECT works, UPDATE/DELETE blocked w/ 42501, CHECK violations w/ 23514, FK RESTRICT w/ 23503 via postgres role). `apps/web/app/api/ocr/confirm/route.ts` (widened ocr_jobs SELECT to include `raw_text`; audit INSERT lands BEFORE medication INSERT, fail-loud returns 500 on audit error). Route tests: existing 5 cases pass + 2 new SEC-007 cases (audit-fail→500 no-med-insert, payload SHA-256 Buffer + field-key shape).

**Follow-up seeds in §1:** TD-144 (atomic OCR confirm via SECURITY DEFINER RPC + SELECT FOR UPDATE — fixes the pre-existing race between status-check and status-update that SEC-007 records-per-attempt but doesn't fix; medication duplication is the real bug being deferred), TD-145 (DDL lint CONCURRENTLY false-positive on the new audit-log indexes — empty table at index time, advisory-only CI fail, fix-forward annotation or lint script whitelist).

**Nits not seeded:** OcrJobStateMachine always single-use (static validator would match); pgTAP case 5/6 labels overpromise REVOKE-before-trigger ordering when only SQLSTATE asserted; field-key derivation duplicated 3x (audit insert, PostHog capture, SQL CHECK allowlist) — folded into TD-144's RPC-collapse work.

### 2026-05-15 — copy-audit-trio finish (UX-049/050) — PRs #528/#529
Sprint `copy-audit-trio-finish` (`/sprint` full pipeline) shipped the §2 (auth) and §4 (journal mood + entry-form) halves of `apps/web/copy-audit.md` plus a 6-site `"Crisis"` → `"Hard"` mood-label rename audit doc §4 called for. Sibling sprint to UX-048 (shipped earlier today). 2 `/opus-on-opus` cycles before clear (4MF/3SF/2N → 0/0/0). Cycle-1 caught: OnboardingForm triple-error consolidation already in main (NO-OP verified); the `"Crisis"` rename missed `apps/web/server/routers/moodEntries.ts:19` orphan + `PromptedComposer.test.tsx:102,107` regex matchers (initial pre-flight grep was scoped to `apps/web/{app,components}` only); brief-headline prose `"crisis moment"` in `lib/brief/headlineStrategies/CrisisStrategy.ts:21` flagged as explicit non-goal (separate copy-decision); single-line ternary shapes locked for SignInForm.tsx:132 and JournalEntryForm.tsx:192.
✅ **UX-049** (PR #528) `SignInForm.tsx` (OTP button "Sign in" → "Verify code", progress loaders), `OnboardingForm.tsx` (3 register fixes, error consolidation NO-OP), `care/[shareToken]/page.tsx` (trim sentimental thanks copy). 7 production edits + 4 test assertions. PRODUCT.md candid-trust register applied.
✅ **UX-050** (PR #529) `JournalEntryForm.tsx` + `ShiftForm.tsx` (§4 copy: placeholder, button "Share update" → "Post to journal", UTC jargon, supporter help-text rewrite), plus the `"Crisis"` → `"Hard"` label rename across 6 sites (`JournalEntryForm`, `SymptomPanel`, `MoodSpectrum`, `PromptedComposer`, `RecipientProfile`, `server/routers/moodEntries.ts`). Mood `value: "crisis"` enum + `--color-mood-crisis` CSS token preserved (red color stays).

**Follow-up seed in §1:** TD-143 — mood-label map duplication (6 maps; `moodEntries.ts:17` has divergent `okay: "Settled"` — decide intentional digest-tone vs accidental drift, extract shared const).

**Nits surfaced, not seeded:** `--color-mood-crisis` token name next to "Hard" label is jarring on read (non-goal per plan); 3 byte-identical mood-label arrays at DRY threshold (folded into TD-143).

### 2026-05-15 — UX-048 empty-state copy polish — PR #525
Sprint `ux-048-empty-states-polish` (`/sprint` full pipeline) shipped a strings-only sweep of `apps/web/copy-audit.md §3` (3 Critical, 8 Medium) plus one sibling site Opus surfaced (`NowBoard.tsx:230` carried the same SaaS-tic phrase BriefHero was fixing). 12 production surfaces + 5 test files; 42 insertions, 32 deletions. 3 `/opus-on-opus` cycles: cycle-1 caught 17-day line drift in the audit doc (562→581, 311→282, 162→168), the NowBoard sibling, and unlisted test files; cycle-2 caught multi-line JSX Find cells needing verbatim source blocks for `Edit`-tool uniqueness; cycle-3 cleared. Test discovery surfaced 2 additional test files (`SymptomPanel.test.tsx`, `CoverageSettings.test.tsx`) beyond the 3 enumerated upfront — updated in same commit.
✅ **UX-048** (PR #525) Replaces generic-SaaS empty-state copy with PRODUCT.md warm·candid register: `"Quiet day in the journal"` (JournalTimeline), `"You're not on a care team yet. Set one up to start logging."` (DashboardClient), `"The brief fills in once someone writes from the journal."` (BriefHero), `"The first note from the journal lands here."` (NowBoard sibling), plus 8 Medium register fixes. /oop --from-sprint clean (0/0/2). PRODUCT.md "the recipient is a person, not a chart" register applied.

**Nits surfaced, not promoted to backlog:** none material (strings-only sweep has lowest architectural surface).

### 2026-05-15 — careteam RLS defense-in-depth pair (TD-120/129) — PRs #522/#523
Sprint `careteam-rls-defense` (`/sprint` full pipeline) shipped two defense-in-depth fixes in a single 2-track wave (~1.5h). `/opus-on-opus` ran 3 cycles before clearing (cycle-1 must-fix: `SAVEPOINT/ROLLBACK TO SAVEPOINT` does NOT reset `SET LOCAL ROLE` — role is transaction-scoped; cycle-2 must-fix: fixture INSERTs needed explicit column-list enumeration with the semicolon-to-comma rule called out for all three test tables). T2's pgTAP run surfaced a real production grant gap that the original migration's `REVOKE FROM PUBLIC` had not closed.
✅ **TD-120** (PR #522) `apps/web/server/repositories/membershipsRepository.ts` — `getCareTeamForRecipient` now takes `supabase: SupabaseClient` as first arg; memberships query reads via session-scoped client (RLS-gated). Per-member `auth.admin.getUserById` stays on `supabaseAdmin` (only way to read `user_metadata`). Caller in `recipient/[recipientId]/profile/page.tsx:84` threads the same session client. Test file got a `makeMockSupabaseClient` helper; all 6 getCareTeamForRecipient tests updated; filter-contract assertions preserved.
✅ **TD-129** (PR #523) `supabase/tests/invite_tokens_rls.test.sql` — added cases 7/8/9 locking grant state on `accept_invite(text, uuid, text)`. Case 7 calls the function as `service_role` (positive path). Cases 8/9 use `has_function_privilege('authenticated'|'anon', 'accept_invite(...)', 'EXECUTE')` returning `false` (grant-state check, sidesteps a PG 17.6 segfault that crashes the server when under-privileged roles attempt to invoke functions — verified independent of `accept_invite`). New migration `20260516000000_revoke_accept_invite_from_anon_authenticated.sql` explicitly REVOKEs EXECUTE from anon + authenticated — Supabase's `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT EXECUTE ON FUNCTIONS TO anon, authenticated` had silently overridden the original migration's `REVOKE FROM PUBLIC`, so PostgREST-direct RPC by `anon` was still possible. Closed.

**Follow-up seed in §1:** TD-142 — TS `acceptInvite` and SQL `accept_invite` encode the same state machine; TS path has a `Promise.all`-atomicity hole called out in its own comment, SQL path is dead code defended only by the new REVOKE. Decision needed: (a) flip TS to `supabaseAdmin.rpc()` and delete the body OR (b) drop the SQL function + migration + pgTAP cases.

**Nits surfaced (not seeded):** S2 leaky dual-role signature on `getCareTeamForRecipient` (consider injecting a `resolveUser?` callable); N1 stale `briefs.ts:232` cite at `membershipsRepository.ts:58`; N2 `console.warn` should be a structured logger / `Sentry.captureMessage(level: "warning")` to match the inline "surface the cause for prod observability" intent.

### 2026-05-16 — Journal types + perf pair (TD-138/137) — PR #520
Sprint `journal-types-perf` (`/sprint` full pipeline) shipped a 2-commit pure-refactor PR from the `journal-meds-quick-wins` `/oop --from-sprint` review. Opus pre-merge ran 2 cycles (1 must-fix → 0/1/1) — substantive catch was that 4 of the 5 `JournalEvent` definitions were byte-identical but `EntryDetailClient.tsx:25` was missing `actor_id` (UX-041 PR #507 had updated 4 sites, missed this one). Plan was patched to address the widening explicitly.
✅ **TD-138** (PR #520) New `apps/web/types/journal.ts` — single canonical `JournalEvent`; 5 consumers import via `@/types/journal`. EntryDetailClient widens to include `actor_id` — safe additive (DB row always has the field). Pure refactor.
✅ **TD-137** (PR #520) `JournalTimeline.tsx` — replaced per-render `members.find()` (O(N×M)) with a `useMemo`'d `Map<string, string | null>` (O(N+M) total). `JournalCard` interface changed from `members: AuthorMember[]` to `membersByUserId: Map<string, string | null>`. 3-case equivalence proof (hit-string, hit-null, miss) preserves behavior exactly. Path (a) shipped; path (b) (pre-resolve into useJournalData state) deferred.

### 2026-05-16 — TD-121 getCareTeamForRecipient rate-limit cap — PR #518
Sprint `td-121-membership-cap` (`/sprint` full pipeline) shipped a three-fix bundle on the care-team query. Opus pre-merge review (0 must-fix / 3 should-fix) was the substantive guide: the original plan's `.limit(50)` alone would not have solved the named scenario because 50 concurrent `getUserById` calls IS the per-second Auth Admin rate limit. Expanded scope at Gate 2.
✅ **TD-121** (PR #518) `apps/web/server/repositories/membershipsRepository.ts` — added `.limit(50)` (absolute cap), chunked the `getUserById` fan-out via `Promise.allSettled` with `CARE_TEAM_CHUNK_SIZE = 8` (holds in-flight under the rate limit even with concurrent profile loads), and `console.warn`'d rejected settlements for prod observability. Members are dropped silently from the rendered list on blip rather than 500ing the page. Test extension: added `chain.limit` to the thenable mock, added a `.limit(50)` filter-contract assertion, and a new test pinning the blipping-getUserById behavior with a `console.warn` spy (12/12 tests green). Cache-based mitigation (extend display_names cache to members) deferred — separate row when actually needed.

### 2026-05-15 — UX-044 Leave-org placebo removal — PR #516
Sprint `ux-044-leave-org` (`/sprint` full pipeline) shipped a single tiny removal that turned out to be subtly wrong-shaped at first pass. Two Opus review cycles forced a Gate-2 scope re-shape: the backlog row's "replace with link to /team/admin" promise was a placebo-relocation — coordinators can't self-remove there (`TeamAdminClient.tsx:126`) AND non-coordinators get redirected away (`team/admin/page.tsx:19`). Re-scoped to ship the removal with honest no-link copy, deferring the real leave-org flow to a future feature.
✅ **UX-044** (PR #516) `apps/web/app/(app)/settings/page.tsx` — deleted the `Leave` button, the confirmation modal, the `useState`, and the orphaned `toast.error` (77 LOC). Replaced with: "To leave a team, ask a coordinator to remove you from the member list. If you're the only coordinator, contact support so we can transfer ownership or close the organization." Danger-zone section stays as the natural anchor for the real flow when it ships.

### 2026-05-15 — Opus follow-up pair (TD-139/140) — PRs #513/#514
Sprint `opus-followup-pair` (`/sprint` full pipeline) closed both Should-fix items Opus surfaced during pre-merge review of the prior sprint via single direct-implementation wave (~1h work, file-disjoint). Opus took 3 `/opus-on-opus` cycles to clear (cycle-1 must-fix: spec exact narrowing pattern; cycle-2 must-fix: helper must live in `apps/web/lib/`, not co-located in `route.tsx` — vitest node project can't parse JSX, and route imports `@react-pdf/renderer` + supabase which would be pulled into a unit test for a 10-line helper).
✅ **TD-139** (PR #513) New `apps/web/lib/pickJournalBody.ts` — pure helper, precedence `text > note > notes` with `typeof === "string"` narrowing. PDF route now uses the helper in place of the inline text-only check. 7-case unit test pins precedence, fallback chain, null payload, empty strings, and non-string slots. Coordinators whose journal entries populate `note`/`notes` no longer get blank PDF exports.
✅ **TD-140** (PR #514) `apps/web/server/repositories/__tests__/membershipsRepository.test.ts` — hoisted `makeListChain` to a named const; added 3 filter-contract assertions on `chain.select`/`chain.eq`/`chain.or` matching the prod query at `membershipsRepository.ts:58-65`. `expect.stringContaining("user_id")` keeps the select-args assertion future-proof against additive column changes. `.not` assertions deferred (called twice w/ different args).

**Follow-up seed in §1:** TD-141 — `JournalPayload` has three parallel free-text slots (`text`/`note`/`notes`) with 3 different "primary body" policies across the codebase. Real cleanup is a schema migration to a single `body` column + call-site sweep; half-day work needing its own plan and ADR for precedence.

### 2026-05-15 — Adversarial-gate cleanup pair (TD-130/122) — PRs #510/#511
Sprint `adversarial-gate-cleanup` (`/sprint` full pipeline) shipped 2 tiny defense-in-depth fixes carried forward from prior adversarial-gate findings via single direct-implementation wave (~30min total). Opus recovered post-degraded; `/opus-on-opus` ran on real Opus this time and cleared the plan at 0 must-fix.
✅ **TD-130** (PR #510) `apps/web/app/api/history/export/pdf/route.tsx:231-235` — restored `typeof ev.payload.text === "string"` narrowing that PR #470's CareEvent union refactor had inadvertently replaced with `String(...)` casts (which silently stringified non-string payload.text values like numbers and objects). Pre-PR-#470 skip-non-strings behavior is back.
✅ **TD-122** (PR #511) `apps/web/server/repositories/__tests__/membershipsRepository.test.ts` — `makeListChain` mock was gated on `chain.not.mock.calls.length >= 2`; a 3rd `.not()` filter would silently stall it. Restructured to mirror the project's existing `makeUpdateChain` thenable pattern (same file, line 34) and Supabase's real PromiseLike contract. All filters now `vi.fn` for future direct call-arg assertions. 11/11 tests pass under new mock.

**Follow-up seeds in §1:** TD-139 (PDF export should also render `payload.note` / `payload.notes` — many journal entries write there instead of `payload.text`, currently rendered as silent blanks) and TD-140 (memberships test should assert filter contracts now that the thenable mock makes it possible) — both surfaced by Opus pre-merge review as Should-fix items.

### 2026-05-15 — Journal & meds quick-win trio (UX-041/042/043) — PRs #505/#506/#507
Sprint `journal-meds-quick-wins` (`/sprint` full pipeline) shipped three small, file-disjoint, user-facing fixes to journal + medication surfaces via direct-implementation wave (single orchestrator session, no parallel-dispatch). Opus adversarial gate was degraded for the planning phase (`/opus-on-opus` 529s); fell back to Sonnet reviewer per CLAUDE.md Opus-fallback rule across 3 review cycles (Must-fix: 2 → 1 → 0). Opus recovered post-merge per status.claude.com.
✅ **UX-041** (PR #507) `apps/web/app/(app)/journal/[recipientId]/JournalTimeline.tsx` — `JournalCard` now renders author display name (Geist 500 11px ink) above the timestamp eyebrow. `members.find(m => m.user_id === event.actor_id)?.display_name ?? "Former member"`. Added `actor_id` field to `JournalEvent` type across 4 of 5 duplicate copies (5th deferred to **TD-138**); threaded `members` prop through `JournalLayout → JournalTimeline → JournalCard`. Restores PRODUCT.md Principle 4 ("lead with names and voices before metrics").
✅ **UX-042** (PR #505) `apps/web/app/(app)/journal/[recipientId]/JournalLayout.tsx:177` — top-bar title now reads `currentRecipient?.display_name ?? org?.name ?? "Care Journal"`. Element preserved as `<span>` (no `<h1>` swap — would breach WCAG 4.1.2). Eyebrow restructure dropped (existing `{sectionLabel}` already covers desktop; mobile gain was illusory under `hidden sm:inline`).
✅ **UX-043** (PR #506) `apps/web/app/(app)/journal/[recipientId]/MedicationChecklist.tsx` — imports `formatClockTime` from `lib/format.ts` so schedule rows render `"Lisinopril 10mg — 8:00a"` instead of `"Lisinopril 10mg — 08:00:00"`. Raw Tailwind green/red utilities swapped for `--color-success-subtle` / `--color-surface-muted` design tokens per `.claude/rules/ui-standards.md`. Missed-button hover de-escalated from punitive red to neutral muted state (logging a missed dose is a neutral act).

**Follow-up seeds in §1:** TD-137 (JournalCard `members.find()` perf hoist) and TD-138 (5-way `JournalEvent` type consolidation) — both surfaced by `/oop --from-sprint` review at `.claude/state/oop-review-journal-meds-quick-wins.md` and seeded via PR #508.

**Hotfix this session:** PR #504 — `apps/web/app/(app)/page.tsx` (created by yesterday's TD-135 subagent that misread an in-existence "modify" instruction) caused a Next.js parallel-route collision with `(marketing)/page.tsx`. Deleted the invented file; main went green. Drove a CLAUDE.md retro entry for "verify owned-file paths actually exist before locking the plan."

### 2026-05-14 PM — Pre-staging mitigations (TD-134/135/136 + SEC-009 runbook guidance) — PRs #497/#498/#499/#500
Sprint `pre-staging-mitigations` (`/sprint` full pipeline) shipped the three pre-staging-tier safety nets seeded earlier in the day from the SEC-001 post-mortem, plus the operator-facing runbook update for SEC-009. `/wave §3b` parallel dispatch — 4 file-disjoint Sonnet tracks. Opus adversarial review was degraded per status.claude.com (529s) so /opus-on-opus fell back to Sonnet reviewer per /sprint Step 5 fallback policy; three review cycles (1 must-fix → 1 must-fix → 0 must-fix) cleared the plan before dispatch.
✅ **TD-134** (PR #500) `scripts/migration-check.sh` + root `package.json` `migration-check` script + 1-line bullet under `.claude/CLAUDE.md` Development Workflow. Subagent also compressed `.claude/CLAUDE.md` 227→156 lines to stay under the global 200-line cap rule (additive bullet would have breached); content preserved.
✅ **TD-135** (PR #499) `apps/web/lib/featureFlags.ts` server helper with **parameter-less `getFeatureFlag(flag)` signature** — distinctId derived internally from `supabase.auth.getUser()` (ADR-0001 anonymous UUID), no PHI parameter surface; `apps/web/hooks/useFeatureFlag.ts` client hook wrapping `useFeatureFlagEnabled ?? false`; `bootstrap: { featureFlags: {} }` added to `PostHogInit`; reference call site in `apps/web/app/(app)/page.tsx` (invisible data-attribute); ADR-0004 at 74 lines.
✅ **TD-136** (PR #497) `.github/workflows/migration-lint.yml` path-filtered on `supabase/migrations/**.sql` + `scripts/migration-lint.sh` statement-collapsing normalizer (awk strips `--` comments, `tr ';' '\n'` splits statements, second awk collapses whitespace) catching 3 risky patterns: `ADD COLUMN ... NOT NULL` without DEFAULT, `CREATE INDEX` without CONCURRENTLY, `DROP COLUMN`. `-- safe-migration: <reason>` opt-out; multi-line DDL self-test confirmed working.
✅ **SEC-009 runbook guidance** (PR #498 — does NOT close SEC-009 itself) inserted new §9 "Populate Vercel's Development environment tier" + new §12 "Optional: enable the new safety nets" into `docs/project-info/runbooks/SECRETS_ROTATION-runbook.md`; renumbered existing §9 (Scrub git history) → §10 and §10 (Close out SEC-001) → §11; regenerated HTML.

**SEC-009 itself stays Ready** in §1 — operator UI work in Vercel dashboard. T4 produced the guidance; the operator closes the row by walking runbook §9. Housekeeping PR #501 added ADR-0004 to `docs/adr/README.md` index.

### 2026-05-14 PM — OWASP audit followups (SEC-004/006 + TD-132) — PRs #491/#492/#493
Sprint `owasp-sec-followups` (`/sprint` full pipeline) closed the three deferred OWASP audit rows via `/wave §3b` parallel dispatch (Sonnet, 3 file-disjoint tracks). T3 (TD-132) pivoted from the audit's literal write-site cap to a DB-level CHECK constraint after comprehensive grep proved `ai_conversations` has no app-code INSERT/UPDATE site — pivot evidence embedded in the plan. Two `/opus-on-opus` passes: pass-1 1 must-fix + 3 should-fix, pass-2 cleared with the must-fix REJECTED-WITH-EVIDENCE after the grep was reproduced.
✅ **SEC-004** (PR #493) `apps/web/lib/ocrSanitize.ts` helper: 8KB byte-cap (codepoint-safe `TextEncoder`/`TextDecoder` truncation), `DRUG_NAME_PATTERN` allowlist, `sanitizeOcrFields` returning `{ fields, sanitized }`. Wired `capRawOcrText` into BOTH OCR Inngest stubs, `sanitizeOcrFields` into prescription only — closes audit FIND-003 parts 1+2 (part 3 audit-log site spun out as **SEC-007** below).
✅ **SEC-006** (PR #492) Migration `20260515010000_share_token_entropy_256.sql` (or near) bumping `outer_circle_requests.share_token` and `care_briefs.share_token` DEFAULTs to 256-bit (`gen_random_bytes(32)`). Existing tokens preserved; rotation deferred to **SEC-008**. Closes audit FIND-005.
✅ **TD-132** (PR #491) Migration `20260515020000_ai_conversations_messages_cap.sql` adds `CHECK (array_length(messages, 1) IS NULL OR array_length(messages, 1) <= 50)` with 4-arg `throws_ok` pgTAP coverage (50 ok, 51 raises `23514`). Archival job deferred to **TD-133** until a write site materializes. Closes audit FIND-007 cap-half.

**Follow-up seeds in §1:** SEC-007 (OCR audit log) / TD-133 (ai_conv archival) / SEC-008 (existing-token rotation) — pre-committed in the plan's §"Follow-up backlog rows" to trace the partial-closure carve-outs.

### 2026-05-14 — OWASP security wave (SEC-002/003/005 + TD-131) — PRs #486/#487/#488/#489
Sprint `owasp-sec-wave` (`/sprint` full pipeline) closed the highest-leverage findings from the morning's OWASP audit (`docs/security/2026-05-14-owasp-audit.md`) via `/wave §3b` parallel dispatch (Sonnet, 4 file-disjoint tracks). Pre-merge Sonnet adversarial gates ran on each PR; T3 (SEC-005) had 2 must-fixes patched by orchestrator (`thread_id`→`org_id` rename + integration test asserting count-only payload and no raw matchedKeys leak to Sentry/PostHog).
✅ **SEC-002** (PR #488) Stripe webhook `stripe_events` event-ID dedup table + `upsert ignoreDuplicates` dispatcher short-circuit + 4-arg `throws_ok` pgTAP — closes FIND-001 (replay protection).
✅ **SEC-003** (PR #489) `rateLimit(request, "brief/share")` mirroring sibling revoke endpoint — closes FIND-002.
✅ **SEC-005** (PR #487) `detectPhiSlip` post-filter + Sentry/PostHog signals on PHI-name reappearance in LLM output; UUID + counts only per ADR-0001; ESLint `carelog/no-phi-in-analytics` clean — closes FIND-004.
✅ **TD-131** (PR #486) `// SECURITY:` inline comment on `AIChatThread.tsx` `{msg.content}` render site + new `carelog/no-html-in-ai` ESLint rule scoped to `components/ai/` (forbids `dangerouslySetInnerHTML` + markdown-renderer imports) — closes FIND-006.

**Deferred to next chunk:** SEC-004 (OCR scaffold), SEC-006 (token entropy normalize), TD-132 (`ai_conversations` growth cap) — explicit non-goals of the just-shipped chunk per plan §"Non-goals".

### 2026-05-14 — OOP refactor Waves B/C/D (10 stories) — PRs #474..#483
Front-loaded combined plan (`docs/plans/2026-05-14-oop-waves-bcd.md`) shipped the 10 surviving OOP stories from the re-grounded Phase 2 audit (#472) across three sequential `/wave` dispatches. All invariant-preserving — no external behavior changes; full vitest + jest suites green at each merge.
✅ **OOP-004** (PR #475) OCR job state machine + optimistic-lock concurrency — extracted `OcrJobStateMachine.transitionTo()`; routes do `.eq("status", currentStatus)` and return 409 on race.
✅ **OOP-006** (PR #476) Brief headline classifier → strategy chain — 8 `HeadlineStrategy` classes; `headline.ts` 223→122 lines (helpers retained).
✅ **OOP-008** (PR #474) `care_briefs.recipient_id` BEFORE UPDATE trigger with `IS DISTINCT FROM` + 4-arg `throws_ok` + `SET LOCAL ROLE service_role` discipline test.
✅ **OOP-009** (PR #481) `PayloadMutator` interface + 3 per-kind mutators under `apps/mobile/lib/offlineMutators/`; `useOfflineWrite.ts` composes via registry.
✅ **OOP-010** (PR #479) Mobile `SecureStore` key registry — `KEYS` const with `v1:carelog:` namespace + per-key `migratedGet` fallback to preserve existing device state.
✅ **OOP-011** (PR #480) `useMutationWithRefresh` composable — 6 of 7 named screens refactored; `schedule/index.tsx` skipped per allowlist discipline (fully-divergent pattern).
✅ **OOP-012** (PR #478) `NameResolver` injection on `buildHandoffSummary()` — `CachedNameResolver` ref impl; snapshot test commits FIRST per push-early protocol.
✅ **OOP-013** (PR #482) `NotificationRouter` registry with Zod `NotificationPayload` schema; root dispatcher in `_layout.tsx` preserves OCR routing bit-for-bit. (Orchestrator pushed missing-barrel-export typecheck fix.)
✅ **OOP-014** (PR #483) `SyncStatusManager` singleton context — single 2s timer + AppState pause/resume; `useSyncStatus()` becomes a thin context consumer. (Orchestrator rebased + pushed after D4 subagent terminated locally.)
✅ **OOP-015** (PR #477) Stripe webhook handler map (PR1 of 2). 4 per-event handlers under `apps/web/app/api/stripe/webhook/handlers/`; `route.ts` 124→47 lines. PostHog error path preserved; PR2 (Sentry routing) deferred.

**Wave A→D telemetry:** 13 PRs total across the day (#468/#469/#470 Wave A this morning, plus #466 dep refresh + #467 E2E bit-rot fix, plus the 10 here). Push-early-after-first-commit protocol (introduced before Wave C after Wave B's 2-of-3 context-exhaustion rate) became load-bearing — Waves C and D landed with orchestrator code-takeover only on a typecheck fix (D3) and a rebase handoff (D4 subagent terminated post-commit).

### 2026-05-14 — OOP wave A (OOP-001/002/005) + TD-128 dep refresh + E2E bit-rot fix — PRs #466/#467/#468/#469/#470
Sprint `oop-wave-a` (`/sprint` full pipeline) shipped three invariant-preserving refactors via `/wave §3b` parallel dispatch (Sonnet, file-disjoint). Preceded by an emergency dep + e2e cascade: TD-128 next 16.2.6 + pnpm overrides (PR #466) cleared 14 high vulns; PR #467 fixed expense E2E date bit-rot (hardcoded `incurred_at: "2026-04-13"` fell outside ExpensePanel's 30-day rolling window on 2026-05-14) via relative-date sweep across `e2e/expenses.spec.ts` + `e2e/brief.spec.ts`. Sonnet adversarial gates flagged two must-fixes that didn't land before auto-merge fired → captured as TD-129/TD-130 in §1.
✅ **TD-128** Next 16.2.6 bump + protobufjs/opentelemetry pnpm overrides (PR #466) — cleared 14 high CVEs blocking OOP wave dep audit. Retroactive entry; no §1 row.
✅ **E2E bit-rot fix** (PR #467) — `expenses.spec.ts:61` + `brief.spec.ts` ISO timestamps swapped from hardcoded to `new Date().toISOString().slice(0, 10)`-derived. No backlog row; defensive sweep.
✅ **OOP-002** Enable RLS + pgTAP on `journal_reactions` (PR #468) — migration enables RLS with SELECT/INSERT/UPDATE policies (team via event→recipient join, `user_id = auth.uid()` + team) + pgTAP coverage. No app-behavior change.
✅ **OOP-001** pgTAP for `invite_tokens` atomicity + expiry (PR #469) — `supabase/tests/invite_tokens_rls.test.sql` covers 6 cases (valid/expired/email-mismatch/already-consumed/concurrent-accept/post-accept membership state). Adversarial-gate gap: missing `SET LOCAL ROLE service_role` discipline → TD-129.
✅ **OOP-005** `CareEvent` payload discriminated union + type guards (PR #470) — new `apps/web/lib/careEvent.ts` exports `MedicationEvent|JournalEvent|SymptomEvent|AppointmentEvent|ShiftEvent|ExpenseEvent|HandoffEvent` + `isXEvent()` guards; refactored 6 consumer sites. Adversarial-gate gap: `pdf/route.tsx` lost `typeof string` narrowing on `payload.text` → TD-130.

### 2026-05-10 PM-4 — Local-dev followups (TD-124/125/127) — PRs #458/#459/#460
Sprint `2026-05-10-pm-followups` shipped 3 of 4 planned tracks; T3 (TD-126) dropped after investigation showed the LCP premise was wrong. Wave executed direct mode after Opus-on-opus review.
✅ **TD-124** `supabase/seed.sql` end-to-end fix — config.toml flag + identity_vault precedence + SELECT-or-INSERT on auth.users (no email unique constraint) + drop ON CONFLICT on memberships (user_id nullable) (PR #458)
✅ **TD-125** Gate Sentry event-sending in dev via `enabled:` flag — silences Hobby-tier 429 storm; SENTRY_FORCE_ENABLED=true escape hatch (PR #459)
✅ **TD-127** ADR-0003 `needs-phi-review` label removal protocol — Path A human review / Path B narrow orchestrator self-clear (PR #460)
🧊 **TD-126** deferred — chrome-devtools performance trace measured local LCP at 132ms; HeroSection has no `<Image>`; hero-4.png is below the fold in HowItWorks (priority prop would hurt, not help).

### 2026-05-10 PM-3 — Recipient profile data wiring wave (UX-103/104/105) — PRs #451/#452/#453
Three sequential tracks all shipped this evening. Each component shell was previously scaffolded (UX-094) and rendered with empty/static props; this wave wired them to real data — no new schema (jsonb columns already existed).
✅ **UX-103** Wire CareTeamList — `getCareTeamForRecipient` in membershipsRepository (auth.admin.getUserById per member, briefs.ts:232 precedent) (PR #451)
✅ **UX-104** Wire LikesDislikesList — `getRecipientPreferences` in new recipientsRepository, session-scoped supabase client + Zod permissive parse defaults (PR #452)
✅ **UX-105** Wire EmergencyFooterCard — pure `parseEmergencyInfo` in identityRepository parsing already-resolved contact_info (no extra DB hop, identical PHI gate) (PR #453)

Sonnet adversarial gate caught 1 must-fix + 2 should-fix on the cumulative diff: T1 lacked defense-in-depth on `supabaseAdmin` query; T1 was N+1 on member-name resolution; T1 mock was fragile. Must-fix applied to T2 (now uses session-scoped supabase). T1 follow-ups filed as TD-120/121/122. Edit affordance for all 3 panels filed as UX-103b/104b/105b (out of scope per plan non-goals).

### 2026-05-10 PM-2 — Stale-Ready row reconcile (no code change)
Surfaced during /sprint planning: 6 Ready rows referenced files that already exist on main from earlier PRs. The /backlog-sync skill's §3a Stale-Ready detector wasn't run during the prior sync, so the rows persisted incorrectly. No new work — purely a status reconciliation.
✅ **TD-112** `/backlog-sync` skill: stale-Ready detector — `~/.claude/skills/backlog-sync/SKILL.md` §3a already in place (shipped 2026-05-09, row stale)
✅ **TD-113** `careEventCommentsRepository.test.ts` — 332 lines, 21 tests covering listComments / insertComment / editComment / softDeleteComment / getFanoutTargets
✅ **TD-114** `medicationTaggingRepository.test.ts` — PR #418
✅ **TD-115** `shiftTradeRequestsRepository.test.ts` — 471 lines, 18 tests covering createRequest / respondToRequest / acceptRequest (happy + status mismatch + partial failure) / forceOverride / expireStaleRequests
✅ **TD-116** `moodEntries.test.ts` — 349 lines, 19 tests covering sparkline auth / membership gate / day-bucket math / MOOD_SCORE / latestMood
✅ **TD-117** Automated PHI-invariant ESLint rule — PRs #433 + #436

### 2026-05-10 PM — Smoke-walk fixes + TD-118/119 admin-error hint sweep (PRs #438–#448)
Two waves on the same day. Wave 1 (smoke walk): 5 stories filed and shipped same-day after a manual UI walk-through. Wave 2 (A11Y-021 + TD-119 follow-ups): contrast sweep + `wrapAdminError` wiring across all admin call sites surfaced by TD-118.
✅ **A11Y-020** `/signin` CTA contrast — `--color-primary-pressed` for AA 5.04:1 (PR #438)
✅ **UX-114** Drop duplicate `/about` "Start your family's log" CTA after UX-112 (PR #439)
✅ **UX-115** Subordinate dashboard AI Assistant FAB via outline treatment to disambiguate from QuickLog (PR #440)
✅ **UX-113** Re-frame HowItWorks landscape hero — desktop crop fix + `aspect-[4/3]` (PRs #441, #443)
✅ **TD-118** Runtime guard for `SUPABASE_SERVICE_ROLE_KEY` misconfig — JWT shape sanity-check at first `supabaseAdmin` use, throws in dev / logs CRITICAL in preview+prod; introduces opt-in `wrapAdminError(error)` helper (PR #442)
✅ **A11Y-021** Sweep 28 white-on-`--color-primary` CTAs across app + marketing — body-text CTAs to `--color-primary-pressed`; 3 deliberate keeps annotated with WCAG large-text justification (PR #446)
✅ **TD-119** Wire `wrapAdminError()` into all admin call sites — 47 sites across 17 files (10 tRPC routers + 3 API route handlers, both initial 13-site + 34-site expansion). TRPCError sites hoist `wrapAdminError(error).message` to `message`, pass original PostgrestError as `cause` (server-only, preserved for Sentry; tRPC's `getDefaultErrorShape` doesn't include `cause`) (PRs #447, #448)

### 2026-05-10 — Wave 14 Part A — visit recorder triage
Read-only investigation per `docs/plans/wave-14-test-gaps-and-visit-recorder.md`. Decision: schema-ahead-of-UI **by design** per PR #348. Updated ROADMAP Phase 4 visit-recorder paragraph to document the design rationale; corrected ON-55 §5 row to reflect "data-model shipped, UI deferred" instead of "Phase 7 / future." No new ON-NN row needed — ON-55 already covers the UI surface.
✅ **UX-107 (triage)** closed; row dropped from §1.

### 2026-05-09 PM — Wave 13-A + retroactive TD-78..82 reconcile (PRs #396–#398)
Wave 13-A intent was UX-045 (PHI gate) + UX-051 (brand cleanup); UX-051 deferred mid-wave when DNS blocker surfaced. Wave 13-B (TD-78..82) aborted at preflight: all 5 test files already on main from earlier sprints — backlog rows were stale.
✅ **UX-045** Redact `content.dob` from `/api/brief/[shareToken]` unless `includes` opts in — fail-closed (PR #396)
✅ **fix(test)** ExpensePanel fixture relative-date — date-rollover flake hit every PR opened 2026-05-10 (PR #397)
✅ **fix(e2e)** shift-calendar helper drop racy conditional — `count()` doesn't auto-wait, ShiftsPanel layout-toggle Calendar tab needs a render cycle (PR #398)
✅ **TD-78..82** retroactively flipped to ✅ Shipped — files `apps/web/server/routers/__tests__/user.test.ts` (#248, 21 tests), `careEventsRepository.test.ts` (#249, 10 tests), `lib/__tests__/stripe.test.ts` (#250, 3 tests), `organizationsRepository.test.ts` (#253, 11 tests), `supabase/tests/care_events_client_id.test.sql` (#258, pgTAP) — coverage matches the original spec.
🧊 **UX-051** deferred — caresync.app inbox (`hello@`/`privacy@`) not yet provisioned; legacy `care-log.org` strings stay until DNS is set up.

### 2026-05-09 PM — Roadmap & harness audit remediation (PRs #387–#394)
Source: `docs/audits/2026-05-09-roadmap-and-harness-audit.md` + `docs/plans/audit-remediation-2026-05-09.md`. 8 chore PRs, ~3 hr active work.
✅ **A.1+A.2** Fixed 9 stale `Documents/projects/carelog` hook paths + worktree-cwd commit guard — restored silently-broken pgTAP-on-edit + mobile-typecheck regression nets (PR #387)
✅ **A.3+A.4** Backlog/roadmap stale-reference sweep (`/carezone-alternative` consolidated; ROADMAP "Weekly digest" was actually shipped) + ON-69/UX-066 strikes (PR #388)
✅ **C.1+C.2** Scoped tsc/eslint hooks to `apps/web` edits + dedupe allowlist 27→18 (PR #389)
✅ **D.3** Pre-commit eslint pass — catches React 19 react-hooks/purity errors locally (PR #390)
✅ **B.1+B.2** Resolved 4 stale Ready rows (UX-035 struck, UX-053 narrowed, SEO-006 → 🧑, TD-87 → 🟡 Spike) + filed 5 ROADMAP-promised rows ON-70..74 (PR #391)
✅ **D.4** /dispatch — early-push guideline + post-dispatch PR verifier (PR #392)
✅ **C.4** Split `.claude/CLAUDE.md` 412→233 lines (−43%); 189 lines moved to new `HARNESS_USAGE.md` runbook (PR #393)
✅ **C.3 phase 1** Deleted 5 byte-identical project skill shadows (project skills 26→21) (PR #394)
B.3 (memory hygiene) shipped out-of-band — repo-external. Plus Wave 12 Session A: SEO-001/002/004 shipped via PR #386 (rows promoted in this sync).

### 2026-05-09 PM — Wave 10 Auth flow hardening (PR #383)
Bundled four auth/onboarding stories into one PR — overlapping file scope (signin/page.tsx, layout.tsx, marketing chrome).
✅ **TD-108** Multi-org caregiver: replaced `.single()` with earliest-accepted-membership pick in `(app)/layout.tsx`
✅ **TD-109** `/signin` redirects authed visitors to `/dashboard` before render
✅ **UX-107** `/signin` + `/onboarding` h1s migrated to `.headline-display` Fraunces italic-em pattern
✅ **UX-108** New `MarketingNavSlim` brand-mark nav mounted on `/signin` + `/onboarding`

### 2026-05-09 — Wave 9 Shifts data plumbing (PRs #372–#379)
Closes the "Handoff / Week / Team / Questions" empty states in ShiftsPanel and finalizes the dashboard brief surface. UX-101/102 each split into multiple PRs at adversarial-review request to keep schema and wiring atomic.
✅ **UX-095 finalize** Brief surface wired to live adapters via new `briefs.dashboardSummary` tRPC + `<BriefSection>` component (PR #372)
✅ **chore(security)** Pin axios ≥1.15.1 + fast-uri ≥3.1.2 overrides; restore TD-29 warn-only on OSV/Trivy (PR #373)
✅ **UX-100** `buildShiftWeekGridBlocks` adapter — ShiftsPanel "Week" tab renders real blocks (PR #374)
✅ **UX-101a** `shifts.handoff_entries jsonb` column + 9 pgTAP tests + types regen (PR #375)
✅ **UX-101b** `shifts.upsertHandoff/getLatestHandoff` tRPC + ShiftsPanel "Handoff" tab wire + 12 logic tests (PR #376)
✅ **UX-101c** `<ShiftQuoteNote>` mount on dashboard via `getLatestHandoff` (PR #377)
✅ **UX-102a** `shift_questions` table + RLS + immutability trigger + 15 pgTAP tests (PR #378)
✅ **UX-102b** `shiftQuestions.list/create/resolve` tRPC + ShiftsPanel "Questions" tab composer + resolve flow + 10 logic tests (PR #379)

### 2026-05-08 — CareSync handoff follow-ups Wave 8 (PRs #367–#371)
✅ **UX-095** Brief surface mount — SleepSparkline + ComingUpRows + ShiftQuoteNote + OnShiftSidebar + PatternCard wired into DashboardClient (PR #368)
✅ **UX-096** `sleepFromEvents` adapter — 7-night sleep buckets from `care_events` (PR #370)
✅ **UX-097** `comingUpEvents` adapter — next ≤5 scheduled events for `<ComingUpRows>` (PR #369)
✅ **UX-098** `deriveOnShift` adapter — current/next caregiver + latest mood join (PR #369)
✅ **UX-099** `detectPattern` helper — threshold-driven 7d pattern signal for `<PatternCard>` (PR #370)

### 2026-05-01 PM — CareSync handoff (frozen design) (PRs #349–#365)
Source: `docs/caresync-handoff/`. 28 stories shipped in one session. Brand decision: Sage default; `hearth` + `slate` retired from runtime. UX-077 (Today route default flip) deferred — needs product decision.
✅ **UX-067** Flip default theme to Sage; retire `hearth`/`slate` runtime alternates (PR #351)
✅ **UX-068a** SageRail — dark `--app-shell` sidebar w/ brand mark, sections, recipient footer (PR #351)
✅ **UX-068b** SageTopBar — crumb + title + search + ⌘K chip + action slot (PR #350)
✅ **UX-068c** Mount SageRail + SageTopBar into `(app)` layout behind `?shell=sage` opt-in (PR #352)
✅ **UX-070** BriefHero default flip — bold display headline; italic via `?headline=italic` alt (PR #356)
✅ **UX-071** SleepSparkline — 7-day inline SVG sparkline + plain-language numbers (PR #353)
✅ **UX-072** ShiftQuoteNote — left-rule indented blockquote for previous-shift voice (PR #354)
✅ **UX-073** ComingUpRows — clean rows of next 4–5 events (PR #354)
✅ **UX-074** OnShiftSidebar + PatternCard — right-rail brief callouts (PR #355)
✅ **UX-075** TimeRailTimeline — vertical rail + chronological events + NOW pill + type icons (PR #361)
✅ **UX-076** TimelineFilterChips — multi-select chip toolbar (PR #361)
✅ **UX-078** MedAttentionHero — full-width hero card for missed-dose state w/ catch-up CTA (PR #358)
✅ **UX-079** RxGlyph — Fraunces italic ℞ component (PR #358)
✅ **UX-080** MedStatusBadge — On track / Catch up / Missed pill (PR #358)
✅ **UX-081** Mount MedAttentionHero + RxGlyph + MedStatusBadge into MedCard (PR #359)
✅ **UX-082** NarrativeHandoff — "Three things you need to know" composer + view (PR #363)
✅ **UX-083** ShiftWeekGrid — Mon–Sun × hours grid w/ per-person color blocks (PR #363)
✅ **UX-084** ShiftTeamList — plain stacked rows; replaces TeamNowBoard default (PR #363)
✅ **UX-085** OpenQuestionsCard — inbox-like callout below handoff (PR #363)
✅ **UX-086** Shifts route default flips — Narrative + WeekGrid + TeamList tabs default; legacy variants behind tabs (PR #365)
✅ **UX-087** JournalEntryForm inline default — confirmed no-op; default mode was already `"standard"` (inline composer + 3 mood badges)
✅ **UX-088** WeeklyMoodBars — bar chart of weekly mood distribution + top tags (PR #360)
✅ **UX-089** FridayExportHint — footer card explaining therapist Friday email (PR #360)
✅ **UX-090** Journal route default flips — WeeklyMoodBars default sidebar; MoodHeatmap behind `?sidebar=heatmap`; FridayExportHint mounted (PR #364)
✅ **UX-091** LikesDislikesList — side-by-side bulleted lists (PR #357)
✅ **UX-092** CareTeamList — stacked rows w/ phone + role (PR #357)
✅ **UX-093** EmergencyFooterCard — DNR / primary contact / hospital preference (PR #362)
✅ **UX-094** Profile route mount — composes RecipientProfile + LikesDislikesList + CareTeamList + EmergencyFooterCard (PR #364)

### 2026-05-01 — Plan C CareSync 2.0 wiring + journal pagination (PRs #336–#343)
✅ **TD-100** Journal timeline cursor pagination — `before`/`limit` on `/api/journal`, `loadMore` on `useJournalData`, IntersectionObserver sentinel + Load older button (PR #336)
✅ **UX-061** Wire `<MedScheduleStrip>` + `<AdherenceChart>` into MedCard — new `lib/medAdherenceFromEvents.ts` adapter + `medications.weekData` tRPC; bundles fix to `listScheduled` (`time_of_day` alias, drop nonexistent `org_id`) (PR #337)
✅ **UX-062** Shifts layout toggle (Calendar / Lanes / Now) — new `ShiftsPanel` + `lib/shiftLayouts.ts`; Briefing deferred to UX-065 (PR #339)
✅ **UX-063** Mount `<MoodHeatmap>` in JournalLayout sidebar — 2-col grid `lg:` breakpoint, sticky right rail (PR #338)
✅ **UX-064** `/recipient/[recipientId]/profile` route + journal Profile link — server-resolved identity via `identityRepository.resolveIdentity`; mood/caregivers/About deferred to UX-066 (PR #340)
✅ **TD-106** Fix `e2e/export.spec.ts:81` toast assertion drift after TD-96 — case-insensitive regex on stable substring (PR #343)

### 2026-04-30 PM — Plan B web hardening + Tier 1 testing (PRs #328–#334)
✅ **TD-77** `identityRepository` cross-org / malformed / expired token tests (PR #328)
✅ **TD-98** Truncation pass on long-text card spans — `min-w-0 truncate` on MedCard / RecipientHeader / ShiftEventCard / JournalTimeline (PR #334)
✅ **TD-101** RTL smoke test — logical-direction utility regression net (PR #333)
✅ **TD-102** Explicit `mutations.retry: 0` in TrpcProvider (PR #329)
✅ **TD-103** `useDebouncedValue` hook + journal/messages search wiring (PR #331)
✅ **TD-104** `pluralize()` helper using `Intl.PluralRules` + 5+ site migration (PR #330)
✅ **A11Y-019** SR-only `LiveRegion` for offline-queue + optimistic-update transitions (PR #332)

### 2026-04-30 — CareSync 2.0 design handoff (PRs #320–#326)
✅ **UX-054** Sage parlor palette + ThemeSwitcher (`data-theme="sage|hearth|slate"` + dark) (PR #320)
✅ **UX-055** Card header variants — outline / accent left-bar / serif italic (PR #321)
✅ **UX-056** Today "Now Board" timeline layout (PR #323)
✅ **UX-057** MedCard day-strip + 7-day adherence chart (presentational) (PR #325)
✅ **UX-058** Shifts BriefingHandoff + ShiftLanes + TeamNowBoard (presentational) (PR #326)
✅ **UX-059** Journal prompted composer + mood spectrum + heatmap (PR #324)
✅ **UX-060** RecipientProfile card (presentational) (PR #322)

### 2026-04-29 — /impeccable wave 2 (clarify) + extract Phase B-1 (PRs #284–#297)
✅ **UX-037** Marketing microcopy — three category-default phrases fixed (PR #284)
✅ **UX-038** `CompareTable` surfaced on landing + about (PR #285)
✅ **UX-039** Recipient-led dashboard shape doc (spike) (PR #283)
✅ **UX-039a** Recipient-led layout A + ReferralCard moved to Settings (PR #287)
✅ **UX-039b** Multi-recipient switcher wired + layout B + view toggle (PR #289)
⚫ **UX-040** Founder decision: keep generated-brief model — no code work (resolved)
✅ **UX-047** Errors + confirmations sweep — 18 strings rewritten, 3 native confirms → `<AlertDialog>`, new `ui/alert-dialog.tsx` primitive (PR #290)
✅ **UX-052** AlertDialog migration follow-up — AppTabBar sign-out + TeamPanel member-remove (PR #293)
✅ **TD-90** `<ErrorBanner>` extracted + raw `bg-red-50` token leaks fixed (PR #295)
✅ **TD-91** `lib/mood.ts` widened with `moodBgClass`/`moodBorderClass`/`moodChipClass`/`moodDotClass` helpers (PR #297)
✅ **TD-92** `<TintedCard>` widened with dark-mode `tone="dark"` prop; `TradeRequestList` bypass removed (PR #296)
✅ **TD-93** `lib/pdfTokens.ts` parallel-token file for react-pdf + parity test (PR #294)

### 2026-04-28 — Agent tooling experiments + Codex re-audit (PRs #262–#266)
✅ **TD-84** Codex adversarial re-audit on apps/web/server + supabase/migrations + apps/web/inngest (16 Critical / 36 Medium) (PR #262)
✅ **TD-85** Tool-use PR-review agent → `/pr-review-agent` skill (PRs #264, #266)
✅ **TD-86** Sentry-issue triage agent → `/sentry-triage` skill (PRs #265, #266)

### 2026-04-27 — Launch-readiness wave (PRs #224–#228)
✅ **A11Y-012/013/014/016/017** Button `type="button"` + member-identifying `aria-label` sweep across JournalTimeline, TeamAdmin (Remove + delete-org), QuickLogFab, JournalLayout (display_name fallback) (PR #224)
✅ **A11Y-015** AppTabBar mobile tab strip `type="button"` (desktop in #224, mobile in #228)
✅ **UX-025/026/027/028/029/030/031/032/033/034/036** Token-drift sweep — `lib/mood.ts` extracted; `--color-danger-subtle` + `--color-success-subtle` + `--color-warning-subtle` tokens added; QuickLogFab/RoleBadge/ExpensePanel/DocumentVault/MedicationPanel/EolPlanner/ErrorBoundary/DashboardClient/CommentItem/CommentThread tokenized (PR #224)
✅ **LAUNCH-002** EAS production build profile + OTA gating + `MOBILE_RELEASE.md` runbook (PR #225)
✅ **LAUNCH-003** Web SEO/OG meta + `sitemap.ts` + `robots.ts` + JSON-LD structured data (PR #226)
✅ **TD-73/74/75** Rate-limit dashboard + digest delivery monitoring + E2E green-streak gate (PR #227)

### 2026-04-26 — E2E unblock + product polish wave (PRs #175–#205)
✅ **TD-38** Update dispatch skills for ~~Mergify~~ queue trigger — drop `--auto --squash`, reach for the `queue` label (PR #175)
✅ **TD-39** Harden `e2e/helpers.ts` — selector ambiguity, OTP regex, auth-callback timeout (PR #176)
✅ **TD-40** E2E AI Assistant FAB needs `ensureCareTeam` fixture — pre-create team in beforeEach (PR #177)
✅ **TD-41** Guard PostHog calls when key is unset — server stub + client `opt_out_capturing()`; bundles Next 16 hydration fix (PR #178)
✅ **TD-42** `ensureCareTeam` selector drift — dashboard "View care journal" rendered as `<p>` inside clickable `<Card>`, not `<button>` (PR #180)
✅ **TD-43** Defensively wrap `posthog.capture`/`identify` so analytics never blocks UX (PR #181)
✅ **TD-44** Rate-limit fail-closed only in real prod (`VERCEL_ENV`), not `NODE_ENV` — was bricking E2E in CI (PR #182)
✅ **TD-45** Bump `ensureCareTeam` post-onboarding `waitForURL` 15s → 30s for slow CI cold boot (PR #183)
✅ **TD-46** Diag: instrument `OnboardingForm` to surface CI E2E redirect mystery (PR #185)
✅ **TD-47** Bail E2E after first failure + upload trace on cancel (PR #186)
✅ **TD-35 / TD-48 / TD-50** Unblock E2E end-to-end — path-filter false-skip fix (TD-35), Onboarding redirect (TD-48), helpers cleanup (TD-50) (PR #187)
✅ **TD-49** Docs: drafted upstream `supabase/cli` JWT-rotation issue write-up (PR #191)
✅ **TD-53** Clear browser cookies in `signIn()` to fix second-call timeout (PR #189)
✅ **TD-55** Fix `benefits.latest` mock URL + response format; un-fixme test (PR #194)
✅ **TD-56** Query `display_names` PHI vault instead of `care_recipients.display_name` (PR #190)
✅ **TD-57** Un-fixme multi-context burnout tests + add step diagnostics (PR #193)
✅ **TD-58** Un-fixme comment-toggle E2E test (PR #192)
✅ **TD-63** Restore AI Assistant FAB visibility (PR #198)
✅ **TD-64** Unify remaining `care_recipients.display_name` callers with PHI vault pattern (PR #197)
✅ **TD-65** Restore brand copy + add sign-out confirmation (PR #196)
✅ **TD-66** Clean up Carelog brand string in user-visible copy (keep email domain) (PR #200)
✅ **TD-67** Dedup team-admin member row — show email once, prefer `display_name` (PR #199)
✅ **TD-68** Add success feedback after Generate shareable brief (PR #202)
✅ **TD-69** Add success feedback after burnout check-in submit (PR #201)
✅ **TD-70** Add success toasts to silent form submits across More panel (PR #205)
✅ **TD-71** Brand the brief expired/invalid empty state with CareSync logo + CTA (PR #203)
✅ **TD-72** Align coverage-settings E2E tests with current product state (PR #204)
✅ **ON-64** Care brief generation pipeline — generator at `apps/web/app/api/brief/route.ts` (de-tokenizes once, stores snapshot in `care_briefs`); Generate shareable brief + Copy link UI in `JournalLayout.tsx`; RLS pgTAP `care_briefs_rls.test.sql`; viewer at `/brief/[shareToken]/page.tsx`. Polish completed by TD-68 (PR #202) + TD-71 (PR #203).
✅ **ON-65** Medication refill alerts — Inngest nightly function `apps/web/inngest/functions/refillAlert.ts` shipped; idempotent per medication × week.
✅ **ON-66** Symptom tracker — `server/routers/symptoms.ts` (+ logic + security tests) reads/writes `symptom_readings` table (chose dedicated table over `care_events` enum extension); web `journal/[recipientId]/SymptomPanel.tsx`; mobile `app/(app)/symptoms/{index,log}.tsx` + a11y test.
✅ **ON-67** Burnout tracker (the differentiator) — `server/routers/burnout.ts` + logic/security tests; weekly Inngest `inngest/functions/burnoutAlert.ts`; check-in submit polish via TD-69 (PR #201).
✅ **PP-014** Mobile subscription page on tRPC — `trpc.billing.getSubscription.useQuery()` replaces hand-rolled REST fetch in `apps/mobile/app/(app)/subscription/index.tsx`; resolves the placeholder TODO from TD-22 (PR #210).
✅ **UX-21** Daily Brief magazine view — extracted `BriefEditorial.tsx` from `app/brief/[shareToken]/page.tsx`; `max-w-[720px]` article with `.headline-display` Fraunces 48 + `.eyebrow-mono` dateline + 5–8 body paragraphs sourced from `recent_entries` + doctor-bullet section + Email family / Print for visit actions; re-uses brief snapshot, no API change (PR #211).
✅ **ON-68** Document share-with-aide signed-URL workflow — `documents.createShareLink` mutation (coordinator-only, 1–168 hour expiry) wraps Supabase Storage `createSignedUrl`; per-row Share button + inline hours-selector + Copy link panel in `DocumentVault.tsx`; no schema change (PR #212).
✅ **Skills** `/live-test` skill — interactive flow investigation + E2E runbook (PR #188); hot-reload + capture-replay + screenshot modes (PR #195)
✅ **CI infra** ~~Mergify~~ `batch_max_wait_time` 5 min → 150 s for faster queue cycles (PR #184)


### 2026-04-25 PM — security + CI hardening + harness consolidation (PRs #158–#173)
✅ **TD-25** `supabaseServer` session-refresh unit test (PR #158)
✅ **TD-28** `messagingPush` + `educationTipRefresh` Inngest failure tests + `DeviceNotRegistered` fix (PR #160)
✅ **TD-29** Long-tail transitive vuln triage — `vite` → 8.0.10, `dompurify` → 3.4.1, `follow-redirects` → 1.16.0, `postcss` → 8.5.10, `hono` → 4.12.14, `uuid` → 14.0.0, `@tootallnate/once` → 3.x; deleted stale `apps/web/pnpm-lock.yaml` (root lockfile is canonical); `apps/mobile/package-lock.json` hand-patched (`workspace:*` blocks `npm install`); OSV/Trivy/pnpm-audit flipped to blocking (PR #165)
✅ **TD-32** Run E2E on PR pushes (PR #154)
✅ **TD-36** Migrated `apps/mobile` to pnpm workspace — deleted stale `package-lock.json`, added `.gitignore` rule, documented in `apps/mobile/CLAUDE.md` (PR #173)
✅ **CI infra** ~~Mergify~~ config: drop phantom `audit` check, switch to `CI Summary` aggregate, upgrade to current format, enable merge queue (PRs #166, #168, #171, #172)
✅ **Docs** CLAUDE.md auto-merge → ~~Mergify~~ queue workflow rewrite (PR #167)


### 2026-04-25 backlog burndown + harness consolidation (PRs #145–#161)
✅ **fix(security)** Drop share_token + error stack from PostHog — closes 2 PHI leak vectors (PR #145)
✅ **TD-24** care_events RLS pgTAP coverage — 15 tests covering coordinator/aide/outer-circle SELECT+INSERT isolation (PR #146)
✅ **TD-22** Billing tRPC router — `billing.getSubscription`, unblocks PP-014 (PR #147)
✅ **TD-21** CVE bumps — Next.js → 16.2.3, protobufjs → 8.0.1, @xmldom/xmldom → 0.9.10; long-tail triage deferred to TD-29 (PR #148)
✅ **TD-23** SHA-pin 5 workflow action refs + checksum OSV binary download (PR #148)
✅ **TD-30** Path-filtered required CI checks — story filed; workflow YAML implementation tracked separately (PR #149)
✅ **TD-31** Automated PHI review label gate workflow (PR #155)
✅ **TD-27** Aide cross-recipient tRPC scoping integration test (env-gated for CI; runs locally with `SUPABASE_INTEGRATION=1`) (PR #153)
✅ **vitest infra** Headless + flake-free by default (PR #157)
✅ **TD-26** useOfflineWrite retry/error branch coverage — closes the offline-sync coverage gap (PR #159)
✅ **TD-33** Document worktree-commit-hook + vitest yaml-flake gotchas in CLAUDE.md (PR #156)
✅ **TD-34** Consolidate /dispatch + /backlog-dispatch into one canonical skill (mirrors /wave shape); promote worktree-subagents to canonical primitive owning pre-flight + symlink-worktree + scope-contract (PR #161)


### 2026-04-23..24 UX-14..21 design-spec batch + CI rescue (PRs #124–#135)
✅ **UX-14** Command palette (⌘K) — modal with Jump / Log / Admin sections, fuzzy search, keyboard nav (PR #125)
✅ **UX-15** Quick-log FAB — bottom-right expandable with Meds/Mood/BP/Note; Meal+Hydration disabled (PR #126)
✅ **UX-16** Fraunces + Geist Mono type system (plumbing only) — `--font-display`/`--font-mono` tokens + 3 utility classes; no component refactors (PR #128)
✅ **UX-18** Patterns strip in Journal — pastel scroll row with 3 hardcoded mocks + `?filter=` routing (PR #127). Real aggregation deferred to UX-24.
✅ **UX-19** Shift Handoff "What did I miss?" view — 24/48/72h period selector, 5 sections (Meds/Moments/Appointments/Concerns/Thanks), 30 tests (PR #129)
✅ **UX-20** Print-friendly visit summary — 6-section printable `/visit-summary` route, `window.print()`, `lib/medAdherence.ts` helper (PR #130)
✅ **TD-14** Restore green lint — 491 → 0 errors via rule downgrade + 6 hand-fixes; contrast-script token-drift bug fixed (PR #132)
✅ **TD-15** CI infra: lockfile drift + workflow script typos — `pnpm typecheck` → `pnpm type-check`, coverage script relocated (PR #131)
✅ **TD-16** Web typecheck 147 → 0 across 2 PRs; CI now actually runs `cd apps/web && npx tsc --noEmit` instead of silently skipping (PRs #132, #134)
✅ **TD-17** Mobile Jest 0→33 suites green: Math.round in scaledFont, virtual expo-device mock, stale screen text + missing trpc mocks fixed, BottomSheet mocked in journal tests (PR #141)
✅ **Docs infra** CI_HEALTH.md runbook (GitHub billing / secrets / auto-merge / branch protection) + THIRD_PARTY_SETUP.md §13–§14 (PR #133)
✅ **Docs infra** Master SETUP.md + ENV_VARS.md + MOBILE_SETUP.md + DEPLOYMENT.md runbook set (PR #135)

### 2026-04-17 codebase survey batch (PRs #116–#120)
✅ **TD-13** CommentThread onError toasts — add/edit/remove mutations now surface errors via sonner (PR #116)
✅ **UX-13** AIPanel loading + error state — Loader2 spinner + onError toast (PR #117)
✅ **TD-12** Dialog + Label UI components — created shadcn/base-ui wrappers; unskipped tests (PR #118)
✅ **A11Y-011** Web aria-label sweep — all 4 targets already WCAG 2.2 AA compliant (PR #119)
✅ **UX-12** Empty states — AIChatThread + EntryDetailClient now show helpful empty/error UI (PR #120)

### Phase 1 — Cleanup (2026-04-07)
✅ P1-01 Display names · P1-02 Invite redirect · P1-03 Entry detail route
✅ **Journal reactions** — `journal_reactions` table + `careEvents.react` tRPC + `JournalTimeline` emoji row (❤️ 👍 💪 🙏) + `/api/journal/[eventId]/reactions` route. Implemented in Phase 1 work, not previously tracked in backlog.

### Phase 2 — Scheduler (2026-04-07)
✅ P2-01 Shift tRPC + schema · P2-02 Shift creation UI · P2-03 Shift list / caregiver view · P2-04 Coverage window UI · P2-05 Gap detector (Inngest) · P2-06 Recurring shifts · P2-07 Weekly digest shift section

### Phase 3 — Medical + Outer circle (2026-04-09)
✅ P3-01 Medication catalog · P3-02 Admin log · P3-03 OCR pipeline (Inngest) · P3-04 Refill alert · P3-05 Volunteer request board (public `/care/[token]`) · P3-06 Care brief (`/brief/[token]`)

### Phase 4 — Depth + retention (2026-04-10)
✅ P4-01 Symptom tracker · P4-02 Burnout tracker + Inngest alert · P4-03 Full history export (JSON + PDF)

### Phase 5 — Financial + legal (2026-04-13)
✅ P5-01 Shared expense log (`ExpensePanel`) · P5-02 Benefits navigator (`BenefitsNavigator` + eligibility lib) · P5-03 Document vault (`DocumentVault` + upload/download API + FTS migration) · P5-04 EOL planner (coordinator-only, linked to vault)

### Before-launch — Claude tasks
✅ B1 Sentry PII hardened (`sendDefaultPii: false`, env DSN, `sentry.client.config.ts` added)
✅ D2 `apps/web/lib/stripe.ts` (renamed from `stripe.server.ts`)
✅ D3 Subscription/plan migration (`20260416000000_superuser_plan.sql`)
✅ D4 Stripe webhook handler (`apps/web/app/api/stripe/webhook`) + checkout + portal + verify routes
✅ D5 Billing tRPC/subscriptions page (`apps/web/app/(app)/subscriptions/page.tsx`)
✅ D6 BillingBanner (soft-gate pattern)
✅ B3 PostHog provider + server helper (`apps/web/lib/posthog-server.ts`, dashboard view tracking)
✅ E2E specs: expenses, team-admin remove, outer-circle create, care-brief, eol-planner, benefits, contact, burnout privacy suppression, OCR review (all 2026-04-13)

### A11Y + infra (2026-04-13/14)
✅ `memberships.remove` + TeamPanel Remove button · last-coordinator guard migration · memberships delete policy · harden outer-circle + care-briefs RLS · secure prescription-images bucket · push_tokens table · user_profiles.email

### Security / RLS follow-ups (2026-04-16..20)
✅ superuser plan · harden outer_circle_requests RLS · memberships delete policy · documents FTS · last-coordinator guard

### 2026-04-16 mobile + web sprint (PRs #75, #85, #87–#97)
✅ **ON-44** Comment threads on care events — `care_event_comments` + RLS + tRPC + web CommentThread/CommentItem/CommentComposer + mobile CommentSection (PR #73)
✅ **ON-45** Shift trade requests — `shift_trade_requests` + RLS + tRPC router + Inngest cron + web/mobile UI (PR #74)
✅ **ON-46** Medication tagging + chip-filter bars + detail panels — junction tables + auto-tag + tRPC (PR #75)
✅ **A11Y-008** Extend `mobile-ui` skill with VoiceOver/TalkBack enable/disable + narrate workflow (PR #78)
✅ **PP-005** Web push notifications (browser Push API) (PR #85)
✅ **PP-002** Mobile onboarding wizard — welcome, care-recipient, invite-team screens (PR #92)
✅ **PP-003** Mobile subscription read-only view + "manage on web" CTA (PR #93)
✅ **PP-006** Android prebuild + boot verification — `apps/mobile/android/` committed + CI build job (PR #90)
✅ **TD-02** Dynamic Type + screen-reader audit — `scaledFont()` + `accessibilityLabel` sweep (PR #87)
✅ **ON-15** Mobile a11y audit (code complete; physical device VoiceOver deferred to human) — folded into TD-02
✅ **PP-011** Offline journal write-queue — IndexedDB + auto-sync on reconnect (PR #88)
✅ **UX-03** Micro-interactions — card hover lift, mood press, sidebar active, sonner toasts (PR #89)
✅ **TD-07** Alert → Toast sweep — 6 `alert()` calls replaced with sonner across 4 files (PR #94)
✅ **TD-08** Supabase types regen + `as any` cleanup — 10 casts removed (PR #95)
✅ **TD-09** ShiftList edit mode — `shifts.update` tRPC + inline edit panel (PR #96)
✅ **TD-10** JournalClient refactor — 3 custom hooks + JournalLayout; 624 → 107 lines (PR #97)
✅ **TD-06** Dark mode variants for Comment + TradeRequest components; WCAG contrast fix (PR #98)
✅ **PP-007** Android FCM push token registration + notifications tRPC router (PR #99)
✅ **PP-009** Android visual QA script — `scripts/android-visual-qa.sh` (11 routes, HTML diff report; run when emulator available)
✅ **PP-010** Android document-share intent — 17 unit tests covering `Platform.OS=android` Alert.alert picker path (2026-04-17)

### 2026-04-17 onboarding + product (PRs #101–#106)
✅ **ON-52** Care history depth counter on dashboard — "X care events over Y months" + `formatCareStats` helper + 6 unit tests (PR #101)
✅ **ON-56** Data stewardship commitment page `/trust` — 4 commitments + hero + footer link + responsive layout (PR #102)
✅ **ON-58** PostHog funnel events — `first_care_event_created`, `onboarding_step_completed`, `team_member_invited` (UUID-only, PHI-safe) (PR #103)
✅ **TD-11** `data-testid` sweep for MedicationPanel + MedicationChecklist (already implemented; no code change needed)
✅ **ON-57** Family referral share link — coordinator dashboard "Refer Carelog" button, `/signup?ref=<orgSlug>`, PostHog `referral_shared` UUID-only event (PR #105)
✅ **ON-50** Weekly digest medications section — `medDoseCount` query + `digestHtml` meds line + 3 new tests (singular/plural/zero) (PR #106)
✅ **ON-49** Shift completion handoff prompt — "Complete shift" button + inline handoff note textarea + `shifts.complete` tRPC proc (assignee OR coordinator) + 9 tests (PR #108)
✅ **ON-51** Aide recipient-scoping — recipient picker in TeamPanel invite form (role='aide' only); `useJournalData` loads org recipients; `handleInvite` overrides `recipientId`; 4 new tests (PR #109)
✅ **ON-60** Referrer resource page `/for-referrers` — hero, audience callout, 4 feature cards, 3-step how-to-refer with clipboard copy, trust signals, footer link; no commission language (PR #107)

### 2026-04-16 backlog sync (PRs #53–#74)
✅ **A11Y-005** vitest-axe assertions on Card, Button, Input, Label, Dialog (PR #59)
✅ **A11Y-006** Mobile a11y snapshot tests per top-level screen (PR #63)
✅ **A11Y-007** Lighthouse a11y audit script + GitHub Actions CI workflow (PR #68)
✅ **A11Y-009** `prefers-reduced-motion` — web global CSS + mobile `useReducedMotion()` (PR #67)
✅ **ON-31** E2E: settings page notification prefs (PR #69)
✅ **ON-37** `ts-prune` unused-exports sweep — removed `getPostHog` + `WatchData` (PR #62)
✅ **ON-48** Neutral design tokens + brief page hex sweep (PR #58)
✅ **TD-05** Regenerate Supabase TS types after messaging migration; removes `as any` in messagesRepository
✅ **UX-01** Loading skeletons across dashboard/journal/team/messages panels (PR #54)
✅ **UX-02** Illustrated empty states — journal, meds, team, vault (PR #70)
✅ **UX-04** Full dark mode via CSS custom properties + ThemeToggle + anti-FOUC script (PR #71)
✅ **UX-05** Mobile journal bottom-sheet + horizontal mood row (PR #60)
✅ **UX-07** Active-panel breadcrumb / dynamic page title (PR #53)
✅ **UX-10** Export styling `/brief/[token]` + `/care/[token]` (PR #55)
✅ **AI assistant** PHI-safe Claude FAB — context-aware suggestions, org-scoped, no PHI sent to API (PR #72)
✅ **Shift calendar** Replace ShiftList with react-big-calendar day/week/month views (PR #66)

### 2026-04-14 parallel agent session (PRs #34–#49)
✅ **ON-21** Web raw-hex audit — all hex replaced with `var(--color-*)` design tokens (PR #34)
✅ **ON-29** Replace `console.log` with project logger in `apps/web` (PR #35)
✅ **PP-004** Unified settings hub at `/settings` — profile, notification prefs, timezone, danger zone (PR #36)
✅ **PP-001** Mobile team admin — change role + remove member with pgTAP coverage (PR #31/#37)
✅ **ON-32** E2E invite-accept happy path + expired-token rejection (PR #38)
✅ **A11Y-001** Web axe-core/playwright — `checkA11y()` helper wired into `e2e/helpers.ts` (PR #39)
✅ **A11Y-002** eslint-plugin-jsx-a11y at `error` severity — `alt-text`, `click-events`, `no-static-element-interactions` (PR #39)
✅ **ON-33** Mobile Sentry breadcrumbs on tRPC errors — procedure name + op type, PHI scrubbed (PR #40)
✅ **ON-20** Mobile `accessibilityLabel` sweep — all icon-only Touchable/Pressable labelled (PR #41)
✅ **A11Y-004** WCAG contrast validator `scripts/a11y-contrast.mjs` — exits non-zero on failure, wired to `pnpm a11y:contrast` (PR #42)
✅ **A11Y-010** Colorblind simulator walkthrough step added to `.claude/rules/ui-standards.md` (PR #42)
✅ **ON-27** Web alt-text audit — all 6 Image elements verified with meaningful alt props (PR #45)
✅ **ON-30** JSDoc on public exports in `packages/shared` (PR #46)
✅ **ON-39** Eliminate `any` types — `ExportDocument`, `careEvents` router, `export/route` fully typed (PR #47)
✅ **ON-47** `data-testid` attrs on `MedicationPanel` + `MedicationChecklist`; E2E TODOs resolved (PR #48)
✅ **ON-34** PostHog funnel events parity audit — `docs/project-info/technology/ANALYTICS_EVENTS.md` (PR #43)
✅ **ON-36** TODO/FIXME audit — `docs/project-info/technology/TODO_AUDIT.md`; 6 deleted, 2 converted to ON-47/ON-48 (PR #43)
✅ **A11Y-003** Mobile `eslint-plugin-react-native-a11y` at `recommended` severity (PR #33)
✅ **ON-26** Mobile empty-state copy pass — Carelog voice with CTA on all screens (PR #32)
✅ **ON-28** Mobile loading skeletons on journal, medications, documents, team index (PR #32)
✅ **ON-43** In-app messaging (DM + group) — `message_threads` + `message_thread_members` + `messages`, RLS, tRPC router, Supabase Realtime web UI, Inngest delayed push (PR #49)
✅ **Security** PostHog contact PHI fix (`distinctId: crypto.randomUUID()`) + WCAG danger token `#c41a1a` (PR #44)
✅ **TD-01** Harden remaining `any` usages (PR #47)
✅ **TD-04** Consolidate `images/` → `apps/web/public/images/` (root dir absent — no-op confirmed)
✅ **UX-06** Sidebar tooltip labels on hover — `TooltipProvider` wraps `<nav>`, icon-only mode wraps each button in `Tooltip`/`TooltipContent side="right"` (2026-04-14)

---

## 8. Human setup (pre-launch)

Canonical references:
- `docs/project-info/runbooks/THIRD_PARTY_SETUP.md` — all third-party service accounts + env vars
- `docs/project-info/runbooks/CI_HEALTH.md` — GitHub billing, secrets, repo settings

These tasks require signing into third-party consoles and cannot be automated:

**Third-party services (THIRD_PARTY_SETUP.md)**
- **Supabase cloud** — project, keys, HIPAA BAA (§1)
- **Vercel** — project + all env vars, `SENTRY_AUTH_TOKEN` (§2)
- **Inngest cloud** — keys + register app post-deploy (§3)
- **Resend** — API key + domain verification (§4)
- **Upstash Redis** — rate-limiting database (§5)
- **Stripe** — account + product + prices + webhook endpoint in live mode (§6)
- **Sentry** — DSN + source maps auth token (§7)
- **PostHog** — project + key + privacy settings (§8)
- **VAPID keys** — generate once, set in Vercel (§9)
- **Firebase / FCM** — Android push (`google-services.json` → EAS) (§10)
- **APNs `.p8` key** — via EAS credentials (§11)
- **Deep-link verification files** — AASA (iOS) + `assetlinks.json` (Android) (§12)

**GitHub / CI prerequisites (CI_HEALTH.md)**
- **GitHub Actions billing** — payment method + spending limit; hard-blocks all CI when failed (§1)
- **`ANTHROPIC_API_KEY` secret** — repo Settings → Secrets → Actions; gates AI security review on every PR (§2)
- **Allow auto-merge** — repo Settings → General → Pull Requests; required for unattended agent PRs (§3)
- **Branch protection on `main`** — current posture permissive; tighten post-launch (§4)

**Local dev (THIRD_PARTY_SETUP.md §14)**
- **Playwright Chromium** — `cd apps/web && npx playwright install chromium` — new machines hit a hard pre-commit failure without this

Claude work that's **gated on the above** (cannot start until the human completes the corresponding step):
- 🧑 **A2** — `supabase link --project-ref <ref>` + `db push` + bucket create + `supabase test db` against cloud *(needs Supabase cloud keys)*
- 🧑 **C3** — update weekly digest FROM address to `notifications@<verified-domain>` *(needs Resend verified domain)*
- 🧑 **PP-008** — Android app-links verification *(needs `assetlinks.json` on a live domain + EAS build SHA-256)*
- 🧑 **TD-03** — Sentry source maps *(needs `SENTRY_AUTH_TOKEN` in Vercel)*

---

## 9. Definition of done (every story)

- [ ] Feature works end-to-end in local dev
- [ ] Role enforcement verified where applicable (wrong role → no access)
- [ ] Vitest and/or pgTAP coverage added for non-trivial logic
- [ ] No Turbopack JSX violations (no template literals in JSX props)
- [ ] Typecheck + lint + test suites green (`pnpm typecheck && pnpm lint && pnpm test`)
- [ ] For mobile UI: spot-check via `/mobile-ui` skill (screenshot at least one state)
- [ ] For web UI: axe hook green (post A11Y-001), respects tokens, keyboard-traversable
- [ ] TECH_DEBT.md updated if a known issue is resolved
- [ ] BUILD_STATUS.md checkbox ticked if relevant

---

## 10. Lifecycle update contract (all agents)

**Single source of truth.** Every planned piece of work lives here. Do not track work in ad-hoc docs, memory, or PR descriptions alone.

**When status changes, update this file in the same commit as the code change:**

| Transition | What to do |
|---|---|
| Picking up a story | Flip `Status:` to `⚡ In progress`, add `Owner:` + `Branch:` |
| Opening a PR | Flip to `🔎 In review`, add `PR: #NNN` |
| Hitting a blocker | Flip to `🔴 Blocked`, add `Blocked by:` with the reason or upstream ID |
| Merging | Move the row to §7 (shipped log) with a one-line summary; delete from §1–§5 |
| Discovering new work | Open a new row with `Status: 🟢 Ready`, pick the right prefix (`TD-*`, `A11Y-*`, `ON-*`, etc.), and leave it unowned |

**`/backlog-sync` runs this reconciliation automatically** against `git log`, open PRs (`gh pr list`), and the shipped log. Invoke it:

- At **session start** when resuming work on this repo
- At **session end** via `/session-end`
- On a **daily cron** via `/schedule` so scheduled agents see fresh state
- Any time the §0 status board looks stale

Never delete a story silently — either move to §7 (shipped) or mark 🧊 with a reason.

---

## 11. Agent contract (what any picking agent can assume)

- Before picking up any row, run `/backlog-sync` and claim the row by flipping its `Status:` to `⚡ In progress` + `Owner:` in the first commit
- `pnpm` at the repo root is the entry point; each app has its own workspace scripts
- `supabase start` must be running for any pgTAP test
- macOS host; `./scripts/mobile-ui.sh` is available for any mobile visual check (iOS or Android)
- `/ollama` is available for mechanical fan-out; `/create-migration` for schema work; `/review` before committing RLS/PHI touches
- **Never** skip hooks (`--no-verify`). If a hook fails, investigate — don't bypass
- **Never** commit to `main` — every story lands on a branch + PR
- If a story is blocked by new information discovered mid-work, update the story's `**Blocked by:**` here and stop — don't improvise scope

### Row template (TD-189 — required for every NEW row, forward-only)

Every row created from 2026-05-20 onward MUST embed these fields in its Notes cell (or heading-block body for §5). Existing rows are grandfathered — do NOT retrofit. The template exists because ~50% of `/opus-on-opus` cycle-1 must-fixes trace to vague/stale row descriptions that go stale against the live codebase before pickup:

- **Surface (verified `<date>`):** `file:line — \`<exact symbol/anchor>\`` — the concrete code location(s) the row concerns, at row-write time.
- **Assertion of current state:** one sentence stating what is true in the code TODAY that motivates this row.
- **Risk:** `LOW` | `MEDIUM` | `HIGH`.
- **Size:** `XS` (<30min) | `S` (~1h) | `M` (~half-day) | `L` (multi-day).
- **Verification command:** a one-shot shell command that confirms or refutes the row's current relevance (lets `/verify-row` re-check the claim at pickup).

The `/verify-row <id>` skill consumes these fields at `/sprint` Step 3a (after Gate 1, before Step 3b): it re-greps the Surface paths/symbols and re-runs the Verification command against current `main`, emitting `current` / `stale: <diff>` / `wrong: <evidence>` (warn-only — surfaces for operator ack, does not hard-block). A row missing the template fields can't be machine-verified, so the verdict degrades to a manual-review prompt.
