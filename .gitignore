node_modules
.env
.env.local
env.local
.turbo
dist
.next
.expo
.worktrees
.eslintcache
apps/web/.eslintcache

# Sentry Config File
.env.sentry-build-plugin

# Playwright production session (contains auth cookies)
.playwright/

# Generated reports / analysis artifacts
apps/web/sonar-report.xml

# Claude memory search index
.memsearch/

# Expo prebuild output — never commit
apps/mobile/ios/

# Mobile lockfile — managed by root pnpm; npm install must NOT be run here
apps/mobile/package-lock.json

# /live-test runtime state (gitignored; flows/ IS committed)
.live-test/state/
.live-test/runs/

# Vitest browser-mode test artifacts (screenshots + DOM snapshots)
**/.vitest-attachments/
**/__tests__/**/__screenshots__/

# Playwright runtime artifacts (re-generated each run)
test-results/

# Lighthouse CI runtime artifacts
.lighthouseci/

# Codex adversarial-review run artifacts (per-session, ephemeral)
.codex-runs/

# Vercel CLI link state (per-developer, contains project IDs)
.vercel/

# Sibling-of-.worktrees variant (older worktree placement)
worktrees/

# Generated runbook HTML (source .md is the artifact)
docs/project-info/runbooks/*.html
.mcp.json

# Claude Code state — track multi-session substrate, ignore everything else
.claude/*
!.claude/state/
.claude/state/*
!.claude/state/sessions/
.claude/state/sessions/*
!.claude/state/sessions/INDEX.md
!.claude/state/claims.json
.claude/state/.counter.lock
